# 认知训练 App · 方案 v0.8

> **v0.7 → v0.8 变更（已实现）**
>
> 用户提了五件事，其中一件是真机上才暴露的重度 bug。
>
> 1. **修「httpPost is not a function」（最重要）**：`httpPost` / `httpGet` 写在了
>    `MainActivity` 上而不是 `Bridge` 内部类里，所以 WebView 看不到它们——
>    **真机上语音陪练、表达体检、真实复盘三个模块全废**，而浏览器里走的是
>    `/api/proxy`，正好绕开原生桥，所以本地怎么测都是好的。已改为 Bridge 内的
>    转发方法，并新增 `tools/test_bridge.js`：静态交叉检查「网页调用的每个
>    EQNative.X 都必须在 Bridge 里带 @JavascriptInterface」，把这类错误挡在构建前。
>    同时统一约定：调用桥一律写 `EQNative.xxx`，不许用局部别名（否则静态检查会漏）。
> 2. **闸门改成全屏浮层**：用户反馈「打开游戏时是直接弹开」——因为上一版是
>    `startActivity(MainActivity)`，游戏已经开起来了，人是事后被拽走的。
>    现在改为 `performGlobalAction(HOME)` 把目标应用推下去 + 叠一层
>    `TYPE_APPLICATION_OVERLAY` 全屏浮层。**诚实说明**：无障碍服务无法阻止另一个
>    应用启动，浮层按 Home 仍能绕；真·打不开要 `DevicePolicyManager.setPackagesSuspended`。
> 3. **UI 重构**：7 个平铺 tab → 5 个按「你要做什么」分层（今天 / 练习 / AI / 成长 / 课程）。
>    AI 三个功能归一处。新增「今天」页作为落地页。二级切换条统一在 `go()` 注入一次。
> 4. **针对「憋需求 + 敏感 + 幽默」画像的定向内容**：8 张卡（b01–b08）+ 2 课（p20/p21）
>    + 新误读类型「含蓄化」。依据：透明度错觉（Gilovich 等 1998，敲歌实验 50% vs 3%；
>    Savitsky & Gilovich 2003 发现讲清这个偏差本身就能降低焦虑）、自我沉默（Jack & Dill）、
>    幽默四风格（Martin；自贬型与满意度负相关；Winterheld 等 2013 冲突里自贬型反而更糟）。
> 5. **题表面变体**：每张卡 2 条 `variants`（换人／场合／措辞，考点与选项完全不动），
>    渲染时随机取一条。变体在 `buildQueue` 落地，一组刷完前不换场景。
> 6. 内容规模：卡片 54→62（+ 124 条变体），微课 27→29。新增 `tools/test_content.js`（15 项）。
>    APK 2.5（versionCode 7），权限 5 个（新增 SYSTEM_ALERT_WINDOW）。
>
> **v0.7.1（2.6）变更（已实现）**
>
> 用户要求「第 2 次必须完成，同时留退路——卸载之后整个阻拦消失」。
>
> 1. **第二条要求排除了最强的那种做法**：`DevicePolicyManager.setPackagesSuspended`
>    和 `pm disable-user` 确实能真拦住，但状态记在**系统**里，卸载发起方之后可能残留。
>    所以闸门全部跑在本应用进程内（无障碍 + TYPE_APPLICATION_OVERLAY），**卸载即消失**。
> 2. **新增不可残留断言**（`tools/test_bridge.js`）：扫全部 Java 源码，出现
>    `DevicePolicyManager` / `setPackagesSuspended` / `setApplicationEnabledSetting` /
>    `pm disable-user` 即失败。这条性质从此有测试兜着，不靠自觉。
>    （扫描前剥注释——`GateService` 的注释里正好写着「我为什么不用它」，不剥就是误报。）
> 3. **第二次拦得更实**：浮层改为**可获焦点**（原来用 FLAG_NOT_FOCUSABLE，按键会穿到下层），
>    返回键被吃掉；`canSkip=false` 时跳过按钮整个不渲染，并说明原因。
> 4. **退路写在浮层界面上**（不是只写在代码注释里）：强行停止 / 关无障碍 / 卸载。
> 5. **Shizuku 没做，理由写在 README**：它能让游戏一启动就被杀（同样零残留），
>    但依赖常驻运行，没在跑时拦截会**静默降级**——那种「看起来在保护、实际没有」
>    比不做更糟。要做就得同时做显式警告。
> 6. APK 2.6（versionCode 8）。测试合计 143 项。
>
> **v0.7.2（2.7）变更（已实现）**
>
> 用户要求：「把前两个退路都删掉，只留最后一个直接卸载的退路，这是强制我学习」，
> 并且「直接给他杀掉游戏」。
>
> 1. **前两条退路删不掉，这件事必须说清楚。** 「强行停止本应用」「关掉无障碍服务」
>    是**系统给用户的权力**，不是这个 App 提供的选项——任何应用都关不掉它们
>    （能关掉就是在利用系统漏洞，安全模式和 ADB 照样绕得过去）。
>    所以这次删掉的是**提示，不是路径**：浮层 setText 里不再写这两条，但注释里
>    保留解释（说明为什么删不掉），防止下一个人误以为「没写 = 不存在」。
>    **「删了提示」被当成「堵死了路」是这个改动最大的误读风险，所以两个地方都写死了。**
> 2. **浮层上仍然必须写着一个真实出口**（现在只剩卸载），并写明代价（进度／预案／
>    身份陈述会一并清掉）。这条被加进断言：不写出口的浮层等于在说「你被困住了」，
>    那正是要防的东西。让「想退出」等于「全部归零」是个会反过来惩罚用户的设计，
>    至少得让它**知情**。
> 3. **杀掉游戏：用 root，不用 Shizuku。** 构建是手工的（aapt2 → javac → d8），
>    **没有依赖管理**——Shizuku 要手工 vendor 它的 API 和 provider，root 只要一句
>    `su -c am force-stop`。效果一样，代价差一个数量级。`am force-stop` 只结束进程、
>    不写持久状态，所以「卸载即消失」仍然成立（这正是它和 `setPackagesSuspended`
>    的本质区别）。新增 `RootGate.java`。
> 4. **这个功能的失败模式必须在界面上显示**——这正是我在 v0.7.1 里给 Shizuku 定的
>    那个条件：root 不可用时它**不报错、只是什么都不做**，你会以为还有保护。
>    所以：root 状态是**三态**（未检测／可用／不可用，压成布尔会把「未知」显示成
>    「不可用」，那是误报）；**探测结果不跨启动保留**（root 会变：系统更新、换机、
>    Magisk 被关）；只有真探测到才允许打开；探测失败时**顺手把开关关掉**；
>    探测异步执行（su 授权弹窗在等人点时同步等会卡住线程）。
> 5. **包名会拼进 shell 命令，先做白名单正则校验再拼**（防命令注入）。来源是本地设置
>    也一样，没有理由留这个口子。断言盯着它。
> 6. 断言从「三个退路都写在浮层上」改成「浮层写明唯一退路并写明代价」+
>    「不再引导去找系统级绕开方式（但注释保留解释）」+「深度拦截带可见状态」+
>    「包名白名单校验」。桥测试 9→12 项。
> 7. APK 2.7（versionCode 9）。测试合计 146 项。
>
> **v0.7.3（2.8）变更（已实现）**
>
> 用户把上一条约束放宽了一半，而且放宽的方向是对的：
> 「卸载一些残留的文本数据是可以留下来的，除非我再用清理软件给它清掉。
> 然后所有的进程，什么代码，脚本什么的那些都要删掉，只留下文本数据。」
>
> 1. **前半句修掉了一个真实缺陷。** 卸载是闸门唯一的出口，而那个出口的代价原本是
>    「全部归零」——几个月的偏差画像、用户自己写的预案，一次卸载全没。
>    v0.7.2 里我自己就标过这一点（「让想退出等于全部归零，这个设计会反过来惩罚
>    用户」），现在真的去掉了这个惩罚。新增 `BackupStore.java`：把 state 作为
>    **纯文本**写到 `下载/认知训练/认知训练-进度备份.json`。外部存储卸载不动它。
> 2. **重装后提示恢复**（`maybeOfferRestore`）：只在本地是空的时候问一句，
>    **不是静默恢复**——静默恢复会让「卸载了又装回来、数据自己全回来了」
>    看起来像软件删不干净。本地已有进度时不弹框，避免诱导用旧备份覆盖新进度。
> 3. **自动写（节流 90 秒）+ 状态常驻可见**，而不是一个「导出」按钮：
>    手动导出人一定会忘，忘了的代价是丢掉几个月记录。
> 4. **写完立刻回读校验。** 卸载重装后能不能读回同一个文件**不保证**——文件一定
>    还在，但 MediaStore 的归属按包名/UID 记，重装后 UID 变了可能就不给读。
>    所以读不回来就如实显示「写进去了，但读不回来……文件本身没丢」，
>    而不是显示一个假的「已备份」。
> 5. 旧系统的权限带 `maxSdkVersion="28"`（Android 10 起走 MediaStore，不需要权限）。
>    每次写之前先删同名 MediaStore 条目，否则会堆出「…(1).json」「…(2).json」。
> 6. **后半句（不许留能跑的东西）是更要紧的一半**，所以加了一组断言：没有任何一处
>    往磁盘写脚本/可执行文件（.sh/.bash/.dex/.apk/.jar/.so/.js）；外部存储只写那一个
>    .json；root 必须继续是一次性 `ProcessBuilder`、不落盘成脚本。
>    **尤其钉住了「不许把 root 命令写成脚本再执行」——那正是最容易为了「拦得更狠」
>    而做、同时又把用户原始要求破坏掉的一步。**
> 7. **踩到的坑（值得记）：** `boot()` 里 `syncGate()` 会调 `save(false)`，那一下就把
>    LS_KEY 写上了；如果到「要不要提示恢复」时才读 localStorage，读到的永远是「有」，
>    恢复提示永远不会出现，而且**静默**不出现。改为在模块初始化时抓快照
>    （`HAD_LOCAL_AT_START`）。这个 bug 在跑测试之前就发现并修掉了。
> 8. 新增 `tools/check_settings_ui.js`（浏览器端 20 项，需要先起 `server.py`）：
>    设置页真实渲染、root 三态、备份三态、「写进去了但读不回来」文案、
>    重装恢复的四条路径。**最后这组跑的是离线单文件版，也就是 APK 里装的那一份**
>    （第一次写成跑本地服务，被 server 的 progress.json 喂了进度、
>    于是「本地是空的」这个前提不成立，现象是「该弹的框没弹」——很容易误判成功能坏了）。
> 9. APK 2.8（versionCode 10），权限 7 个（新增两个外部存储权限，带 maxSdkVersion=28）。
>    测试合计 114 + 37 + 20 = 171 项。
>
> **v0.7.4（2.9）变更（已实现）**
>
> 用户报了一个真实场景：健身群（6 人）里有人发了沙县小吃的照片，北玄和樊明鑫
> 都说自己也在沙县，他接着发了
> 「不错，又有新人加入了」+ 猫图 +「等你练的比他好了，以后叫他樊师弟就好」，
> 然后群里就没人说话了。「然后我就感觉话题停住了。」
>
> 1. **诊断：话题不是自己停的，是被换掉的。** 前两句是极好的共同话题（具体、在场、
>    谁都接得上），他跳过了它，退到外面评论这个局面，紧接着又给两个人排了名次。
>    名次类的话不给人「顺着说」的选项，只能接受或反驳——接受等于承认自己低，
>    反驳等于不给面子，所以最省事的回应是沉默或一个尴尬表情。
>    截图里北玄回的那个挠头出汗的兔子，就是这件事的可视化。
> 2. **题库原来 62 张全是输入侧**（她这句话什么意思），这是第一次补**输出侧**
>    （我这句发出去为什么话题死了）。新增 4 张（`type: talk`，id t01–t04，
>    新领域计数进「泛社交」/「朋友」），新增微课 p30《接话：你这句话发出去，
>    别人能接什么》。新增 `tools/add_cards_talk.py` / `add_lesson_talk.py`。
> 3. **两个新的偏差类型，都在输出侧**：「评论代替接话」（用对人和局面的评价替掉
>    对方正在讲的那件事）、「替人定位置」（当众给人排名次）。它们和原来的 10 个
>    放在同一张表里，因为偏差画像要能同时显示「你读偏的方向」和「你说死话的方式」
>    ——它们是同一个人的两种表现。为此加了第二个 `type` 标签「怎么接话」。
> 4. **防「把新规则当锤子」是刻意设计的。** t03 的正确答案是「你把话题接到了
>    自己身上」，而 **B 选项就是「评论代替接话」**——刚学会这条规则的人最容易
>    套错，选它会计入偏差。t04 同理，B 是「替人定位置」。
>    只教一条规则而不教它的边界，等于换一种误读。
> 5. **渲染：`quote` 改成多行对话记录。** 卡片原来是单句，`.quote` 加了
>    `white-space: pre-line`，否则换行会被 HTML 折掉、整段对话挤成一行。
>    新增 `tools/check_talk_cards.js`（12 项，跑离线版）验证真实渲染——
>    包括 computed style 是 pre-line、quote 里保留了换行、高度 >100px。
> 6. **顺手修掉一个「记得改清单」的隐患：** `test_content.js` 原来硬编码了一份
>    误读类型清单。新类型如果不在 app.js 的 `BIAS_TYPES` 里，`recordBias()` 会直接
>    return——卡片照常出题判分，只是那个偏差**永远不出现在画像里**，零报错。
>    现在这份清单从 app.js 源码解析出来，并且反向报告「定义了但没有任何题目用到」
>    的类型（只提示不失败）。
> 7. APK 2.9（versionCode 11）。测试合计 114 + 37 + 32 = 183 项。
>
> **v0.7.5（2.10）变更（已实现）**
>
> 用户报了三件事：语音陪练「模型返回 404」；每日任务界面「不明显」；题库感觉还是少。
> 他明确要求：**先解决前两个，题库只提建议。**
>
> 1. **404 的真因是两个 bug 叠在一起**，而且都不在「模型」上：
>    a. 「接口地址」这个输入框里的东西是**被直接 POST** 的（不拼路径），但
>       这四个字读起来像「服务地址」。用户填了 `https://api.deepseek.com/anthropic`
>       ——那是 Anthropic 协议的**基础地址**，而且是**另一套协议**（max_tokens 必填、
>       system 单独字段、不认 response_format），必然 404。
>    b. 原来的报错只从 JSON 里取 `error.message`，而这个网关的报错是**纯文本**
>       （实测 `Authentication Fails (governor)`，不是 JSON）。JSON.parse 一抛异常，
>       服务端的话被整段丢掉，界面上只剩一个「404」。
>       **「404 但没有任何说明」恰好是「路径不对」的典型特征**，用户完全无从下手。
>    修法：新增 `normalizeEndpoint()`（能补的补全，补了什么要说出来，不许偷改用户输入；
>    `/anthropic` 明确拦下并说明协议不对）+ 新增 `httpProblem()`（**非 JSON 的报错原文
>    也必须露出来**，并附上实际 POST 的地址、模型名、按状态码给方向）。
>    设置页加了实时预览「实际会 POST 到哪个地址」，填的时候就挡掉 404。
>    新增 `tools/check_llm_errors.js`（25 项）。
> 2. **每日任务界面：一个设计问题 + 一个真 bug。**
>    a. 设计：原来那块是个**仪表盘**——三个数字方块写着 `0 / 需 4`，配一句
>       「还差 5 步」。要读的人自己做减法；而且「加练」跟两个必做项并排、字号一样，
>       看起来也像任务。改成**清单**：一句人话说清「还差：2 张卡片 + 1 条微课」，
>       每项单独一行带勾选状态，必做与可选分开，按钮点名下一件具体的事。
>    b. **进度条一直是不可见的。** `.tbar` 和 `.tbar i` 只定义在 `.vh-temp` 下，
>       今日卡片上那条 `.tbar.today-bar` 拿到了 8px 高度，却没有底色、里面的 `i`
>       也不是 `block`，**填充宽度恒为 0**。「完成到哪了」这条最该一眼看到的信息，
>       从来没有显示过。这说明「界面不明显」这个感受里，有一部分是实打实的 bug。
>       修法：把 `.tbar / .tbar i` 提升成**基础样式**，`.vh-temp` 只覆盖尺寸。
>    新增 `tools/check_today_ui.js`（24 项，跑离线版）——其中一条直接量进度条的
>    底色和填充宽度，因为只看「DOM 里有没有」是抓不到这个 bug 的。
> 3. APK 2.10（versionCode 12）。测试合计 114 + 37 + 24 + 25 + 12 + 20 = 232 项。
>
> **v0.8（2.11）变更（已实现）**
>
> 用户提了三件事：题库要「扩充许多、全面一点」；要「根据每日的答题情况调整下一次的
> 出题方向，有偏重」；以及「训练我说话之前思考，我经常说话不动脑子，这个是硬伤——
> 查查资料，看看有没有什么方法」。
>
> 1. **查资料的结论推翻了我本来会做的设计。** 我原本打算加「说话前停三秒」那种摩擦。
>    Wilkinson（1975, J Abnorm Child Psychol）把冲动的孩子分三组：纯延迟训练、
>    语言自我指导训练、对照。**两组的反应延迟都变长了，但只有语言自我指导那一组
>    错误减少。** 也就是说停顿本身不改善判断，停顿里**装着的那一步**才改善判断。
>    所以没做计时器，而是把「那一步」做成**必须走过的关口**（下一条）。
> 2. **判局机制（读局卡）：选项被挡在「这是什么局？」后面。**
>    读局卡在给出四个动作选项之前，先要求判断这是哪种局（分享/报喜、倒苦水、求建议、
>    邀约、要确认、客套收尾、自谦、纯闲聊）。选项在这之前**真的不渲染**——否则这一步
>    就只是装饰。判错会记进偏差画像的「误判场合」，而且和选错动作**分开记**
>    （id 带 `#genre`），因为「分类错了」和「分类对了但动作选错」是两个能力。
>    新增 12 张读局卡（r01–r12），每张带 `state` 字段写那一步的结论——它**不在答题前
>    显示**，答完才出现，提前给就等于把答案送出去。
> 3. **偏重出题：加权随机，不是硬筛。** `biasFocus()` 取偏差画像里最高的一档
>    （样本不足 6 次不动手，跟画像本身「样本太小」那条线一致），`cardWeight()` 给命中
>    弱项的卡、以及做错过的卡加权，`weightedPick()` 做加权随机。**为什么不只出弱项**：
>    只练弱项会把它和惩罚绑在一起，而且其他类型会永远没数据、画像再也调不动——
>    反馈回路需要一个探针。界面上有 `focusBar()` 说明最近偏重哪几类、为什么，
>    不让它变成黑箱。
> 4. **今日第三人称复盘**，这一条有一项稀有的证据：Grossmann & Kross（2014,
>    Psychological Science）发现人对自己的困境做的推理明显差于给别人的建议
>    （所罗门悖论），但换成第三人称（用自己名字或「你」）差距就消失；
>    Grossmann 等（2021, Psychological Science）两项**预注册纵向实验（N=555）**
>    用第三人称日记在一个月后测到智慧推理和情绪复杂度的增长——这是「智慧可训练」的
>    第一个纵向证据。所以做成每天写两三句**真实发生过的事**，用自己名字或「你」写。
>    不进达标线（用户要求任务量少），记为加练，但摆在今天页最显眼处。
> 5. **诚实边界写进了微课 p33 和复盘卡**：这些证据主要来自书面反思和实验室任务，
>    **不是实时对话**。社交技能训练的迁移研究里，只在抽离场景里练的没有泛化
>    （Berler 等 1982：角色扮演里学会了，回到真实环境表现不变），做了多对象／多场景／
>    真实环境训练的能泛化并保持到 3 个月（Hansen 等 1989）。所以真正起作用的地方
>    不在屏幕上；也据此写明「说话快本身不是缺陷」，避免把它变成另一套表演。
> 6. **题库扩到 84 张 + 变体从 2 条加到 4 条（336 条）**，微课 33 节。
>    变体加量不是凑数：迁移研究里「多场景」正是让训练泛化的条件之一。
> 7. **踩到的坑：**
>    a. `bumpDaily('extra')` 静默失效——函数里只有 card/lesson 两个分支，写 'extra'
>       走不进任何分支，加练计数永远是 0，而调用方以为记上了。补了显式分支。
>    b. 复盘提示写进 `#reviewMsg` 后被 `renderToday()` 当场重画冲掉——这个坑这个项目
>       已经踩过一次（身份陈述那次），改用 toast。
>    c. `const GENRES` 是多行声明，测试按行切开解析，漏掉后三个选项，
>       误报「不在可点清单里」。改按 `];` 切。
>    d. **一条新断言当场抓到 11 张永远判不对的卡**：卡片里写 `分享 / 报喜`，
>       而 app.js 里是 `报喜`——按钮照常渲染、点哪个都算错、画像还一直记「误判场合」。
>       同时还抓到 6 个把「局名」误当 `err` 类型用的选项。
>    e. 生成变体的工具原来「已经有 variants 的卡不重跑」，PER_CARD 从 2 调到 4 时
>       一张都不生成却打印成功；而且不认缓存，中途被 kill 就全部重来。两处都修了。
>    f. 后台跑的变体任务在被 kill 之前持有旧的内存副本，它最后的一次写入把新加的
>       6 张卡覆盖掉了。教训：**内容脚本和长跑生成任务不要同时写同一个 JSON。**
> 8. **顺手修掉一个存在很久的观感 bug**：内容里写了 100 多处 `**强调**`，
>    但一直没有渲染器，所有地方都在显示字面的星号——几乎每张卡的解释里都有。
>    新增 `rich()`（先转义再替换，所以不可能引入 XSS），只用在正文类字段上，
>    不用在 input 的 value/placeholder 里。
> 9. APK 2.11（versionCode 13）。测试合计 117 + 37 + 24 + 25 + 22 + 20 + 12 = 257 项。
>
> **v0.8.1（2.12）变更（已实现）：内容质检**
>
> 用户问：「那些题库都是精品的吗？不要有次品。」
>
> 这个问题问得对：到那时为止所有测试**只验结构**（四个选项、有 why、有 plan、
> 变体字段完整），从来没有一条验过内容好不好。所以写了 `tools/audit_quality.py`，
> 查「结构没问题但内容是次品」的几种可判定形态。**结果是：不是精品，抓出两类真次品。**
>
> 1. **正确答案明显最长（会教出「挑最长」的作弊策略）。**
>    初查 25 张卡的正确选项超过最长干扰项 1.5 倍，最极端 2.76 倍
>    （我新写的 t01：正确 72 字 vs 干扰项平均 26 字）。
>    **修法是把干扰项写足，不是把正确答案砍短**——干扰项短是我写得偷懒，
>    而一个像样的干扰项本来就该和正确答案一样具体、一样有说服力；
>    写足之后卡片反而更难、更有用。共改写 81 条干扰项，涉及 25 张卡。
>    复测：0 张超过 1.5 倍。
>    （口径差点搞错：`ok` 里列的是**可接受的替代答案**，它们也标 `err='正解'`，
>    拿它们当「干扰项」做参照会误报。按「真正错的选项」重算才准。）
> 2. **变体之间近乎同一句话。** 13 对变体的相似度 ≥0.80，最极端的 0.94。
>    典型：「你上次说的那家咖啡店，还算数吗？」vs「你上次说的那家甜品店，还算数吗？」
>    ——换个名词就算一条新变体。**这不是外观问题**：变体的全部意义就是换检索线索
>    （多场景正是训练能泛化到真实对话的条件之一），如果四条的检索线索一样，
>    这个机制等于没生效，而界面上一切正常。共换掉 14 条，几乎全部换了句式结构
>    而不只是换词。复测：0 对 ≥0.80。
> 3. 顺手在生成器（`gen_variants.py`）里加了**相似度护栏**：新变体的句子跟原句
>    或已有变体相似度 ≥0.75 就直接丢弃。原来只查「完全相同」，所以才产出上面那种东西。
> 4. **把两个发现固化成断言**（`test_content.js` 从 18 项增到 21 项），
>    用 bigram Dice 算相似度。它们都是会静默削弱训练效果的形态，必须有测试守着。
> 5. 未被判定为次品的、但值得说明的：`g01` 的四条变体相似度 0.82，
>    但它们问的是不同的事（轻松/自由/累/难进），判断为合格，保留。
> 6. APK 2.12（versionCode 14）。测试合计 120 + 37 + 24 + 25 + 22 + 20 + 12 = 260 项。
>
> **v0.9（2.13）变更（已实现）**
>
> 用户提了五件事：界面太复杂要归并；AI 要能语音对话；还在报 404；解释性的文字
> 收进「!」小弹窗；删掉现实任务。
>
> 1. **404：查清了「不是模型名的问题」。** 本机的生成脚本（gen_diagnosis.py）用的是
>    `BASE_URL = https://api.deepseek.com` + `MODEL = deepseek-v4-pro`，
>    而它**成功生成了 336 条变体**——所以模型名和端点的默认值都是对的。
>    用户填的 `https://api.deepseek.com/anthropic` 是 Anthropic 协议的**基础地址**，
>    那是 404 的唯一来源。2.11 的规范化已经会自动纠正它。
>    这一版再加一道兜底：**404 就用正式端点自动重试一次**，成了就把「自动改过地址」
>    记在设置页上说明白（不悄悄改）。只对 404 这样做——401 是密钥、400 是请求形状，
>    重试都是白费。
>    （查证过程本身有个发现：`.env` 里那把 key 现在已经失效了（401），
>    所以无法用它复现用户的 404。这一点在报告里说清楚了，没有假装验证过。）
> 2. **「!」小弹窗。** 用户的原话：「有些解释的地方，能不写出来就不写出来，
>    需要写出来，也可以弄个感叹号，我点击一下，它才会有一个小窗口弹出来。」
>    做法：正文只留**结论**，「为什么」「另一种可能」收进「!」。
>    一条自我约束：**「要记住的原理」和「真要开口时」不进弹窗**——那两句是产品本身，
>    藏起来等于没讲。收进去的只有推导过程。不写出来的是过程，不是内容。
> 3. **删掉现实任务。** 牵连面比看起来大：**关卡 4 的门槛原本就建立在它上面**
>    （`gate.type = followThrough`），直接删会让那一关无法判定。
>    所以给关卡 4 换了一个在 App 内也测得到、而且是真技能的判据：
>    **读局卡那一步（这是什么局）的准确率**（判局满 20 次且 ≥70%）。
>    理由：做得多不等于判得准，而判局是后面所有动作的前提。
>    同批删掉：assignments（7 个任务）、offerAssignment/checkIn/assignmentCard/
>    assignmentPool/followThrough、今天页的现实任务卡、成长页的对账次数与执行率。
>    **一句必须留下的话**：现实任务原本是这个 App 里唯一把练习牵到屏幕外面的东西。
>    删掉之后那个位置只剩「今日第三人称复盘」——而迁移研究说得很清楚，
>    只在抽离场景里练是不泛化的。这一点写进了 stages.json 的 note 和天花板声明。
> 4. **新增断言**：关卡用的 `gate.type` 必须是界面真的会判的类型（写错一个字母，
>    那一关就永远过不去或者永远自动过，而且不报错）。同时补了 5 条 genreAcc 的
>    边界测试，包括「只判 10 次全对不能算过」和「动作作答不能混进判局统计」。
> 5. APK 2.13（versionCode 15）。测试合计 118 + 37 + 24 + 25 + 22 + 20 + 12 = 258 项。
>
> **本次没做完的两件（已在 v0.9.1 / 2.14 补上，判断过程保留）**：
>   - 界面的归并简化（用户说「很多窗口可以归为一类」）只做了「!」这一步，
>     底部五个 tab 与二级子页的结构没动。
>     → **2.14 已做**：5 tab → 4 tab，卡片页下的第三层消掉。
>   - AI 的语音对话（像大模型那样说一句答一句）没做。现有的语音陪练是
>     「系统给你一句台词 + 语音识别」，不是自由对话；要改成自由对话需要
>     接一个新的对话回路（STT → 模型 → TTS，带打断和轮次控制），
>     属于新功能，不是改配置。
>     → **2.14 已做，但结论和这里的判断不同**：读代码后发现对话回路**本来就有**
>     （`nextTurn` → `llmCall` → 她带语气/评分/内心活动地回应），缺的只是
>     「场景由提示词生成」这个入口。所以没有新写回路，只补了入口——
>     这一条记错的判断留在这里，比改掉更有用。
>
> **v0.9.1（2.14）变更（已实现）**
>
> 用户在这一版前的最后指令：「你自己决定界面规划。只要分成几个常用功能，用的最多的。
> 不要把它摊开。语音功能选后者，先给提示词，然后建造一个场景。」
> （「后者」指的是我在两版之间给的选项：自由聊天 vs 场景对话——他选了场景对话。）
>
> **1. 界面：5 tab → 4 tab，第三层彻底消掉。**
> 「我的预案 / 偏差画像」原来挂在卡片页下当子视图，于是卡片 → 子视图 → 卡片内容成了三层；
> 但这两个东西都不是「刷卡片」，它们是对**已经积累下来的记录**做回顾，和「阶段评估」同类，
> 所以并进「成长」（成长页直接给三块：阶段 / 画像 / 预案）。微课不是练习、是阅读，
> 但为了不再多一个 tab，和「练习」放在一起。现在四个是：今天 / 练习 / AI / 成长。
> 实现上 `growthSubview` + `SUBNAV.growth` 新增；`setCardsView` 改成只做重定向（兼容旧调用）；
> `cardsNav()` 返回空串。二级切换条仍然**只在 `go()` 里注入一次**——这个坑的说明还在下一节。
>
> **2. AI 语音对话：先给提示词，再建造一个场景。**
>
> *先澄清一个事实（我读代码后才发现，值得记下来）*：**对话引擎本来就在**。
> 原来的「语音陪练」不是念台词，它已经是「按住说话 → 模型演对方 → 每轮告诉你他心里在想什么」，
> 带轮次、温度条、追问计数。真正缺的只有用户点出来的那一步：
> **场景原来是写死的 6 个，不能从自己的提示词生成。** 所以这不是新功能，是补一个前端入口。
>
> 做法：
>   - 无会话时渲染**建场景页**：一句提示词（多行框）+ 6 条示例提示词，
>     点示例是**填进输入框再改**，不是直接开跑。
>   - `buildScenario(prompt)` 调一次模型，要求返回
>     `{title, stage, difficulty, her, her_state, opening, goal, trap}`
>     ——**和 `data/scenarios.json` 完全同形状**，所以下面那套对话机制一行都没动。
>   - `startSession` 改成「场景对象或 id」都收；那 6 个老场景按 id 照样能开，两条路都通。
>
> *刻意没做成开放聊天。* 理由不是界面，是训练目标：开放聊天里模型会配合你、顺着你说，
> 你练的是「聊得下去」；场景对话里模型有立场、有没说出口的期待、会因为你的失分降温，
> 你练的是「在具体情况下做对那一个动作」。这个 App 的全部内容都指向后者——
> 换成开放聊天等于换了一个产品。
>
> **一个必须挡住的静默失败**：建场景是一次模型调用，返回值什么都有可能（多了外层键、少了开场白）。
> 如果只是把返回值往 session 里一塞，界面会正常切到对话页，然后卡在「她说：undefined」——
> **用户在界面上看不出失败，只会觉得「语音功能坏了」**（这个项目已经踩过好几次同类坑）。
> 所以：缺 `opening` 直接报错、绝不放行；模型多套一层外层键时自动拆开
> （在 `Object.values` 里找带 `opening` 的那层），不白报错。
> `tools/check_scene_talk.js`（30 项）三层都盯着：界面（提示词在前、示例可点、缺密钥有出口）、
> 坏输入（空提示词 / 缺开场白 / 外层套一层都不许变成能进的会话）、以及进得去会话。
>
> **3. APK 2.14（versionCode 16）。** 新增浏览器检查 `check_scene_talk.js`。
> README 里顺手清掉两类会过期的东西：一是「测试合计 N 项」那种计数
> （改成逐条写它盯什么——数字每加一条断言就过期，而过期的数字看起来仍像事实），
> 二是三处「语音陪练」旧称。
>
> **4. 顺手修的仓库不一致（如实记下）。** `eq-app/android/` 下的源码副本还是最早那版脚手架
> （versionCode 2、只有 `MainActivity.java`），而真正构建用的是 `E:\android-build\app`
> （6 个源文件、versionCode 15）。已把目录和产物同步成实际构建的那一份。
> 桌面上的 `认知训练.apk`（未加版本号那个）原本是 **2.6**——最容易拿到的文件名反而是最旧的，
> 已用 2.14 覆盖，2.13 保留在同名的带版本号文件里可回退。
>
> **v0.9.2（2.15）变更（已实现）：免提语音 + 随机场景 + 对话后复盘**
>
> 用户纠正了我上一版的理解：
> 「我说的语音功能是跟之前一样的，但是你要加入一个语音输入，相当于跟大模型对话。
>   是自己选择场景，或者说你随机给我一些场景，让我自己来训练，随机的。
>   然后对话完之后给我评价，以及改进措施。」
>
> **先说这次误解。** 我上一版把「语音对话」理解成了「把场景改成用提示词生成」，
> 于是做了「提示词 → 场景」那个入口。而用户要的是**输入方式**的改变：
> 不要按住说话，要像跟大模型说话那样说完自己发出去。
> 两者的界面看起来都像对话，所以我没察觉做偏了。
> 教训：听到「加个语音对话」要分开问两件事——**输入方式的问题？还是场景来源的问题？**
>
> **1. 免提（这是「相当于跟大模型对话」的核心）。**
> 原来只有「按住说话」：按住才收音、松开才发。那是对讲机不是对话——手不能离开屏幕、
> 中途不能停下来想一下。现在的循环是：收音 → 你说话 → 系统判你说完了 → 发出去
> → 对方在想 → 对方出声 → 说完 → 自动交回麦克风。
>
> **没有自己写 VAD**：Android 的 `SpeechRecognizer` 本来就会在静音后自动结束并回调
> `onResults`，这一层只是把那个回调接起来。
>
> 三件专门处理过的事，因为它们的失败**在界面上看不出来**：
>   - **绝不在对方出声时开麦**，否则对方的话被自己录进去、变成自问自答。
>     只有收到 onSpeakDone 才重新收音，再多等 350ms（有些机型回调早于尾音）。
>   - **空结果不无限重试**：连续 3 次就停，把控制权交回来，而不是让麦克风一直开着耗电。
>   - **权限/引擎错误不静默重试**，说清楚并关掉免提，而且**写进设置**
>     （下次进来原因一样，不记住就是每次白开一次麦克风）。空结果只在本次暂停，不写设置。
>
> 按钮的文案**就是点下去的后果**（开始说 / 说完了发出 / 打断他），四种状态四种含义。
> 一个关不掉的麦克风比没有这个功能更糟，所以这条是硬要求。
>
> **2. 随机场景。** 用户说「或者说你随机给我一些场景，让我自己来训练，随机的」。
> 随机不是图省事：**自己选题会稳定地偏向自己已经擅长的那类**，练得舒服但练不到短板。
> 所以池子由零件拼出来（谁 / 在哪 / 练什么 / 难在哪 / 最容易怎么错），
> 再叠上 `biasFocus()` 的弱项，让随机偏向弱项而不是均匀随机。
>
> **3. 补回「直接选一个现成场景」。** 上一版我改设置页时把「点开就进」的入口
> 顺手删掉了（只剩提示词输入框），用户这次明确要「自己选择场景」，所以补回来。
> 现在三条路并存：写提示词造 / 随机出题 / 点开现成的。
>
> **4. 对话后的评价 + 改进措施。** 原来的结束页全是**统计**（接住了几次、温度、
> 提问数）——统计是用户自己看出来的，评价是有人替他点破。加一次模型调用做复盘：
> 整体评价 / 反复出现的模式 / 做对的 / 要改的动作 + **下一次可以这么说**的原话 /
> 下一局只盯这一件事。
> 三条硬约束写进提示词（这类输出最容易烂成鸡汤）：必须引用他真正说过的原话
> （所以每轮把 `userText` 记进了记录——原来只有模型判定的 signal）、
> **不许恭维**、每条改进必须是一个具体动作 + 一句原话示例（明确禁掉「多倾听」这种）。
>
> **5. 这一版找到并修掉的 5 个真问题**（都是「看起来正常、实际不工作」那一类）：
>
>   a. **native 的 `onStop` 没实现。** `tts.stop()` **不回调 onDone，只回调 onStop**。
>      漏了它，「对方正在说」这个状态永远关不掉——表现是点「打断」之后整局不动、
>      且不报任何错。而浏览器走的是 `speechSynthesis`，回调路径完全不同，所以
>      **本地怎么测都是好的**。已补上 `onStop`，并且 JS 侧的 `stopSpeak` 也自己把
>      状态落下来（不把状态托付给一个可能不来的回调）。
>   b. **「再来一局」对随机场景是静默失效的。** 它原来传的是 `s.sc.id`，
>      而生成的场景 id 是 `gen-…`，`startSession` 按 id 查不到 → 点了没反应也不报错。
>      改成直接用场景对象。
>   c. **「免提：开/关」的文案不会跟着变。** 那段字只在 `renderPractice` 里写了一次，
>      于是「连续空结果自动关掉免提」之后按钮上还写着「免提：开」——**界面在说谎**，
>      而用户判断麦克风开没开全靠这几个字。现在文案只有 `renderTalkBar` 一处来源。
>   d. **权限错误没写进设置**，导致每次新会话都白开一次麦克风再失败一次。
>   e. **人称写死了。** 角色提示词里是「你扮演一位中国女性」，界面上一排「她的温度」
>      「她的内心」。6 个老场景都是女性所以看不出来，但用户要的随机场景会造出
>      男同事、男上级——那时角色和界面都对不上。现在场景自带 `ta`（他/她），
>      界面文案统一从一个 `ta()` 取；老场景不带这个字段，默认「她」，行为不变。
>
> **6. 一个测试方法的改进（值得单独记）。** 免提这套逻辑**在浏览器里根本跑不起来**
> （没有原生识别桥，`startListen` 直接回 NO_ASR），所以沿用「在浏览器里点一遍」
> 的老办法等于没测。`tools/check_voice_talk.js` 改成**装一个 native 桩**
> （假的 listen/speak/stopSpeak），再手动把 `__onSpeech` / `__onSpeak` 回调打进去，
> 直接验状态机的迁移。
> 两个真正危险的事**直接用当时的真实状态量**，而不是事后猜事件顺序：
> 调 `listen()` 那一刻 `V.speaking` 必须为 false；调 `speak()` 那一刻麦克风必须是关的。
> （第一版我写的是「speak 到 stopSpeak 之间不许 listen」——这是错的，
> 「说完了」是靠 onSpeakDone 通知的、不是靠 stopSpeak，正常流程也会被判违规。）
>
> **7. APK 2.15（versionCode 17）。** 新增 `tools/check_voice_talk.js`。
> README 里「语音陪练」那一整节重写（它已经落后两个版本的交互了）。
>
> **v0.9.3（2.16）变更（已实现）：修「两个界面一样」+ 闸门改成挑应用**
>
> 用户报了两件：「练习和AI点进去发现两个界面变成一样了」；
> 「闸门的设置，那个包名太麻烦了，你直接读取应用，然后我告诉你选哪些就行」。
>
> **1. 「两个界面一样」的根因：设了一个没人读的状态。**
>
> 查下来是一行代码的事：`cardsView` 这个变量在 5 个地方被赋值，
> **没有任何一处读它**。`viewCards()` 无条件走 `renderCard()`，于是：
>
>     练习 → 卡片        → 刷卡片那一页
>     成长 → 偏差画像    → 也是刷卡片那一页     ← 应该是偏差画像
>     成长 → 我的预案    → 还是刷卡片那一页     ← 应该是我的预案
>
> 来由是上一轮界面归并（5 tab → 4 tab）：我把这两个视图从卡片页搬到成长，
> `VIEWS.growth` 里写了 `cardsView = 'bias'; viewCards();`，
> **以为 viewCards 会看这个变量——但它从来不看。**
>
> 这类 bug 的形状值得记住，因为它和这个项目上次栽的坑是同一个：
> **写了一个状态，但没有任何一处读它。** 上次是内容白名单（`CONTENT.stages`
> 一直是 null 而接口返回得好好的），这次是子页开关。
> 两次都是「不报错、不抛异常，功能静默退化」，两次都是靠人事后发现。
>
> 所以修完之后加的不是「检查一遍三个渲染函数」，而是一条**结构性断言**
> （`tools/check_tabs_unique.js`）：**十个页面（4 个 tab × 二级子页）的正文两两不同。**
> 任何一次「某个子页悄悄退化成另一个子页」都会立刻失败，不依赖人去逐个看。
> 比对时必须把二级切换条排除掉——「阶段|偏差画像|我的预案」这三个标签在
> bias 和 plans 下**文字完全相同**（只有高亮不同），带着它比会恰好漏掉这个 bug。
>
> 顺带：那条断言还扩到了 rich() 的三个消费方（卡片诊断、微课、复盘），
> 所以 rich() 本身哪天坏了也会被抓到。
>
> **2. 闸门改成从已安装应用里挑。** 原来只有一个输入框要手打
> `com.tencent.tmgp.sgame` 这种包名。手打的失败方式很难受也很隐蔽：
> 打错一个字母，闸门永远不触发，而界面上没有任何异常。
>
> 原生新增 `listApps()`：查 `MAIN`/`LAUNCHER` 的 activity，返回 `{label, pkg}`，
> 按应用名排序、按包名去重、排除自己。网页侧是一个面板：搜索（应用名和包名都能搜）、
> 点一下选中再点一下取消、选中的打绿勾、顶上说清已选几个。
>
> 几个刻意的取舍：
>   - **没有申请 `QUERY_ALL_PACKAGES`。** Android 11+ 默认看不到别的应用，
>     常规做法是要这个广权限，但这里只在 manifest 里声明要查的那一个 intent
>     （`<queries>`）就够了。这个 App 的底线是不做流氓软件，能不多要一个权限就不多要。
>     构建后核对过：**权限仍然是 7 个**，包内 manifest 里 `QUERY_ALL_PACKAGES` 不存在。
>   - **手填包名的入口保留**，收进折叠。双开的应用、没有启动图标的入口不在清单里，
>     全砍掉等于把那条路堵死。
>   - **点一下就立刻存**，不是等「完成」才提交。挑到一半接个电话退出去，
>     等提交的写法会把已经挑的全丢掉。
>   - **只重画被点的那一行**，不重画整个列表——列表可能有一两百项，
>     重画会把滚动位置和搜索词都清掉。
>   - 读不到应用 / 原生抛错，都要写出原因并指出手填那条路还在，不留一个空列表。
>
> **3. 顺带修掉两处「** 直接显示给用户看」。** 我在闸门那段写了一句
> `列表里只有**有启动图标的应用**`，界面上真的显示了两个星号。顺着查发现
> 不是只有这一处：`httpProblem()` 里那句「检查接口地址是不是**完整端点**」
> 也从 2.13 起就一直在设置页上露着，还有提醒那句「只在**没达标**的时候响」。
>
> 根因是这个 App 有**两条文本通道，而规则相反**：
> `rich()` 走 innerHTML（`**` 变粗体，内容文件里用了 100 多处）／
> `textContent` 原样显示（`**` 就是两个星号）。写文案时挑错通道不报错、只是难看，
> 所以一直没人发现。修法：进 textContent 的一律不带 markdown 标记。
> 断言：扫**真实渲染出来的文本节点**，出现 `**` 就失败——它不关心走的哪条通道。
> （这条断言我第一版写错了：扫了 `document.body.textContent`，而离线单文件版
> 把所有 JS 和内容 JSON 都内联在 `<script>` 里，源码注释里的 `**` 全被误报。
> 改成只看非 script/style 元素的直接文本节点才对。）
>
> **4. APK 2.16（versionCode 18）。** 新增 `tools/check_tabs_unique.js`。
>
> **v0.9.4（2.17）变更（已实现）：让对话真的像对话 + 情景直接选好**
>
> 用户问：「你检测一下那个 ai 对话的功能，能不能像豆包一样那样交流。
> 然后情景是你直接选好的。」
>
> **结论先说：对话功能一直是通的，不像豆包的原因是一个默认值。**
>
> 用真实请求量了每轮耗时（`tools/probe_chat.py`）：
>
>     deepseek-v4-pro    30.65s/轮    推理 5006 字   ← App 原来的默认值
>     deepseek-flash      4.63s/轮    推理 1015 字
>     deepseek-chat       1.59s/轮    推理    0 字   ← 像豆包的是这个
>
> 多轮下来 JSON 字段齐全、内容也像真人（「没多久，刚到。停车是挺难的，这附近。」），
> 所以引擎没问题。**但一轮等三十秒那就不是对话，是写信。**
> 豆包的体感靠的就是「说完一秒内有回应」，这一项就把差别解释完了。
>
> 顺带纠正一个我之前的判断：上一轮用户报 404 时，我因为「本地脚本用
> `deepseek-v4-pro` 成功生成过 336 条变体」而断定这个模型名没问题。
> 名字确实是对的，但**它不适合对话这件事我没看出来**——生成是批量的、
> 可以等，对话不行。模型名有效 ≠ 适合这个用途。
>
> **1. 换默认模型，并且迁移已经装好的旧设置。**
> 这是本次最容易漏的一步：用户手机上 localStorage 里已经存着 `deepseek-v4-pro`，
> **只改代码里的默认值对他完全没有效果**——默认值只在「没存过」的时候生效，
> 而他早就存过了。「改了默认值但没迁移」= 什么都没改。
> 所以：改默认值 + 一次性迁移（只迁移正好等于老默认值的那一种，
> 用户自己手填过的其它名字不动）+ 设置页写明「替你换过、为什么换」。
>
> **2. 用实测延迟自检，而不是写死模型名。**
> 一轮超过 8 秒就主动提示。判断依据是**实测耗时**而不是模型名——
> 模型可用性会变（今天三个，下个月未必），而「这一轮等了几秒」永远是真的。
> 如果当前已经在用最快的那个，提示改说网络/地址，不劝人换成一个他已经在用的模型
> （第一版就是那么写的，被自己的测试抓出来了）。
>
> **3. 设置页的模型选择带实测速度。** 点名字就切换，下面写着「实测约 N 秒一轮」。
> 用户唯一需要知道的就是这个数字。
>
> **4. 情景直接选好。** 用户要「情景是你直接选好的」，所以进 AI 页顶部就已经
> 摆好一个「今天就练这个」，一个按钮开始。选法是**本地**的：练得最少的优先、
> 同分时难度低的优先——故意不用模型现造，那样每次打开 AI 页都要等一次请求、花一次钱，
> 而大多数人打开只是先看一眼。「换一个情景」也是本地换。
> 为此给每个现成情景记练过几次（只记现成的六个；现造的 id 每次不同，记了是垃圾数据），
> 少了这个轮换，推荐会一直是同一个。
>
> **5. 又一次「界面露着 ** 」**：截图里看到「接口地址要填**完整端点**」。
> 上一轮我加的那条断言没抓到它，因为断言只打开了「每日计划」面板、
> **没打开主设置页**——断言是对的，但覆盖面缺了一块，而缺的那块正好是出问题的地方。
> 这种「断言写对了但没盖到」比断言写错更危险，因为它给人已经测过的错觉。
> 现在扩到全部五个面板（主设置页/每日计划/挑应用/卡住了/闸门）。
>
> **6. 一个流程教训。** 上一个脚本因为一个锚点没对上就整体退出了，
> 而「练完一局记一笔」那段代码排在失败点**之后**，于是根本没写进去——
> 是测试里那条 `state.scenes` 断言把它抓出来的。
> 每处替换都断命中次数让脚本立刻停下（这是对的），但**脚本中途失败时，
> 必须确认它到底写进去了哪几处**，否则会留下一个半成品，而它看起来像成功。
>
> **7. 补上一个「文档说有、实际没有」的兜底。**
> 核对 README 时发现两处不符：写着「现在默认 4000」（代码是 3000），
> 以及「遇到 `finish_reason=length` 会自动翻倍重试」——
> **而 `finish_reason` 这个词在 voice.js 里出现 0 次**，那个兜底根本不存在。
> 这个坑项目里真踩过（1800 被推理吃掉 1762，正文只剩 83 字节残缺 JSON，
> 报的却是「返回的不是 JSON」，完全看不出是额度问题），所以选择把功能做出来
> 而不是把文档改弱：截断就翻倍重试一次（上限 12000），两次都截断就如实说是
> 额度不够、并指出多半是推理吃掉了。
>
> **8. APK 2.18（versionCode 20）。** 新增 `tools/check_doubao.js`、
> `tools/check_truncate.js`、
> `tools/probe_chat.py`、`tools/probe_prompts.py`、`tools/probe_flash.py`。
>
> **v0.9.5（2.19/2.20）变更（已实现）：音色（语音包）+ 识别精度**
>
> 用户问：「他是不是应该会有语音包那种的？发出类似真人的声音。
> 然后这个对话也能读取我的音频，然后识别成文字。」
>
> **第二个问题的答案：一直可以。** `SpeechRecognizer` 录你的声音转成文字当用户那一轮。
> 但顺着查出一个真问题（见 2）。**第一个问题查出三个问题**，其中一个是
> 「这功能等于不存在」（见 1）。
>
> **1. `voices()` 早就写好了，但网页一次都没调用过。**
> 所以用户从来没能挑过音色，一直用系统默认那个嗓子。
> 这正好是上一版刚踩过的同一类 bug：**写了能力，但没有任何入口能碰到它**
> （上次是 `cardsView` 只写不读）。
>
> 而且更值得记的是：`tools/test_bridge.js` 里**早就有**这条检查
> （「Bridge 里暴露了但网页从不调用」），只是被写成**软提示**，理由是
> 「死方法不会导致功能坏掉，硬失败会让人习惯性忽略输出」。
> 这个理由现在被事实否掉了：死方法的确不会让功能坏掉，**它会让功能不存在**——
> 用户想要的语音包就是被它吃掉的。所以改成硬失败，并留一份**写明原因**的白名单
> （`httpGet` / `deepBlockStatus` / `openAppSettings`，每条都写了为什么保留）。
> 改完立刻抓出我自己新加却没人调用的两个方法（`currentVoice` / `openTtsSettings`）。
>
> **2. 识别被强制离线。** `EXTRA_PREFER_OFFLINE = true` 写死的，
> 注释理由是「能离线就别联网，也避免被墙的云引擎拖住」。
> 这条理由在这个 App 里不成立：**对话本身就必须联网**，所以强制离线换不来任何
> 离线能力，只是白白牺牲准确率（离线中文模型小得多，长句更易错）。
> 改成用户可选、默认「优先在线」，并如实说明这只是偏好、引擎有权不听。
>
> 还修了一处关联的路：等麦克风权限期间要**记住当初请求的是哪种模式**，
> 否则用户选了「优先离线」、点了允许，重试时却按在线跑。
>
> **3. 音色列表（语音包）。** Android 上「语音包」就是合成引擎的音色数据包。
> 所以不做自带音色（那等于往安装包里塞几十 MB，还不如系统自带的），
> 而是：列出本机所有中文音色 + 试听 + 去系统里下载音色 + 打开系统语音设置。
> 音色名（`zh-cn-x-ccc-local`）对用户没意义，翻成
> 「中文（中国大陆）· 可离线 · 音质高 · ccc」这类判断依据。
>
> **从截图里发现的一个真缺陷**：两行都显示「可离线 · 音质高」，只有底下的小字不同——
> 而这是个**用来「挑一个」的列表**，两项长得一样等于没得挑。所以把命名里那段
> speaker 编号（ccc / fff，引擎里确实是不同的嗓子）抽出来当短标签。
> 抽不出来就不加，不硬凑。
>
> 另外加了一个「当前实际生效的音色」显示：音色没下载或被引擎忽略时，
> 声音会静默退回默认而勾还打着——把真实状态显示出来，这种不一致才看得见。
>
> **4. 一个诚实的天花板，写进 README 而不是含糊过去。**
> 手机自带的合成引擎出来的**都不等于真人**。「像真人」只能走云端神经语音合成，
> 需要另一个服务的另一把密钥，而且 **DeepSeek 没有语音合成接口**——
> 所以只填 DeepSeek 密钥拿不到真人音。这一版给的是「把系统里已有的嗓子都用起来」，
> 不是「和豆包一样的声音」。想要云端音色，最省事的路是接一个 OpenAI 兼容的
> `/audio/speech`；这一版**没有**接。
>
> **5. APK 2.20（versionCode 22）。** 新增 `tools/check_voice_pack.js`（27 项）。
> 权限仍然是 7 个——这些能力都在系统里，没有新增任何权限。
>
> **v0.9.6（2.21）变更（已实现）：修好两个死掉的 AI 模块 + 实测成本**
>
> 用户问「会不会 token 消耗太多了，分析一下」，然后「要修复的地方你自己修复，
> 然后告诉我一个场景完成之后大概消耗多少钱」。
>
> **1. 查成本时发现：两个 AI 模块在手机上一直是死的。**
> 「表达体检」和「真实复盘」调的是 `/api/ai/checkup` 和 `/api/ai/replay/*`——
> **只存在于电脑上 server.py 里的接口**。APK 里没有服务端，离线适配层对这三个地址
> 一律回 503。实测点「体检」和「出题」都只拿到一句「这是离线单文件版，不含 AI 功能」。
>
> 也就是说 AI 页里三个模块平起平坐地摆着、README 也把它们列为 AI 模块，
> 而实际只有「场景对话」能用（它走原生桥，不经过 /api/*）。离线版顶上那条横幅
> 写着「AI 那两个需要电脑上的 server.py」，说明当初是有意为之——但它和现在的
> App 已经矛盾了：**手机端才是主产物**。
>
> 修法：三份提示词和三次调用都搬到前端（`public/app.js` 的 `aiCheckup` /
> `aiReplayQuestions` / `aiReplayReveal`），统一走 `llmCall`——手机走原生桥、
> 电脑走 `/api/proxy`，两边一致，且只需要用户那一把密钥。
> `server.py` 那三个端点删掉，避免两处提示词各自漂移。
> 顺带删掉随调用者一起失效的 `post()` 助手（又一个「死代码」实例）。
>
> *漏改一处、被测试抓住*：搬家时 `post()` 返回的是 `{ok, result}`，
> 而 `llmCall` 直接返回解析好的对象。体检那处我改对了，
> 复盘那五处 `r.result.xxx` 忘了改 → 一提交就报
> 「Cannot read properties of undefined (reading 'questions')」。
> 典型症状是「函数名换了、参数也对，只有返回值多套了一层」。
>
> **2. 顺手发现一个真 bug：`**` 会露给用户看，而且是选择性的。**
> 跑测试时随机翻到一张卡，诊断里印着 `**明确提问**`。查下来是一整类：
>
>     1098  esc(picked.why)       ← 你选错那条的解释：星号露出来
>     1108  rich(correctOpt.why)  ← 正确那条的解释：星号变粗体
>     1556  rich(r.quote) / 1557 esc(r.face_value) ← 同一张卡上下两行不一致
>
> **同一张卡里一个对一个错**，说明不是有意规则而是漏改；而出问题的恰好是
> 「你选错的那条为什么错」——用户最需要看的那段。
> `rich()` 就是 `esc()` 加一次安全的 `**`→`<b>`（先转义再替换，不可能引入 XSS），
> 所以统一改成 rich()：**一条规则（内容文本一律走 rich）比逐个字段判断可靠**，
> 而逐个判断正是当初漏掉的原因。共 20 处。
>
> **3. 一条写脆了的断言，顺手改成测设计不变式。**
> `check_tabs_unique` 里「刷卡片页是题目」原来数正文里 A/B/C/D 的个数，
> 但**卡片是随机抽的**，而带判局的卡片会故意先把四个选项藏起来
> （先判断「这是什么局」再选动作，这是 App 的核心设计）。
> 所以抽到判局卡时必然失败——这条断言时好时坏（实测 4 次里挂 2 次）。
> 改成直接问 DOM，并把设计不变式也断言上：**判局在时选项必须是藏着的**。
> 另外给「`**` 不许露出来」加了一条**确定性**的守卫：指定一张错项诊断里带 `**`
> 的卡，故意答错，断言渲染成粗体且无星号——靠随机抽卡碰上不可靠，
> 而这条 bug 原来就是靠随机抽卡才偶然发现的。
>
> **4. 实测成本（这是用户真正要的答案）。**
> 量法：把 Node 侧的真发请求函数用 `page.exposeFunction` 暴露给页面，
> 让 App 自己拼的请求真发出去，读服务端返回的 `usage`。
> 所以量的是 **App 真实发出的请求**，不是别处复刻的提示词。
>
> 一个场景（建场景 + 8 轮对话 + 复盘）= 10 次调用，5946 输入 + 784 输出，
> **约 0.54 分钱（空闲时段）／1.09 分（高峰）**；把体检和真实复盘也用上 ≈ 1.15 分。
> 一天 2 局 ≈ 0.33 元/月。
>
> 三条从数字里看出来的事：
>   - **钱主要在输出上**（4 元/百万，是未命中输入的 4 倍、命中输入的 200 倍）。
>     输入有 52% 命中缓存（对话那几轮 68%），因为固定系统提示词坐在每次请求最前面，
>     正好吃到前缀缓存——这个结构本来就对，没什么可优化的。
>   - **一局的固定成本是两个收尾调用**：建场景 + 复盘占输出的一半以上，
>     而 8 轮对话本身才 274 个输出 token。所以**少开新局、把一局聊透最省**。
>   - 体检和复盘的缓存命中是 0%，且输出天然长（出题 396 / 揭晓 466 token），
>     这是它们贵一档的原因，不是 bug。
>
> **5. APK 2.21（versionCode 23）。** 新增 `tools/check_ai_modules.js`、
> `tools/measure_cost.js`。权限仍然是 7 个。
>
> **v0.9.7（2.22）变更（已实现）：修用户报的两个 bug + 顺手查出的两处**
>
> 用户报：「偶尔 ai 界面还是会显示练习界面的内容」
> 和「AI 界面我点了开始说他没有用，然后我打字说出了，显示模型返回内容为空」。
>
> **1. 串台：根因是子页状态跨 tab 保留。**
> 四个子页状态（`practiceSubview` / `aiSubview` / `growthSubview` / `cardsView`）
> 是模块级变量，切 tab **从不清**。而「成长」那一路的偏差画像和我的预案
> 是借 `cardsView` 实现的分发（`growth` 分支里写 `cardsView='bias'` 再调 `viewCards()`），
> 所以做过一次「成长→我的预案」之后 `cardsView` 就留在 `'plans'`，
> 此时点「练习」→ `viewCards()` 照着 `'plans'` 画 → **练习 tab 下渲染出预案页**。
> 这就是他看到的「AI 界面显示练习界面的内容」那个现象的同类。
>
> 修法是让子页状态属于「这一次浏览」：加一张 `TAB_HOME`（practice→cards、
> ai→voice、growth→stages），`go(tab)` 进入任何 tab 时先归位再画。
> 需要「回到我刚才那一页」的场景本来就不存在——四个 tab 的内容各自独立。
>
> **2. 「开始说」点了没反应：`micTap()` 在四种情况下静默 return。**
> 没有会话、这一局已结束、没有识别引擎、麦克风没权限——四条路都是一句
> `return`，**一个字都不说**。点了没反应又不报错比报错更糟：用户只能反复点，
> 也不知道该换什么办法。改成每种原因都讲出来，并且都指向打字那条退路
> （识别引擎缺失和权限缺失两句话会同时留在页面提示里，不只是弹一下就没了）。
>
> **3. 「模型返回内容为空」：报错既没原因也没方向，还把服务端原文丢了。**
> 真正的原因多半是**带推理的模型把 `max_tokens` 额度吃在推理上**，
> 正文一个 token 都没剩下（2.17 已经因此把默认模型换成 `deepseek-chat`，
> 但设置页里仍然可以选回带推理的）。现在这种情况会明确指出原因、
> 点名该换成哪个模型，并把服务端返回的头 200 字原样带上。
> 普通空返回也带原文——原来只留一句「再试一次」，排查等于零。
>
> *更省事的防线是让它别发生*：2.17 那次迁移只认一个字符串
> `model === 'deepseek-v4-pro'`，而用户手机上存的很可能不是这一个名字
> （`deepseek-reasoner` / `deepseek-r1` 是常见的同一类），那就等于没迁移。
> 改成一张表 + 小写包含匹配（`deepseek-r1-0528`、大小写变体都能盖上）。
> `deepseek-flash` 也带推理但 4.6 秒能用，是设置页里允许的选择，刻意不迁移——
> 迁移只处理「测下来没法用来对话」的那些。
> 断言覆盖五个该换的 + 两个不该动的，其中「不该动」那两条同样重要：
> 迁移写宽了会把用户自己选的模型改掉，那是另一种坏。
>
> *顺手修的*：`answerCard` 在视图已经被切走之后再被回调会崩
> （诊断抓到 `Cannot set properties of null (setting 'innerHTML')`），加了守卫。
>
> **4. 修的过程中自己撞出一个回归，被老测试抓住。**
> 第 1 条我是让 `go()` 无条件归位来实现的，而 `setAiSubview()` 内部调的正是 `go()`——
> 于是「表达体检」「真实复盘」两个按钮**点了就弹回场景对话**。
> 这是很典型的一类错：两个动作名字像、后果正相反
> （「从外面进这一路」vs「已经在这一路里换一页」），共用一个函数必然出这种事。
> 改成 `go()` = 归位后画、`showTab()` = 只画不归位，setter 一律用后者。
> `check_ai_modules` 和 `check_voice_talk` 立刻报错——它们是真的点 DOM。
>
> 教训写进了新测试：**断言要真的去点界面上的按钮，而不是调那个 setter**。
> 调 setter 时这个回归完全看不出来（setter 本身是对的，是它内部调错了函数）。
>
> **5. 新增 `tools/check_reported_bugs.js`**，直接断言用户能感觉到的结果：
>   - 点底部任意 tab 一定落在那一路的默认页（11 组「先去A子页再点B」的组合）
>   - 来回切 30 次，任何 tab 下都不出现别的 tab 的页面
>   - 三个 tab 的二级切换条**逐个点 DOM 按钮**，且连点两次不会弹回去
>   - `micTap` 五种不能开始听的情况都会说话，能听的时候真的开麦
>   - 空返回 / 只返回推理 / 被截断三条报错各自命中，且都带服务端原文
>   - 带推理的模型名开机会被换掉，而用户自己选的 `deepseek-flash` / `deepseek-chat` 不动
>
> 判「现在是哪一页」用 DOM 结构（id / class），不用文字关键词——
> 前三个诊断脚本都栽在关键词上（「今天」页里就有「第 0 关」「1 条微课」，全线误报）。
>
> **6. 顺手清了自己留下的坏工具。** `diag_view_writes.js` 漏传 `page.evaluate`
> 的第二个参数（`OWNER` 是 undefined，`Object.keys` 直接抛），另有两处把
> 页面里的 `window` 写到 Node 侧。修好后它现在能一路走完 24 个交错动作、
> 报「没有跨 tab 写入」——并且它自己也有个判错的地方：`viewCards` 是共用分发器，
> 不能一刀切成 practice，得看它当时按 `cardsView` 画了哪一页
> （切错了会漏报，而原来那个 bug 正是这种）。另外 5 个被它完全取代的
> 探索性脚本（diag_tabs/diag_tabs2/diag_tabs3/diag_tab_bleed/diag_tab_who）删掉，
> 免得以后有人拿它们得出不同结论。
>
> **7. 构建脚本的坑（差点打出一个缺类的包）。**
> `E:\android-build\build_apk.py` 是**旧的单人份拷贝**，javac 那步写死只编
> `MainActivity.java`；加了 GateService / ReminderReceiver / Prefs / BackupStore
> 之后它报「找不到符号」，而报错指向调用处，看不出是没编进来。
> 真正在用的是仓库里的 `android/build_apk.py`，它自动收集 src 下所有 .java。
> 这次误用了旧拷贝，白跑一轮。**以仓库里那份为准。**
>
> **8. APK 2.22（versionCode 24）。** 权限仍然是 7 个。
> 回归：node 单元 6 组（18/21/18/18/32/12）+ 离线 37 项 + 浏览器 13 组
> （含新增的 check_reported_bugs），全绿；
> `check_tabs_unique` 连跑 4 次、`check_reported_bugs` 连跑 3 次都稳定。
>
> **v0.9.8（2.23）变更（已实现）：搜资料库 → 改成一个游戏 + 两处内容纠正**
>
> 用户先让我搜「有没有什么其他好的资料库」，然后说
> 「这些你自己来决定，主要还是为了塑造一个更好的我……最好有一点趣味性，
> 如果有小游戏就更好了」。
>
> **0. 搜索结论（决定了后面做什么）。** 找到的最好的东西是
> **SwordsmanImp**（上海交大，CCL 2024）：200 条《武林外传》对话，
> 每条一句言外之意 + 4 个解释（1 语用解 + 1 字面解 + 2 干扰项），
> 还标了违反哪条格莱斯准则。**结构和这个 App 的卡片几乎一一对应**，
> 而它正好填最薄的那块。但两处泼冷水：仓库里**没写 license**，
> 而且 GPT-4 已经 94%（人类 93.1%）——拿来做训练偏简单。
>
> 其他值得记的：**MITI 4.2.1**（免费、带 1–5 分行为锚点的对话评分手册）、
> **会话接受度**四标记（Yeomans 2020）、**STEU/STEM**（STEM 是文献里
> 最接近本 App 卡片格式的东西：情境 + 4 个动作 + 专家加权正解，可用来校验正解）、
> **CPED 的 19 个对话行为** + ISO 24617-2（判局类目的权威命名）。
>
> **明确不用的**：天机 Tianji（语域是酒桌/求人办事，偏「会来事儿」）、
> 「情圣.skill」（自称蒸馏自中文情感付费课程语料——正是这个 App 拒绝做的东西）。
> **顺手查出的两处对自己内容的纠正**见下面第 4、5 条。
> 说明：只有 SwordsmanImp 是我打开仓库核过的，其余来自检索摘要。
>
> **1. 决定不搬语料库，改做一个游戏。** 理由：他缺的不是知识点，是
> **「分辨」这一下的手感**，而手感只能靠大量重复判断 + 立刻知道错在哪 + 看见自己
> 在变。搬别人的题给不了这个，语料库也不长这个样子。
>
> 所以做「**信号场**」：一局 20 条线索，每条只判一件事——
> **这是只对你，还是她对谁都这样**。判据只有「排他性」一个，
> 因为它可以观察：「她喜不喜欢我」无法观察、越想越乱。
>
> **2. 计分用信号检测论，而不是答对率——这是这个功能里唯一重要的设计决定。**
> 题池里「不是信号」的比「是信号」的多（14:12），按答对率记分的话
> **一律答「不是」就能得高分**，游戏就废了。所以给两个数：
> **分辨力 d′**（看不看得出区别）和**偏向 c**（更容易往哪边倒）。
> 两种毛病的改法正相反，混在一个答对率里看不见。
> 依据是 Brandner 2019/2022 用信号检测论重做那批研究的结论：
> 差异主要来自分辨力而不是普遍偏向——分辨力可练，所以更有用也更诚实。
>
> 两个实现上不可省的细节：**对数线性校正**（命中率或虚报率是 0 或 1 时
> z 值发散、d′ 变 NaN，而这恰是新手最可能的两种局面），
> **每局固定抽 10+10**（d′ 要求知道每类试次；按真实比例抽会变成猜多数类）。
> 断言盯着这个性质：一律答「是」或一律答「不是」，d′ 都是 **0**（不是高分），
> 偏向符号相反；对照组：全对 3.38、八成对 1.50。
>
> 题面 26 条分三类，**「信息不足」这一类最重要**（6 条）：它教的不是
> 「她不喜欢你」，而是「这条还不足以说明任何事」——把「无法判断」当成
> 「有信号」是最常见的虚报。这一类每条都要写「下一步该看什么」。
>
> **3. 写这个游戏的过程中自己撞出三个 bug，全是同一句病因：
> 「渲染函数会在每次重画时跑一遍」。**
>   - 反馈原来是「答完删掉按钮、插入解释」→ **切走再回来**会重新画出按钮，
>     同一条能答第二次，计数重复。改成一整轮都从状态渲染。
>   - 结果页一边渲染一边 push 成绩 → **打完一局、切走、再回来，同一局记两笔**，
>     来回切得越多历史越假。加了 `game.recorded` 标记。
>   - 反馈页那行「这条是\*\*只对你\*\*」插进了纯文本位置，而旁边的「判据」走 `rich()`
>     ——**同一张卡里一个对一个错**，和第 20 处 esc/rich 混用是同一个毛病。
>   三条都补了断言（第 5 条里的「重画不重复记账 / 不重复作答 / 不露 `**`」）。
>
> **4. 语境校准 9 → 19 条（57 个场合）。** 这是全 App 最薄的模块，而它正对
> 最初的问题。新加的这批补上了格莱斯四条准则里原来只碰到两条的缺口
> （补了「数量」和「质量」），以及一个反复出现的真实错误：
> **同一个句子，在对方的情绪已经在高处时说出来，性质就变了**（k15/k16/k19）。
> 每条都保证「同一句话在不同场合判定不同」——有断言守着，
> 因为只有一个判定的题，「校准」这个能力根本没被练到。
>
> *写断言时把断言写错了*：「只有 revise 才许有改法」报出 17 条违规，
> **全在已有的 9 条里**。界面上这个字段叫「改后可以说」，对「别说」来说
> 意思是「别说这句，说这句」，比只写「别说」有用。**断言和事实冲突时先假设断言错了。**
>
> **5. 两处内容纠正（比新增内容更值钱）。**
>   - **第三人称复盘有一条反着伤人的边界**：有研究报告在抑郁风险偏高的人身上，
>     这类「站远一点看自己」的写法会让情绪当场变差。引的 Grossmann 2021
>     是平均效应，不是对每个人都成立。所以把「状态很差的那几天先别写、
>     **漏几天没有代价**」写进了复盘页面和微课 `p33` 的 `warn`——
>     这是少数几种「功能本身可能有害」的情况，不靠用户自觉。
>   - **NVC 和 Gottman 的「86%」撑不住**：NVC 的系统综述筛出 14 项
>     研究、异质性高到做不了元分析，12 项里没有一项 RCT；Gottman 的 86% vs 33%
>     找不到同行的重复验证。结论写进了 PLAN 备查，内容里不要当事实用。
>
> **6. 顺手修掉两个「写死清单」**（这个坑的第 4 次和第 5 次）：
> `build_offline.py` 和 `server.py` 都硬列了六个数据文件名，
> 而 `data/` 现在有七个（加了 signal.json）。两处都改成自动发现 `data/*.json`。
>
> **7. 修了一份说谎的文档**：README 有一整节在讲「现实任务」，而那个功能
> **早就按用户要求删掉了**（代码注释里写着）。留着比没有更糟——下一个人会按它
> 去找代码然后找不到。那一节改成明确写「已经没有了」，并说明删掉之后关卡
> 换成了判局准确率。
>
> **8. APK 2.23（versionCode 25）。** 权限仍然是 7 个。
> 回归：node 单元 6 组（28/18/18/32/12/18）+ 离线 37 项 + 浏览器 14 组
> （新增 `check_game.js`），全绿；`check_game` 与 `check_tabs_unique`
> 各连跑 3 次稳定。
>
> **v0.9.9（2.24）变更（已实现）：整个项目自检**
>
> 用户要求「整个项目自检一下」。查出来的东西按重要性排，第一条是这一版
> 唯一真正要紧的发现。
>
> **1. 构建链上有一个洞：产物可能是旧的，而所有校验都会通过。**
> 原来的校验只有一环——`build_apk.py` 把 **APK 里的 `assets/index.html`**
> 与 **`认知训练-离线版.html`** 逐字节比对。它保证「包里装的是那份 HTML」，
> 但**不保证「那份 HTML 是由现在的源码生成的」**。而这一版真的发生了：
> `public/app.js` 和 `data/curriculum.json` 被改过（把「自我抽离」那条边界
> 换成带 Giovanetti 2019 / Kross & Ayduk 2009 的版本），但离线版最后一次
> 重建在改动之前——也就是说**当时交付的 2.23 里装的是旧文字**，而构建日志
> 每一项都正常、字节比对也通过（APK 确实等于那份旧的 HTML）。
> 这个洞的性质和「打 APK 时会用旧内容」是同一个，只是上移了一层。
>
> 修法：`build_offline.py` 把全部输入（index.html / style.css / voice.js /
> app.js / icon.svg / 所有 `data/*.json`，排除存档）算 sha256 写进
> `认知训练-离线版.src.txt`；`verify_offline.js` 重算比对，对不上就失败并
> 指明「重跑 build_offline.py」。链条闭合：源码 → HTML（校验）→ APK（校验）。
> 两个踩到的细节：指纹文件必须 `newline="\n"` 写（Windows 默认翻成 CRLF，
> 读回来每行多个 `\r`，路径全不存在，校验会永远红而原因与源码无关）；
> 第一版我把它写成了永远失败的检查，修好之后**故意改源码一个字节验证它能红**。
>
> **2. 新增 `tools/audit_project.js`：专查这个项目真的栽过的那几类。**
> 不是通用 lint，检查项都对应历史事故：只写不读的状态（栽过 4 次）、
> 定义了没人调用的函数（`voices()` 那次 = 功能根本不存在）、版本号在
> manifest/README/PLAN 三处是否一致、产物是否新鲜、以及用户的硬约束
> （包里无密钥、无系统级状态、权限数量、外部存储调用）。
> 只报告不修改，退出码非零即有问题。
>
> **3. 它一次抓出两个只写不读的状态。**
>   - `usedAssignments`：被删掉的「现实任务」的遗留，直接删。
>   - `boundarySeen`：界面上写着「建议**现在就看一遍**，不要等通关」，
>     而 App 其实**不知道你看没看**——那个标记只写不读。这次接上了：
>     它决定那句话是「建议看一遍」还是「你看过了，随时再看」。
>     这一条的教训比前一条有意思：**写一个状态却不读它，通常意味着
>     某个本来打算做的行为从来没做**。所以自检脚本查这一项是有道理的。
>
> *（自检脚本第一版自己有三个 bug：只扫 app.js 导致 voice.js 里读的
> `state.scenes` 被误报成死状态；用 `pop()` 取 PLAN「最新」一条结果取到最早的；
> 写死了一个仓库外的相对路径。一次误报就够让人不再相信这个检查，所以都修了。）*
>
> **4. 给「自我抽离」那条安全边界加了两个守卫。** 上一版那条边界
> （第三人称复盘在抑郁风险偏高的人身上可能让情绪变差）是内容里
> **用错了会反着伤人**的东西，而它恰好最容易在改稿时被当成「泼冷水」删掉。
> 现在 `test_content.js` 断言 `p33.warn` 里两边证据都在且点了名
> （Giovanetti / Kross & Ayduk），`check_today_ui.js` 断言复盘页界面上
> 也写着这句（含「漏几天没有代价」），并顺手断言它没有露出 `**`。
>
> **5. 硬约束复核（都通过）。** 包里没有 `sk-` 形式的密钥；产物里没有
> `DevicePolicyManager` / `setPackagesSuspended` / `setApplicationEnabledSetting`
> / `pm disable-user`；权限仍是 7 个；6 个 java 源文件与 manifest 在仓库和
> 构建树两边逐字节一致。
>
> **6. 全量回归。** node 单元 6 组（31/18/18/32/12/18）+ 离线 39 项
> + 浏览器 14 组；四个含随机性的套件（check_game / check_tabs_unique /
> check_reported_bugs / check_read_flow）各连跑 3 次全绿；
> 63 个 py 脚本 py_compile 全过；32 个 js 工具语法全过。
>
> **7. APK 2.24（versionCode 26）。** 因为 2.23 的字节和现在的源码不一致，
> 所以**必须换号**——同版本名两份不同字节，正是「我装的到底是哪一个」的来源。
>
> **8. 交付做成了一条命令，并且自动清桌面。**
> 用户的要求：「每一次迭代，桌面上都会出现一堆的文件，你要把前的全删掉，
> 只留最新一次和上一次的，再之前的就全部删掉。」
> 当时桌面上躺着 2.13–2.24 共 12 个包（另外还有他的论文、图表，
> 那些不动——只匹配 `认知训练-数字.数字.apk`）。
>
> 写成 `tools/deliver.py` 而不是「记得手动删」：这个项目的教训就是
> 「凡是需要记得做某件事的设计，迟早会忘」（内容键白名单、javac 源文件清单、
> 两个数据文件清单，都栽过）。交付每次迭代都要做，放脑子里必然有一天忘了。
>
> 它一条命令做四件事：从 manifest 读版本号（不手抄）→ 拷成
> `认知训练-<版本>.apk` 和固定名 `认知训练.apk` → 同步仓库 `android/` 的
> apk 与 manifest → 删掉更老的版本只留最新两个。三道保险：只匹配那个文件名形状、
> 删前确认是真 APK（zip 魔数 + 体积）、`--dry` 可先看。
> 另外交付前会再比一次「APK 内 HTML == 离线版 HTML」——打错包的事故这一版
> 刚发生过，最后一道门值得自己再看一眼。
> 删除路径是**真的测过**的：种两个假的旧版本进去（2.10 / 2.11），
> 确认被扫掉、2.23 和 2.24 留下，而不是"看起来应该能删"。
>
> **v0.10（2.25）变更（已实现）：背景声 + 设置页收成一个**
>
> 用户的要求：「我神经比较紧张，容易焦虑，所以在这个软件里面加入音乐或者脑波。
> 然后每次进入 APP 之前需要询问我是否需要开，然后最好多来几首，
> 让我在设置里可以更改，然后可以把部分的功能全部归在一个设置里面，
> 然后就跟大部分软件一样。」
>
> **1. 先查证据，因为这一块流行说法远超证据。** 结论决定了整个功能的写法：
>   - **有证据的是「安静听一会儿声音」这件事本身。** 音乐干预元分析（累计九千
>     多名被试）报告的焦虑下降「与部分药物干预相当」。
>   - **双耳节拍那部分证据不明。** 被引最多的 Garcia-Argibay 等 2019 元分析
>     （22 项，g≈0.45–0.58）作者自称「有希望但不算确证」；2023 年一篇脑电牵引
>     综述里 14 项只有 5 项支持、8 项相反；2023 年一项大型居家试验里它让学习
>     表现略变差；还有综述指出双耳节拍的皮层牵引比单耳/等时节拍更弱。
>   - **最要命的是混淆**：绝大多数实验把节拍混在音乐里放。2022 年双盲试验发现
>     ASMR 与「双耳节拍+音乐」效果一样；2025 年 n≈308 的随机对照发现**单耳**
>     节拍同样有效。都不支持「双耳」有什么特别。
>   → 所以界面上不能写「缓解焦虑」了事。设置页里三段说明 + 一段 warnbox
>   把上面这些写清楚了，`check_audio.js` 钉着它们不许消失
>   （那正是最容易被当成「泼冷水」删掉的部分）。
>
> **2. 8 条音轨，全部 WebAudio 现场合成，零音频文件。** 体积是硬约束：
> APK 现在 0.45 MB，一首 3 分钟 mp3 就 3–5 MB，几首就是十倍以上，还要处理版权。
> 而这几类声音本来就能合成：噪声加滤波器就是雨和风，左右耳差几赫兹就是双耳节拍。
> 附带好处是它真的无限长，没有循环接缝。
> 做成：雨 / 海浪 / 风 / 低频铺底 / 暖垫 / 双耳 4·6·10 Hz。
> 三条双耳音轨都标了「要戴耳机才成立」——不戴耳机它物理上就不成立
> （两个频率进不了两只耳朵，外放会变成一个颤音），有断言钉着这个标记。
>
> **3. 两条不是"按钮能点"的断言。** 把 `AudioContext` 的原型方法包起来，
> 记录真正被创建的节点参数：
>   - **双耳音轨的左右耳差必须正好等于 4/6/10 Hz**，载波 200 Hz，
>     而且必须走 ChannelMerger（不是普通立体声）；
>   - **三种噪声的相邻采样平均差必须满足 白 > 粉 > 棕**——这条能抓住
>     「三种颜色算成了同一种白噪」这种错，光看代码是看不出来的。
>
> **4. 两个交互决定。** 她说话时背景声自动压到 0.35（语音被音乐盖住比反过来糟）；
> 「以后别再问」必须真的生效（否则启动询问就成了每次都要点掉的弹窗，
> 结局是被讨厌）；已经在别的浮层上时不叠加。
>
> **5. 设置页收成六个分区：声音 / AI 陪练 / 计划与提醒 / 闸门 / 数据 / 关于。**
> 一个刻意的取舍：**后三块的内容没有搬进来**，而是从设置里点开它们原来的面板
> ——那些面板已经能用、也有测试盯着，为了「看起来像设置页」拆开重写是把风险
> 换成整洁。设置页的职责是**唯一入口**，不是复述。
>
> *代价说清楚*：这次重构把两处旧测试弄红了（`check_doubao` / `check_llm_errors`
> 一打开设置就找模型那一栏，而默认落的是「声音」）。这不是 bug，是它们假定
> 「设置一打开就全都在」——改法是显式 `openSettings('ai')`。
> 另外「关于」里写了一个版本号，这是第 4 处会漂的地方，所以
> `audit_project.js` 现在也核对它和 manifest 一致。
>
> **6. 写这个功能时又踩了两次 `**` 泄露**（同一类错误的第 N 次）：
> 证据文本里有 `**强调**`，而我先把它丢进了纯 `esc()` 的通道、
> 又在一段纯 HTML 里直接写了 `**现场合成**`。前者改成 `rich()`，后者改成 `<b>`。
> 教训还是那一条：**渲染函数有两条通道，混了就静默地把星号印给用户看**。
>
> **7. 顺手记下的自检结果。** `check_settings_ui` 在这次重构后仍然通过，
> 我特意去确认了它不是空过——它打开的是 `openDailySettings`（每日计划面板），
> 与被重构的 `openSettings` 是两块，所以通过是真的。
>
> **8. APK 2.25（versionCode 27）。** 权限仍是 7 个。
> 回归：node 6 组（31/18/18/32/12/18）+ 离线 39 项 + 浏览器 15 组
> （新增 `check_audio.js`，40 项；`check_audio` 连跑 3 次稳定）。
> 交付走的是新写的 `tools/deliver.py`，它自动把 2.23 扫掉了，
> 桌面只剩 2.25 / 2.24 / 固定名三个。
>
> **v0.10.1（2.26）变更（已实现）：自检 + 补内容 + 顺手修的三个真问题**
>
> 用户说：「那就自检一下。而且 app 做大了，其实也无所谓。让这个 App 更好用，
> 更舒服，加入一些内容。」
>
> **先说清一件事**：体积从来不是约束。音频做成零资源字节不是为了省空间，
> 是因为**手上没有有授权的音乐**可以打进包——能下载到的都是有版权的，那个不能塞。
> 所以「加入内容」往两个真方向做：题更多、音轨更多。
>
> **1. 最实的发现：信号场的题库会耗尽，游戏退化成记忆测试。**
> 一局抽 20 条（10 真信号 + 10 噪声），而池子只有 26 条 —— **玩两局就把题看完了**，
> 之后靠的是记得答案，不是分辨。它唯一想练的东西（手感）反而练不到。
> 这是内容量不足导致的**训练失效**，不是"内容少了点"。
> 扩到 45 条（真信号 12→20，噪声 14→25），按 95% 覆盖算：
> **看完全部题从第 2 局推迟到第 5 局**。
> （顺便修了这个脚本自己的一个错：第一版算的是 `min(两类)/per`，那是"抽完较小的
> 那一类要几局"，和"会不会看到重复"是两回事，还把旧值硬编码成了 1.2 局。）
>
> **2. 音轨 8 → 12**：加溪流 / 白噪（盖环境音）/ 深夜 / 远处的人声。
> 合成的东西不占体积，所以这一项是白给的。
>
> **3. 修了一个"界面在说谎"的静默 no-op。** 我给「深夜」写了 `layer_noise`
> 参数（描述里说"一层棕噪"），而引擎只对 `kind='noise'` 读 `layer`——
> 那个参数被**静默忽略**，实际什么都没放。数据和代码不一致时，界面上那句话
> 就变成假话，而且没有任何报错。给引擎补上对铺底音轨的噪声层支持，
> 并加了一条断言：**凡是声明了噪声层的音轨，必须真的建出滤波器**。
>
> **4. 更舒服一处：把一千多字的证据说明折起来（`<details>`）。**
> 摊开时它把「音轨」「音量」「每次问不问」全挤到下面，而那几项才是天天要碰的。
> 关键约束是：**折起来不等于藏起来**——`<details>` 里的文字仍进 textContent，
> 所以「证据必须在」的断言依然有效，这是刻意的：那段说明是安全相关的，
> 不能靠折叠把它变成不存在。
>
> **5. 写这个功能时踩了两个坑，都是同一类。**
>   - 我把一段**块注释写在了模板字符串里面**。模板字符串里的注释不是注释，
>     会被原样渲染成页面上的字——**我的实现说明曾经显示给用户看**。
>   - 修的时候更好笑：我在解释"反引号会被截断模板字符串"的那句话里，
>     又用了反引号，于是同一个 bug 原样复活。反引号在模板字符串里会把字符串
>     当场截断，标签从文字变成表达式；**语法检查照样通过（那是合法的 JS）**，
>     只在运行时抛 `details is not defined`。语法过了不等于跑得起来。
>   现在六个设置分区都有断言：渲染出来的 innerHTML 里不许有 `**`，
>   textContent 里不许有 `/*`。
>
> **6. 顺手修的两条自己的断言错**（不是产品错）：
> 「最后一条音轨滚得到」原来写成「滚到底就能看见」——但音轨下面还有内容，
> 滚到最底时最后一条反而在视口上面；「声明了噪声层 ≥3 条」是我编的魔数，
> 实际只有 2 条。断言和事实冲突时先假设断言错了。
>
> **7. APK 2.26（versionCode 28）。** 权限仍是 7 个。
> 回归：node 6 组 + 离线 39 项 + 浏览器 15 组全绿，`check_audio` 连跑 3 次稳定，
> `audit_project.js` 0 错。新增 `tools/expand_signal.py`、`check_audio.js` 的
> 折叠与泄漏断言，以及一个 `tools/shot_settings_full.js`
> ——它解决的是"浮层内容区 88vh 带滚动，整页截图看不到里面"这个审查盲区。
>
> **v0.11.3（2.35）变更（已实现）：复盘传聊天截图；顺手挖出「原生桥发的是字符串」这个静默 bug**
>
> 用户的原话：「在聊天。复盘那一栏加上一个上传聊天截图的功能。你要看看能不能识别图片。
> 一般来说只需要图片识别成文字就行，不需要真正的……做好之后自己试一下。所有的复盘都需要。
> 帮我分析情况，详细一点，还有就是我该怎么回，如果对面下一步说什么，我在该怎么回之类的。」
>
> 三件事：**传截图 → 抄成文字**、**复盘输出要详细**、**要给"我该怎么回"和分支应对**。
>
> **1. 复盘多了一个 📷 上传聊天截图。** 原生 `ACTION_GET_CONTENT`（`image/*`）选图 →
> `BitmapFactory` 两次采样降到最长边 1400px → 白底压平 → JPEG 82 → base64 →
> 桥回传 JS。全程只用框架 API，`build_apk.py` 那条「包里一个 `lib/` 都不能有」的断言照旧成立。
> JS 侧：宽高比 > 2.4 就等分切段（聊天截图常是 1:3、1:4 的竖长图，整张缩到模型能吃的大小
> 之后字会糊），一次请求最多 3 段，每段再缩一次。模型换成视觉版
> （`deepseek-v4-flash-vision-exp`，设置里 `visionModel` 可改）。**刻意只做"图 → 文字"**：
> 用户说了"只需要图片识别成文字"，而且这样抄出来的字他**看得见**、能当场改——
> 识别完**不自动出题**，填进输入框就停。切片之间**不留重叠**是个明写的取舍（有重叠更不易
> 漏字，但拼回来会重复一整句，而重复比漏字更难发现）。
> 隐私提醒跟着改了，写明"截图本身也会发到接口"；原生**刻意不做**
> `takePersistableUriPermission`——截图是用完就扔的东西。
>
> **2. 复盘输出加了「现在该怎么回」和「如果她下一步这么说」。** 用户要的就是这两样，
> 所以写进提示词成了**必给字段**：`how_to_reply{situation, say, why, avoid}` +
> `branches[3-4 条]{if_they_say, what_it_means, you_say, watch_out}`，并要求覆盖
> "她冷下去不回"这一种、以及"这段记录已经结束就按下次类似情形给"。同时守住底线：
> 给的是**一个**可行的说法、不是标准答案，界面上就写着"照着念会不像你"。
>
> **3. 挖出一个真 bug：原生桥回传的对象，网页收到的是 `typeof 'string'`。**
> 这是这一轮最有价值的东西，值得单独写。原生一直是
> `js("window.__onX&&window.__onX(" + q(json) + ")")`，而 `q()` 是 `JSONObject.quote`——
> 它把 JSON **包成 JS 字符串字面量**，所以网页那边拿到的是字符串。
> `__onHttp` 一直是 `JSON.parse` 之后才用（所以 AI 功能正常），但
> **`__onShot` 和 `onMusicState` 直接当对象用**，于是两处**静默失效**：
> 不抛异常、不打日志，每次走"数据不成立"的兜底分支。
> · 现场：真机上一直提示「没识别成功：没选到图片」，而原生日志明明是
>   `emit ok=true why=null len=43195`——图选到了、解码了、base64 也送到了。
>   我一度在怀疑权限、怀疑选择器、怀疑桥，都没结果；最后是在 `__onShot` 里临时把
>   `typeof payload` 写进界面上的"背景"输入框，才看见 `typeof=string`。
> · 顺手发现 `onMusicState` 开头是 `typeof o !== 'object'` 就 `return`——
>   **「选好了」的提示和播放状态更新从来没生效过**，同一个坑的第二处。
> · 修法：`voice.js` 里加 `bridgeObj()` 统一归一化，两个处理函数都过一道。
> **以后新增任何 `window.__onXxx`，第一步就是 `bridgeObj(payload)`。**
>
> **4. 为什么 24 条断言全绿、真机却全挂——测试测的是"应该怎么被调用"。**
> `check_replay_shot.js` 原来的 24 条全走 `#shotInput`（电脑那条路），等于**在测试里把
> payload 当对象**交给处理函数，而真机上原生发的是字符串。两边各自都对，合起来什么都没验证到。
> 补了第六节：按**真实形状**调桥（`JSON.stringify` 之后再喂进去），并覆盖
> 坏 JSON 不抛异常、`onMusicState` 同类问题。`test_bridge.js` 加了两条静态断言
> （用 `q()` 发 JSON 的回调必须解字符串；不许出现 `typeof x !== 'object'` 这种"只认对象"的守卫）。
> 写这两条时又踩了两个小坑：正则写成"紧邻"的 `__onX\(\s*q\(`，中间隔着字符串拼接，
> **一条都没扫到，检查空转还打印 PASS**；以及 `bridgeObj` 的说明注释里正好引用着
> `typeof o !== 'object'` 这个坏模式，**自己的说明被当成违规代码**——扫描前剥注释这个坑
> 在 Java 那边已经踩过三次，这是第四次，所以 JS 侧也剥（但只剥整行注释和块注释，
> 因为 `'https://api.deepseek.com'` 里的 `//` 会把整行截断）。两条断言都做了**负向验证**：
> 把旧写法注回去，确认它们真的会红。
>
> **5. 构建链的新鲜度校验前移到了构建时。** 我在 `voice.js` 里加了一行调试输出，
> 然后**只跑了 `build_apk.py`**（顺手，因为它会自己同步 assets），装进模拟器开始查问题——
> 查半天没进展，因为**装在手机上的 JS 是上一版的**，那行输出根本不在包里。
> `verify_offline.js` 能发现，但那是收尾才跑的东西。现在 `build_apk.py` 开头就重算指纹、
> 对不上直接中止并写明「先跑 `build_offline.py`」；那段代码还特意写明它只**拷贝**离线版 HTML、
> 不会重新生成。**这条是硬规矩：改了 `public/` 或 `data/` 里任何东西，`build_offline.py` 必须重跑。**
>
> **6. 一次自己造成的损失，和补救。** 给 `test_bridge.js` 做负向验证时，我用
> `git checkout -- public/app.js` 还原注入的坏写法，结果**把整个未提交的复盘传截图界面
> （按钮、`shotDone`、提示词扩展、渲染）一起清掉了**。补救：`认知训练-离线版.html` 里
> 内联着完整的 `app.js`，从最后一个 `<script>` 块里抠出来就是刚才那份，按 diff 逐段核对后写回。
> 教训：**做破坏性验证前先复制一份到一边，别拿 git 当"撤销上一步"**——这个仓库的未提交改动可能有很多。
>
> 另外记两个自动化上的事实：这个 App 的界面在 WebView 里，`uiautomator dump` **看不到里面的节点**
> （只有一个 WebView 框），所以模拟器上只能按坐标点；而启动时那个「要不要放点声音」浮层
> （**每次进来都问一次**）会把后面每一次点击都吃掉，盲点序列连偏三次。
> 新增 `tools/drive_app.py`：每步截图、按**背景模糊程度**判断浮层在不在，一次走完并留下总览图。
>
> **验证到哪一步（要说实话）**：真安卓上跑通了 **选图 → 原生降采样/编码/base64 → 桥 →
> JS 解析 → 切段 → 组装请求**，界面停在「还没填 API 密钥：点右上角 ⚙ 填一次就行」——
> 这正是那台模拟器上唯一走不下去的地方。`check_replay_shot.js` 里那 34 条断言是把
> `llmCall` 换成桩来跑的，验的是**请求形状**，不是模型答得好不好。
>
> **视觉那一跳后来补上了（2026-09-11，用户给了有效 key）。** 新增
> `tools/probe_shot_ocr_live.js` 走 App 自己的代码路径真打一次：中文聊天截图
> **2.4 秒返回、真值关键词 4/4**，按原生那种 JSON 字符串形状再调一次也通。
> 顺带量出一个必须记住的数：**它是推理模型，OCR 一张图要花约 1500 个推理 token**，
> 所以 `ocrChatShots` 里那 4000 `max_tokens` 是必需的（我第一版探针给 900，
> `finish_reason=stop` 而正文是空的——全被推理吃了）。另外
> `tools/probe_vision_models.py` 量出：`deepseek-v4-flash-vision-exp` **不在
> `/models` 列表里但是能用的**（Vision 是未公开实验名），而列表里的
> `deepseek-v4-pro` 反而答「无法读取图片」——**别因为列表里没有就把它换掉**。

>
> **APK 2.35（versionCode 37）**，权限仍 6 个，体积 0.51 MB。
>
> **v0.11.4（2.36）变更（已实现）：达标要判对；修掉场景人称 bug；接外部评审并逐条核实**
>
> 用户给了一把有效的 DeepSeek key，让我「测一下」，然后说「你应该能找到 GPT 的中转站吧……
> 让他对于目前的这个项目提出意见改进，然后你再制定计划，然后再问我」。最后又补了两句
> 更正：「中转站没有挂，是你自己使用方法错了」和「那些资料库，你要自己核对一下，
> 是否真的正确，我看到有一些问题，比如说人称混乱」。**两句都是对的**，下面按顺序记。
>
> **1. 视觉那条链真跑通了（补上 2.35 留下的空白）。** 新增
> `tools/probe_shot_ocr_live.js` 走 App 自己的代码路径真打一次：中文聊天截图
> **2.4 秒返回、真值关键词 4/4**；按原生那种 JSON 字符串形状再调一次也通。
> 顺带量到：**它是推理模型，OCR 一张图要花约 1500 个推理 token**，所以
> `ocrChatShots` 里 4000 的 `max_tokens` 是必需的（我第一版探针给 900，
> `finish_reason=stop` 而正文是空的——全被推理吃了）。另外
> `tools/probe_vision_models.py` 量出：**`deepseek-v4-flash-vision-exp` 不在
> `/models` 列表里但是能用的**（Vision 是未公开实验名），而列表里有的
> `deepseek-v4-pro` 反而答「无法读取图片」——**别因为列表里没有就把它换掉**。
>
> **2. 我把中转站误判成「挂了」，是我自己的客户端错。** 用 `urllib` 请求
> `ai.ailink1.com` 时所有 `chat/completions` 都返回 `error code: 502`（而 `/v1/models`
> 正常），我据此写下「上游故障，不是配置问题」，改用 DeepSeek-v4-pro 当评委。
> 用户纠正后逐项隔离：**网关把 `User-Agent: Python-urllib/3.12` 当爬虫挡了**，
> 换成 `User-Agent: curl/8.4.0` 同一个请求就是 200，跟模型名/路由/`max_tokens` 都无关。
> 教训写在 README：「对方挂了」是会让我**直接放弃一条能用的路**的结论，举证责任很高，
> 至少要用第二种客户端复现一次。修法是给请求加一个正常的 UA；脚本改成双通道
> （`--provider ailink|deepseek`），两个都留着。
>
> **3. 内容库真的有人称 bug——用户看出来的。** `scenarios.json` 里 **s7（男上级）
> 和 s8（男同事）通篇是「他」，却没有 `ta` 字段**，而代码里 `ta` 缺省就是「她」
> （`voice.js:1955`）。所以打开这两局，界面会把一个 40 岁的男上级叫成「她」。
> 已补 `ta: "他"`。**为什么一直没被发现**：JSON 合法、字段齐全、测试全绿——
> 因为没有任何测试把「正文里的性别」和「`ta` 字段」放在一起比。
> 新增 `tools/audit_content_quality.py` 专门盯这一类，已进 `run_checks.py` 当门禁。
>
> 这个脚本第一版报了 **84 条误报**（拿卡片的 `stage` 去比 `stages.json`——两个 `stage`
> 是两个概念：卡片里的是**关系阶段**，`stages.json` 里的是**6 个关卡**），
> 而真问题只有 2 条，全被淹了。**刷屏的检查等于没有检查**，已收紧判据。
>
> **3b. 顺着核内容，又发现 README 自己的统计数是错的。** 它写着「`ok` 为空的卡
> （干扰项最多、区分度最好）目前是 11 张」——**实际 51 张**（实测分布：`ok` 为空的 51 张、
> 1 条的 10 张、2 条的 21 张、3 条的 2 张；84 张卡都是 4 个选项）。多半是后来扩题库时
> 没回头改。已改正，并照「版本号三处一致」的做法加了断言：**README 里那几个数字
> 必须和数据算出来的一致**，对不上就红。
> 写这条断言时又踩了同一个坑的第三次——README 里的数字是加粗的（`**` 和反引号夹在句子中间），
> 我直接拿原文去匹配，结果**正确的 README 也报错**，而那个报错看起来像"数字对不上"；
> 更糟的是它让负向验证失去意义（改错和没改错报同一句）。现在先剥掉 markdown 标记再比，
> 并且做了双向验证（正确→0 错，改错→1 错）。
> 另外顺手核了：卡片解释里点名选项字母（「C（责怪）过重」这类）与选项文本一一对得上，
> 没有因为改选项而错位；没有一处内容还在引用已删除的功能（语音输入/现实任务/今日复盘）。
>
> **4. 换成 GPT-5.6-sol 问了一遍，逐条核实。** 值得单独记的是——它的意见里
> **有一条是我没想到的、而且很可能对**：「读局」训练可能**强化**用户自述的
> 「分不清情况就直接下结论」——把一个更快给人贴标签的习惯练出来。这一条和
> 达标改成「判对」有张力（判对会不会更鼓励快速贴标签？），值得后面单独想。
> 其余我核实后分三类：
> · **成立**：`d′`/卡片/阶段评估都缺**迁移指标**（从没测过真实行为有没有变）；
>   「27 个检查只证明构建正确，不证明产品有效」；`INTERNET` 权限在包里，
>   所以「离线模块零网络」只是**代码约定**，不是权限层面的事实。
> · **不成立（我查了代码）**：它说「AI 返回内容走 innerHTML 就可能注入 → 调桥 → 读密钥」。
>   查下来**没有入口**：模型输出的每个渲染点都过了 `rich()`（内部先 `esc()`），
>   OCR 的文字是写进 `<textarea>.value`。为这条写了 `tools/audit_innerhtml.py`
>   把入口钉死（并做了负向验证：故意去掉一个 `esc()`，它确实会红）。
> · **与既有决定冲突，我没有采纳**：「砍掉护城河」「补上真实行为闭环」——
>   前者和用户定的硬约束（卸载即消失、不写系统级状态）冲突，
>   后者是用户**已经删掉**的「现实任务」并用「判局准确率」替代过。
>
> **5. 用户拍板只做一件：达标改成「判对」。** 这条一查就发现是真洞且很严重：
> `answerCard` 里是 `rate(...); bumpDaily('card')`——**无条件加一**，所以
> **连点四张、全错的也达标、也解锁游戏，护城河形同虚设**。而这和 App 自己的原则矛盾：
> 关卡用的是「判局准确率」。判据：有读局步骤的卡看读局判对没有，没有的看动作选对没有；
> 「也算说得通」不算。界面与闸门浮层都写明规则，并显示「答了 N 张、判对 M 张」。
> `check_today_ui.js` 新增一节**真的去答卡**（走真实 `answerCard`）验「判错不进、
> 判对进、闸门保持武装」——因为 `test_daily.js` 的达标断言是**复刻**判定逻辑的，
> 那个无条件加一它永远看不见。**测副本不等于测代码**，这已经是同一天里第二次栽在这上面。
>
> **APK 2.36（versionCode 38）**，权限仍 6 个，体积 0.51 MB。回归 **29 项**全绿
> （新增 `audit_content_quality.py` / `audit_innerhtml.py` 两项，`run_checks.py`
> 也改成按扩展名选 node/py——原来写死 node）。
>
> **方向文档：`人格发展研究规划.md`（2.34 之后新增，尚未实现任何一条）**
>
> 用户的话：「对我之前的认知app进行优化迭代。查阅资料，包括各种心理学pua，还有其他人际交往，
> 还有人格培养等等资料，广泛全面的收集，看看下一步能够使用什么、怎么做，做好总结规划、
> 具体的实现步骤、以及实现之后的结果预测。自行优化，以完善我的人格为最终目标，
> 不要快速得出答案，要全面综合的分析，而且要针对我的性格，有针对性。」
>
> **画像来源的更正（我先写错了一版）**：第一版我拿 `data/progress.json` 当成了"他的真实使用
> 数据"，据此推断出「盲点集中在事务化」并写进文档说"决定了整个方向"。用户纠正：
> **这个聊天在他手机上，那份文件不是我能在模拟器里观察到的他的行为**，而且他的个性他早就说过。
> 第二版起画像**只以他的第一人称自述为准**，`progress.json` 只当待确认的参考。
> （教训和 2.33 那次"桩返回同一个对象"同类：**手上有一个文件，不等于手上就是那个事实**。）
>
> 他的自述（原话）：「比较敏感，神经容易紧绷。经常喜欢自己一个人宅在家里，一直玩游戏，
> 脑子有时候不太清醒，不想事，不思考，直接得出答案。分析不清楚情况，加容易手淫。
> 介于过度为他人着想以及过度利己之间来回摇摆不定，不知道怎么弄，有时候喜欢学习别人。
> 完全没有自主性，还要担心这担心那的。个人自认为比较幽默，比较外向，说的比较多，
> 但是有时候分不清情况，就胡说。」
>
> 九路文献调研后（第一轮五路 + 针对自述的第二轮四路），关键判断：
> · **这些不是零散毛病，是一个闭环**：敏感紧绷 → 用低成本即时奖赏（游戏/手淫）压下唤起 →
>   时间精力被占 → 该做的没做 → 宅更深、社交回避被负强化（Piccirillo 2016）→ 更紧绷。
>   所以主线是**打断这个环**，不是继续加内容。
> · **他的两个核心特质恰好是文献上最可改的两个**：12 项研究 n=2238 的 mega 分析显示
>   有意改变对外向与情绪稳定效应最大（Hudson 2020）；情绪稳定性在 207 项干预研究里
>   变化幅度最大、随访 6–12 个月维持（Roberts 2017）。
> · **最高杠杆是「每天排一个非屏幕动作（含运动）」**：运动是唯一同时命中"敏感/紧绷/担心/宅"
>   四个主题的干预（Singh 2023, BJSM，97 综述/1039 试验）；行为激活的核心是**增加行为**
>   而不是减少行为，App 自助版有阳性 RCT（Dahne 2025, JAMA IM, N=649）。
> · **「没有自主性」的解法是让他自己选价值观**：「给理由＋承认感受＋给选择」→ 内化，
>   「规定＋压力」→ 内摄且后果更差（Deci 1994）；而价值观澄清对低 SCC 者收益最大。
> · **他的强项是杠杆不是短板**：「装外向会开心」有直接因果实验（Fleeson 2002；Margolis &
>   Lyubomirsky 2020），且耗竭只见于"外向者被迫装内向"（Kuijpers 2022）；但红利只来自
>   **energy facet**、不来自话多（Margolis 2020）——目标设"投入度"而非"更能说"。
> · **两条新增的硬边界（明确不做）**：不做 HSP/高敏感人格测评（Hellwig & Roth 2021 显示
>   该构念与神经质高度重叠，是"神经质换了个好听名字"）；不做"色情成瘾/性成瘾"诊断与
>   戒断打卡（DSM-5 无此诊断、ICD-11 的 CSBD 不在成瘾章节；频率不是问题，痛苦多来自
>   **道德不一致**而非用量：Grubbs 2019；NoFap 好处无实证：Prause 2024）。
> · 纠错卡候选从 2 条扩到 28 条（前 8 条是他本人最可能信的：HSP、戒色、色情成瘾、
>   游戏必须戒、找到真我、正面肯定、内向改不了、"等 3 秒就不胡说"）。
> · 明确不做：HSP 测评、成瘾诊断、人格分型、真我结论、空泛肯定、强戒断/设备管控、
>   创伤暴露、ABM、双耳节拍、冥想/呼吸的疗效承诺、给频率贴道德标签。
> · **转介边界**：紧绷/担忧影响睡眠工作、或出现自伤念头 → 做专业评估；PHQ-9≥20 或自杀风险
>   不提供自助流程；"脑雾"不搞器质诊断与补剂建议。
>
> 拆成 2.35 排班（BA 最小版本）/ 2.36 价值观→if-then / 2.37 IU + 判断依据 + 外向配额 /
> 2.38 前后测框架 + 转介提示，每步写了改哪个文件、加什么断言、怎么验收，以及**可验证的
> 结果预测**（含"没达到说明什么"，例如两周内行为完成率上不去提示上游可能是睡眠/焦虑障碍）。
> **这一轮没有动任何代码或内容** —— 交付物就是这份规划。
>
> **v0.11.2（2.34）变更（已实现）：UI 重排第一步——设计地基（变量层 / 层级硬伤 / 死代码）**
>
> 这一轮由用户拍板：「我这个制作App你看看有没有什么skill或者工具之类的，也可以先用上
> 方便制作。还有UI设计，看看有没有什么skill之类的，可以重新设计一下。」→ 我查了 skill
> 清单（没有 UI 设计类），给了方案，他选「**连信息架构一起重排**」和「**装模拟器，我想看
> 真机跑起来**」，方案批准。方案分三步，每步都能单独交付：**第一步设计地基**（不碰结构）、
> 第二步信息架构、第三步逐屏视觉重做。这一条记的是第一步。
>
> ***第 0 步：先上保险。*** 这个仓库之前**没有任何版本控制**——要重排一个 1046 行的唯一样式
> 表而没有回退手段，改坏了只能手改回来。先 `git init` + `.gitignore`（排除 `.env`、
> `progress.json`、生成的离线版 HTML、`output/`、构建树；**刻意保留** `android/认知训练.apk`）
> + 首次提交 198 个文件。（密钥本来就在 `.env` 和手机本地，仓库里没有。）
>
> ***第一步动了什么***
> 1. **抽变量层**（`tools/design_tokens.py`）。铺之前的实况：125 个色值 / 214 处、36 个字号 /
>    159 处，而 `var()` 只用过 246 次——同一份规则里上一行写字面量、下一行写 `var(--good)`。
>    颜色按"角色"收成 6 个家族 × 4 个位置（main/bg/line/fg）+ 表面/描边/文字，
>    色值 125 → 40 附近，替换 201 处、**零漏网**；字号收成 5 档（11/13/15/18/22px），
>    **正文从 13.4px 提到 15px**（原来 13 个值挤在 .84→.96rem 这 1.92px 里，视觉上根本分不出来）；
>    间距 6 档、圆角 4 档、z-index 7 档、阴影 2 个。
> 2. **修交互层级的硬伤**（`tools/css_structure_fix.py`，14 处，每条都带断言）。铺之前
>    toast 和 pop-sheet **都是 60**（互相盖）、闸门 80 让 toast 永远看不见；
>    现在 toast(90) > 闸门(80) > pop(60) > sheet(50) > 浮标(40) > tab(30) > 顶栏(20)。
>    顶部补 `env(safe-area-inset-top)`（刘海会顶进状态栏），`.sheet .inner`/`.pop-in` 补底部
>    安全区，浮层高度 `vh → dvh`（键盘弹起时不被遮）。
> 3. **删死代码 + 合并重复组件**（`css_drop_dead.py` / `css_merge_dupes.py`）。删 27 块
>    （2.31 撤掉语音输入留下的 `.ptt*`/`.mic-big*`/`.hf-toggle*`/`.phase-*`，以及 `.tabs`
>    的两组失效覆盖等），文件 1099 → 1041 行；合并 10 处（`.pkg-item`/`.voice-item`/`.track-item`
>    是三份逐字拷贝，`.daily-card` 与 `.today-card` 逐字节同款）。**类名只合并、不重命名**
>    （旧类名留着当别名），避免某处 `querySelector` 突然找不到。
>
> ***这一步我犯的错：变量定义自己被自己替换掉了***
> 色值那一遍是"整表逐值替换"，它把 `:root` 里的**定义行本身**也换了——
> `--line: #2e323e` 变成 `--line: var(--line)`，于是全部描边、进度条槽、分割线变成透明。
> 而我的自查**只检查了 `:root` 之外的正文**，所以自己说"0 个漏网"，同时把界面弄坏了。
> 是 `check_today_ui.js` 逮住的（「进度条有底色 → rgba(0, 0, 0, 0)」）——
> **又一次"语法检查过了不等于跑得起来"**。修法：先把 `:root` 整段切下来、只变换正文，
> 并在脚本里加一条"token 定义不许出现 `var(自己)`"的自查；然后从备份恢复原文件、
> 重跑整条流水线。这一条现在有断言守着了。
>
> ***这一步的验收方式：不靠眼睛猜，量出来***
> 截图上看「设置」竖排成两行、tab 条副标题折行，我第一反应是"字号提到 15px 挤的"。
> 但"是不是这一步挤的"光看新图判断不了，所以**用 git HEAD 那份 style.css 拼了一份对照组
> 页面**（`.tmpcmp/prev-step1.html`），两边跑同一段测量（`tools/diag_step1_layout.js`
> 扫全页文字块、`tools/probe_step1_spots.js` 定点量）。结论和直觉相反：
> · tab 副标题折行**原来就是两行**（旧 [1,2,1,1] → 新 [1,2,1,1]），tab 条只从 75 涨到 82px；
> · 成长页「设置」被挤到 24px 宽、竖排 3 行**也是原来就有的**，新版反而是 2 行（更好的那个）。
> 两次都差点去修一个没坏的东西。这一条和 2.33 的"桩返回同一个对象"是同一类：
> **怀疑回归之前先量一遍**。
>
> ***量出来真正属于这一步的两处，当场修了***
> · **tab 条是"外壳"不是正文**：正文提到 15px 时它跟着长（75→82px），而它下面那行 11px
>   副标题一个字也没变清楚。`b` 从 `--fs-md` 改回 `--fs-sm`，tab 条 82 → 79px。
> · **按钮上的字永远不换行**：中文按钮被挤窄时会一格一个字竖着排。加了
>   `button.primary/ghost/plain/.link-btn { white-space: nowrap }` + 行内非主按钮
>   `flex-shrink: 0`。改完「设置」从 27px/2 行变成 30px/1 行，正好是它的文字宽度。
>   （通用规则，不是给那一个按钮打补丁——"一行两个按钮"的地方都可能有这个形状。）
> · 已知的、**故意留着**的：间距映射把 2px 这类小内边距四舍五入到 `--sp-1`(4px)，
>   于是小标签高度 24 → 28px、整体略微"松"了一点（205 处，最多 2px）。
>   这是这一步的本意（统一到档位），逐屏精修放在第三步。
>
> 复核：`tools/shot_ui_step1.js` 拍 9 张关键页（今天/卡片/AI 对话/成长/设置/历史列表/
> 历史详情/答题后/底部浮标与 tab），**每拍一张先跑一遍溢出扫描**（`scrollWidth > clientWidth`、
> 纵向裁切，排除横向滚动容器和 ellipsis 截断）——0 处被裁、0 处页面横向滚动、0 条页面报错。
> 人眼逐张看过。回归 26 项全绿（含专盯样式行为的 `check_today_ui` / `check_tabs_unique` /
> `check_audio` / `check_settings_ui`），`audit_project.js` 0 错。
>
> **APK 2.34（versionCode 36）。** 权限仍 6 个，体积 0.50 MB。
>
> ***第一次在真 Android 上跑起来（这个项目从 2.0 到 2.33 一直是空白）***
> 用户说过「装，我想看真机跑起来」，模拟器其实早就在跑了（`emulator-5554`，Android 14，
> 1080×2400 / 420dpi）。`adb install -r` 装进去、`am start` 拉起来、`adb exec-out screencap`
> 截图——**一次就起来了，没有报错**。随后 adb 点着四个 tab 各拍了一张。
> 三件得到确认的事：
> · **布局是真的能用**：412px 的浏览器截图和真机几乎一模一样。
>   换算关系是 1080 / 420 × 160 = **411 CSS px**，跟我拍图用的 412px 视口差 1px——
>   也就是说我这一路"在浏览器里量"的办法对这台机器是**成立**的，不是自我安慰。
> · **安全区真的生效了**：WebView 是 edge-to-edge 的，`env(safe-area-inset-top)` 让
>   顶栏躲开了状态栏、`env(safe-area-inset-bottom)` 让 tab 条躲开了手势条
>   （底部实测留了约 26px）。第一步补的这两处不是纸面功夫。
> · **一次误判**：真机截图上看 AI 页右上角那个「📋 历史」像是贴到屏幕边上了，
>   我当成"右边少了内边距"。量完（`tools/probe_subnav_gap.js`）**左右各 16px、完全对称**，
>   旧 CSS 也一样。把那张图切出来放大两倍看，果然留着白——是我看图看错了。
>   **截图上的"看起来像"不能当证据，能量就量一下。**
>
> ***真机上量出来的一处真问题：tab 条副标题折行***
> 机身 411 CSS px 宽 / 4 个 tab = 每个 103px，减去按钮左右内边距只剩 95px；
> 而「卡片 · 校准 · 微课 · 信号场」在 11px 下要 110px 以上，
> **任何能读的字号都放不下**——四个里三个折成两行，tab 条连底部安全区一起占 105px，
> 相当于每页少三四十 px 内容（这个折行第一步之前就有，不是那次挤出来的，见上面的量尺结论）。
> 这里有一条**浏览器和真机不一样、必须记下来的**：桌面 Chromium 走雅黑、
> 手机走 Noto Sans CJK，**同样字号下中文字宽不一样**，所以我在 412px 浏览器里
> 看到"只有练习那一条折行"，真机上三条都折。**折行这种事以真机为准。**
> 按方案第一步第 5 条「加一个断点替代手工覆盖」，收了窄屏的副标题
> （`@media (max-width: 480px) { .tabs button small { display: none } }`），
> tab 条 79 → 38px；宽屏保留。信息没丢——那串副标题列的东西正是各页**页内切换条**
> 上那几个按钮，点进去就在眼前。新增 3 条断言钉住：手机上必须收起、
> 宽屏必须还在、宽屏下每条必须只占一行。
>
> 新增脚本：`tools/design_tokens.py`、`css_structure_fix.py`、`css_drop_dead.py`、
> `css_merge_dupes.py`、`shot_ui_step1.js`、`diag_step1_layout.js`、`probe_step1_spots.js`、
> `probe_subnav_gap.js`。
> **下一步（第二步 信息架构）**：「练习」切换条加 `🔁 翻车回放` 动作位、删掉成长页顶部那张
> 重复的每日计划卡、拆开一个变量给两个 tab 当路由的 `cardsView`、入口收敛（天花板 3→1、
> 脑雾记录从设置挪进成长）、「上次没聊完」在今天页也露一条。
>
> **v0.11.1（2.33）变更（已实现）：AI 上下文接得上；删掉顶部那条横幅；画像改成"只是参考"**
>
> 用户的原话：
> 「还有就是认知训练中AI模块上下文很容易丢失。还有最上面那一个提示离线单文件版，
> 离线模块全部可用，什么密钥那个直接删掉，不需要提示了。然后那个摸底形成对我的标签
> 只是弱相关，就是仅仅作为辅助，从而影响接下来的出题，并不能这么给我下定义。
> 然后随着我不断答题，然后不断更改我的个人形象。然后我这个制作App你看看有没有什么
> skill或者工具之类的，也可以先用上方便制作。还有UI设计，看看有没有什么skill之类的，
> 可以重新设计一下。」
>
> 1. **AI 上下文丢失——先诊断再动手，结论和直觉不一样。** 写了
>    `tools/diag_context_loss.js` 分三步实测：**在 App 里切页不丢**（V.sess 在内存里，
>    回来照原样重画，输入框也在）；**丢的是"离开 App 再回来"**——手机上被系统杀掉 /
>    划掉 / 放置太久重建 WebView，内存里的会话没了。而逐轮记录其实一直在存
>    （2.31 就改成每轮落盘了），所以**内容没丢，丢的是"他知道还能接着聊"这件事**：
>    回来只看到一个干净的选场景页，那条没聊完的记录躺在「历史」里要自己翻进去点。
>    看着就像对话没了。
>    修法：AI 首页最上面加一张**「上次没聊完」**的卡（暖色边，跟蓝色的「今天就练这个」
>    区分开）——标题、什么时候聊的、聊了几轮、温度，一个按钮「接着聊这一局」接上。
>    没有没聊完的局时这张卡不出现；点过「结束」之后也不再出现。
> 2. **顶部那条横幅删了**。它是我加在离线构建里的（不是页面的一部分）：
>    「离线单文件版 · 离线模块全部可用 · AI 只要在设置里填一次自己的密钥」。
>    那是**给第一次拿到这个文件的人看的说明**，而他是天天用的人——每天开 App 先看一句
>    "离线可用"没有信息量，还占掉最上面那一条。所以从 `build_offline.py` 里删掉，
>    产物里已经查过（0 处）。
> 3. **画像改成"只是参考，不是给你下定义"**。原来那页上来就讲偏差、底部还挂一句
>    「你最主要的偏向」——口气像结论。现在多了一段说明：下面这几类是把你**已经答过的
>    那几道题**里出现过几次同一类误读数了一遍，跟"你是什么样的人"是**弱相关**；
>    样本少的时候说明不了什么，继续答题它就跟着变，练得多了某一类自然会掉下去；
>    它也**不会决定出什么题**，最多让那一类多出现一点（实现上确实如此：`cardWeight`
>    只给那类卡片加 1 分权重，出题主体仍是卡片本身和你的进度）。那句标签也改成了
>    「这阵子出现得最多的一类」。
> 4. 顺手修了个**轮数 off-by-one**：原来五处显示都拿 `sess.turn`（把她那句开场白也算了），
>    所以回过两次会显示"聊了 3 轮"。统一成 `talkRounds()` = **你回过几次**
>    （= 逐轮记录条数）。是新加那张卡时发现的——新卡上写错数字等于我自己在说谎。
>
> ***这一轮我犯的错（两次都是"看起来像产品坏了"，其实是我测试写错）***：
> · **桩返回了同一个对象**：`llmCall` 的假实现写成 `async () => args.turn`，
>   两次调用共用同一个对象；而 `nextTurn` 会往返回值上写 `userText`，于是第二轮把
>   第一轮那条记录里的 `userText` 覆盖了——现象是"第一句丢了"，我差点去查产品。
>   改成每次 `Object.assign({}, …)` 返回一份新的。**桩要返回新对象**，记下来。
> · **`open()` 里的 `addInitScript` 每次导航都会重置存档**，而第十节要验的恰恰是
>   "重载之后存档还在"——用它等于自己把记录清掉，看起来就像这张卡没做出来。
>   那一节改成自己开页（也更接近真实的"重开 App"）。
> · 另外 `check_doubao.js` 里「AI 页顶部就是今天就练这个」这条**撞上了新行为**：
>   前面刚聊过一局，顶部该是「上次没聊完」。改成把**两种顺序都钉住**（有没聊完的局时
>   它排第一、清掉之后「今天就练这个」才是第一），没有放宽。
>
> 测试：`check_talk_history.js` 新增第十节（没聊完的卡出现/排第一/写了轮数/按钮指向
> 接着聊/点了真接上/屏幕重放/结束后消失，共 10 条）；`run_checks.py` 26 项全绿。
>
> **APK 2.33（versionCode 35）。** 权限还是 6 个，体积 0.50 MB。
>
> 关于用户问的**「有没有 skill / 工具可以少费点手工」和「UI 重新设计」**，
> 我逐条看了手上可用的 skill 清单，结论：
> · **能用上的**：`playwright`（我那些 shot_*/check_* 脚本是手写 node + 硬编码
>   Playwright 路径，用它的 CLI 更省事）、`browser-use:web-gui-tester`（黑盒 GUI 核验的
>   方法学——"每个结论都要有一张真看图"这条正是我这轮在做的事）、`skill-creator`
>   （可以给这个项目写一个专属 skill，把"出图 → 拆浮层 → 人眼看 → 断言"这套流程和踩过的
>   坑固化下来）、`recording-solved-problems`、`agent-handoff`（跨会话交接）。
> · **最值的一个是官方 `android-emulator` 插件**：带 MCP + `android-dev` skill，
>   能 build → 装进模拟器 → 跑起来 → 截图核验。**这是这个项目最大的空缺**——
>   我到现在为止一次都没在真 Android 上跑过，只能靠"浏览器里跑离线单文件版"近似。
>   但它现在**没在本会话启用**，而且本机缺两样东西：`sdk/` 里没有 `emulator` 本体、
>   也没有 `system-images`（只有一个 `harness.avd` 的空壳定义，target android-31）。
>   `avdmanager` / `sdkmanager` 都在（`cmdline-tools/latest/bin`），补装是一行命令的事，
>   代价是一次 1 GB 量级的下载 + Windows 上要能用虚拟化。等他定。
> · **没有 UI 设计类的 skill**（我把清单逐条过了一遍）。所以 UI 重设计只能我自己来 +
>   参考公开的设计规范（Material 3 那套颜色/字号/圆角/层级的角色化写法）。
>   这件事我**没有动手**：这个仓库没有版本控制，改坏了不好回退，方向得他先定。
>
> **v0.11（2.32）变更（已实现）：AI 情景库——造过的攒起来，按缺口铺开，按关系混排**
>
> 用户的原话：「ai 里面的情景数量太少了，你都用 AI 了，应该可以生成许多，但是也不能
> 随机生成，只要有环境，你得给一些比较好的提示词让 AI 生成。你先分析一下吧，之后再执行。」
> 接着说：「查阅一下资料，用大白话的方式或者举例的方式跟我说明，我来决定。」
>
> **先查资料再定（这一轮没有直接动手，先把依据摆给他）**：
> · 同一个模型拿同一个提示词反复要，出来的东西会越来越像——*Artificial Hivemind*
>   （arXiv 2510.22954，NeurIPS 2025）实测 79% 的情况下两两相似度超过 0.8；
>   所以"多"应当是**一次多要几个**，而不是反复点「换一批」。
> · 用 AI 造题库的通行做法是**先生成、再过滤去重**（Self-Instruct 那套：相似度超阈值就扔、
>   同主题最多留几条），光过滤就淘汰三四成——所以质量不能只靠提示词求。
> · 难度该卡在"大约八成半能应付"（Wilson 等，Nature Communications 2019，即 85% 规则，
>   对应维果茨基的最近发展区）：太难学不到东西，太容易也没东西学。
> · **同一个题目隔几天换个说法/情境再练**，比"每次全新"和"每次一模一样"都记得牢
>   （PNAS 2024，变式检索；受试者自己还以为"重复同一条更有效"）——这条直接支持"攒成库"。
>
> **他据此拍板四条**：攒成库；一次造 6 个；现成的和 AI 造的**按关系混在一起**排；
> 造什么**只按缺口铺开**（不按弱项倾斜、更不掷骰子）。
>
> 实现：
> 1. `state.genScenes` 情景库（`GEN_KEEP=100`），跟 `talks` 一样进 localStorage 和备份 JSON，
>    **不进 APK**。满了先淘汰"一次都没练过的、最旧的"，练过的留着。
> 2. 提示词重写成"brief + 硬规则"。硬规则里最关键的一条是
>    **her_state 必须解释 opening**——它就是现成场景 s1 那个 bug
>    （开头说"地方挺好找的，我绕了一圈才到"，内心独白却说"她紧张、怕尴尬"）的解药；
>    另外加了"opening 必须留接话口""第三个人只能在、不能开口"（对话引擎只演一个人，
>    一桌人的场景演不了）、"难度给判据"、"关系只能从固定枚举里挑"。
> 3. 缺口表 `sceneGap()` + `sceneBrief()`：造之前数一遍现有场景，点名最缺的那几个关系；
>    已有标题/主题一起喂进去去重。**删掉了原来那套"五轴随机"**
>    （谁/在哪/练什么/难在哪/最容易错 各抽一个拼起来）——它会拼出
>    「健身房的熟人 + 深夜发消息 + 必须当场答复」这种生活里不存在的组合。
> 4. 生成后机械检查 `screenScenes()`：标题/主题跟已有的撞就扔、同一次里同一个关系超过 2 个就扔；
>    不够数**只补缺的那些**（最多一轮），补造失败也如实说一句"少了几个、为什么"。
> 5. 列表改成 `sceneLibCard()`：筛选胶囊（复用卡片页那套 恋爱/职场/朋友/家人/泛社交）+
>    展开后按关系分组的小标题 + 每张卡标"练过 N 次""AI 造"。默认那几张用
>    `sceneSample()` 取——**练得最少的优先、每个关系最多一张**。
> 6. `startSession(id)` 的查找范围从"现成 14 个"扩大到 `allSceneList()`：否则从库里点一个
>    AI 造的场景会**静静什么都不发生**（`if (!sc) return;` 是这一页最容易犯的那类毛病）。
>
> **真接口实测（`tools/probe_batch6_live.js`，打的是 App 自己这套提示词）**：
> 连造两轮，各 9.1 秒 / 7.4 秒出 6 个；关系按点名的一个一个来；两轮之间缺口表自己往前挪
> （第一轮补完的档位升到后面，下一轮去补别的）；与现成 14 个无重名、库内无重名、
> 字段全、theme 全给；`her_state` 确实在解释开场白（例：
> 「可以啊你，今天讲得真稳。」→「嘴上夸你，其实是在等你主动把功劳分她一半」）。
> 难度分布 3/6/3。
>
> ***这一轮我犯的错（都记下来）***：
> · **同一毫秒生成的两条 id 会撞**，撞了以后第二条被 `addGenScenes` 按 id 去重**静静扔掉**——
>   测试里表现是"造 6 个库里只多 3 个"。id 改成带随机后缀。这个是**真 bug**，
>   而且症状正是最难查的那种：数量不对、不报错。
> · **注释写反了**：我在 `sceneSample()` 上写"新造的一定排得进默认那几张"，实测发现
>   新造的和没练过的现成场景计数都是 0，同分时现成的在前——**根本挤不进去**。
>   已改成实话，并想清楚了入口：刚造完那一下靠批卡的「练这个」，以后找要靠关系筛选或「看全部」。
> · 编辑时把 `const sc = {` 复制成了两行，`node --check` 当场逮住——这次没流到浏览器里。
> · 测试里我又写错了三处断言（假设库里第一条在首屏、假设列表顺序还是"数据前 6 个"、
>   样本没给 id），改的都是样本和断言，不是产品。
>
> 测试：新增 `tools/check_scene_lib.js`（缺口表/brief/去重/进库/上限淘汰/端到端两轮/
> 按关系混排/从库开局/进存档重载还在，共 40 余条），注册进 `run_checks.py`；
> `check_voice_talk.js` 里四条守"五轴随机 + 弱项"的断言**整块重写**成守新形状
> （缺口点名、枚举给全、已有标题去重、弱项**不再**掺进生成），没放宽。
>
> **APK 2.32（versionCode 34）。** 权限还是 6 个（没动权限），体积 0.50 MB。
>
> ***这一轮我验证不了什么***：「多」和「可控」我验了（缺口、去重、数量、进库、重载）；
> **场景好不好、像不像真人，我验不了**——只能把造出来的摆给他读。我自己读了 12 个，
> 觉得「妈妈翻到你药盒」「朋友试新发型」「上级追问进度」写得挺准，
> 但那是我的读感，不是验证。
>
> **v0.10.6（2.31）变更（已实现）：删掉语音输入；情景 6→14 且能 AI 现造；历史能续聊；窗口和入口改一轮**
>
> 用户这次一口气说了四件事，原话：
> 「ai 里面的情景数量太少了，都已经连上 ai 了，可以给出不一样的。」
> 「历史记录并不是结束之后才保存，而是只要有过一轮对话，即便中途没结束，
> 也可以继续续上。」
> 「有一个问题是你这个地方还挺好找的，我绕了一圈才到，那到底是说他好找还是不好找。」
> 「还有一些窗口什么的，感觉设计的不是很好，但是整体其实还是挺棒的，
> 然后一些设计的问题，你看看能不能参考一些资料或者去询问 GPT。」
> 以及插进来的一句（优先级最高，其余三件照做）：
> 「做情景模拟 ai 对话的时候，等他说完话，我这边就会自动录音，停止这个功能。
> 直接把这个录音的功能删除。我直接打字算了。」
>
> **1. 语音输入整块删掉。** 删的不只是"她说完就自动开麦"那个循环，而是整条输入链：
> 免提状态机、按住说话、「开始说」按钮、语音识别（系统那三条路 + 2.30 才打进去的
> 开源离线识别）、麦克风权限、设置页里那一整块识别诊断，以及原生侧那 10 个桥方法
> 和 `LocalAsr` 整个类。**她的声音保留**（TTS，音色照样能选）——他嫌的是"我被录音"，
> 不是"她出声"。
> 为什么整条删而不是只关自动那一段：只留「按住说话」的话，那一整套识别代码、
> 65MB 的中文模型、`RECORD_AUDIO` 权限都还得背着——为一个他明确说不要的功能背着这些，
> 比删掉更糟。**结果：安装包 48 MB → 0.5 MB，权限 7 个 → 6 个。**
> 那套东西（Vosk 的 jar、两个 ABI 的 `.so`、中文模型）留在 `android/vendor/` 里没删，
> 恢复步骤写在 `android/vendor/README.md`；`build_apk.py` 现在反过来**断言 APK 里没有**
> 它们（`stray` 检查），因为构建树里留着的旧文件会继续被打进包——
> 这一条被自己的断言抓到过一次（APK 又从 0.5 MB 变回 42 MB）。
> 顺带纠正一个我上一轮写错的数字：2.29 的包是 **0.5 MB**，不是我在 2.30 那一轮写的
> 「14 MB → 48 MB」里的 14 MB（那 14 是错的，这一轮对着文件大小核过）。
>
> **2. 情景 6 → 14，并且能「让 AI 现造 3 个」。** 现成情景全部重写过一遍并加了 8 个，
> 覆盖职场上下/职场平级/家人日常/朋友之间/泛社交，每个挑的是**不同的错法**，
> 不是同一个东西换句话（爸爸住院瞒着、朋友半夜失恋、被当众问收入、邻居搭话…）。
> 其中两个的对方是男性，所以 `ta` 这个字段第一次在现成情景里真的用上了
> （界面文案靠它，写错就会把爸爸写成"她"）。
> 另外加了中间那条路：一次让 AI 造 3 个、**摆出来让他挑**——随机那个每次只造一个、
> 造完直接进对话，他没得挑。提示词里把"三个必须不同（关系、场合、要练的动作）"
> 写成硬要求，并把已有情景的标题喂进去防重复；解析器认数组、认 `{"scenarios":[]}`、
> 也认数字键的对象。真接口实测（`tools/probe_batch_live.js`）：默认模型约 20 秒、
> 造出来的三个确实互不相同（合租室友 / 妹妹深夜电话 / 同事变上下级）；
> 换成带推理的 deepseek-flash 要 40 秒。额度从默认 3000 提到 5000——
> 推理吃掉额度时表现是"三个变两个"，看不出原因。
>
> **3. 历史改成每轮落盘，没结束也能接着聊。** 每聊完一轮就存一次（`done:false`），
> 列表上标「没聊完」并给一个「接着聊这一局」：点进去会把这一局**整个重建**——
> 场景、逐轮记录、温度、轮数、追问计数，以及**给模型的上下文**（按原来的形状：
> 开局那条元消息 + 她的开场白 + 一来一往），所以她是接着那个话题说的。
> sid 用原来那条记录的 id，接着聊的内容写回同一局，不会在历史里另开一条。
> 顺手修了一个真 bug：**切到别的子页再切回来，屏幕上的对话是空的**
> （`V.sess` 还在，但 `#herSlot` 是新建的空壳）——从历史"接着聊"同样看不到前面聊过什么。
> 现在渲染完统一重放一遍（重放是幂等的）。用户看到的"刚才那局没了"其实什么都没丢，
> 但**看着就像丢了**，这类"状态在、显示没了"最唬人。
>
> **4. 窗口和入口按资料改了一轮。** 我按他说的去查了资料（没法"问 GPT"，
> 但这个方向上有成文的东西），主要参考 Material 3 对底部浮层的规格、
> 以及几篇设置页 UX 的通行建议：
> [M3 Bottom sheets](https://m3.material.io/components/bottom-sheets/specs)、
> [Mobile settings page design patterns](https://setting.page/mobile-settings-page-design-patterns)、
> [Toptal: settings UX](https://www.toptal.com/designers/ux/settings-ux)。
>   · **他的问题「到底好找还是不好找」，我的判断是：能找到，但要绕。** 历史原来只在
>     「场景对话」那一页上有一个小图标——从体检、复盘、或者正在聊的一局里都够不着，
>     得先回那一页（3 步，而且路径不唯一所以"记得住位置"也没用）。现在它在 AI 那一路
>     的**二级导航右侧**：这一路 tab 的任何一页都是一步可达。这是资料里那条
>     「每一页都能用的动作应该跟导航同级，而不是藏在某一页的内容里」。
>   · **浮层不许叠浮层。** M3 里底部浮层本身就是最上层模态（和对话框同层），
>     在它上面再叠一个是明确的反模式；多步流程应该换成一个整屏目的地。
>     我们这儿真实发生过两个后果：两层遮罩（后面看着更暗但还能点）、
>     以及 `closeSheet()` **把所有浮层一起删掉**——于是"关掉每日计划"把下面那个设置面板
>     也一起关了。现在设置和每日计划是**同一个浮层里的两块内容**，中间是「← 返回设置」。
>   · **设置分区导航从"两列六个大格子"改成一行胶囊**（和 practice/ai/growth 三条二级导航
>     一个样式），加上 M3 规格的拖拽把手。那一排格子原来占掉浮层顶部三分之一，
>     而每个分区里往往只有两三行字——每次都得先滚过一排按钮才看得到正文。
>   · 顺手删掉重复的历史小图标：二级导航上已经有了，选场景页/结束页上再来一个是重复。
>
> **5. APK 2.31（versionCode 33）。** 权限 6 个（没有新增，删掉了一个）。
> 回归：见下面那行。**测试这一块是重写的**：`check_music_asr` 去掉识别三层路由、
> `check_voice_talk` 去掉免提状态机那一整套（它原来是这个测试的主体）、
> `check_voice_pack` 去掉识别自检、`check_talk_history` 去掉识别与麦克风那四节。
> 删掉的都是"那个功能本身没了"的部分，剩下的断言没有一条被放松。
>
> **6. 这一轮我自己犯的错（记下来，都是同类）：**
>   · **拿行号当剪刀删 Java 代码**，把 `onRequestPermissionsResult` 的括号删花了，
>     javac 报一串「class, interface, or enum expected」——看着像语法错，其实是结构错，
>     而且报错行离真正的伤口几百行。改成**按文本块整体替换 + 断言"只命中一次"**，
>     一处对不上就整体不写。
>   · **语法检查过了不等于跑得起来**：`updateLocalPrepNote()` / `asrHint()` 的定义被我删了、
>     调用点还在，`node --check` 全绿，一渲染就 ReferenceError。这两个都是截图脚本
>     先炸出来的（如果只看静态检查，会一路绿到手机上）。
>   · **截图脚本又被浮层骗了一次**：四张图全被「每日计划」浮层盖着，而断言查的是 DOM
>     （是对的），脚本一声不吭地跑完。上一轮刚记过这条，这一轮还是忘了——所以这次
>     把"每拍一张之前先清浮层"写进了脚本本身。
>
> ***我验证不了什么***：真机（这次没有 native 库了，比上一轮的风险小得多，但
> 「她出声」那条仍然只能在手机上听）；AI 现造那三个的**质量**——我只验了形状
> （三个互不相同、能解析、能渲染、界面摆了三张卡），好不好是模型的事；
> 以及窗口改动那股"手感"（静态截图看不出滑动和展开的手感，只能靠他用）。
>
> **2.31 交付之后又补了一轮（版本号不变，还是 2.31 / versionCode 33）**
>
> 这一轮不是新功能，是把「删掉语音输入」和「入口/窗口改造」的余波收干净——
> 好多断掉的地方是**删了功能但留下了话**，界面上还写着已经不存在的语音。
>
> 1. **浮层没拆就跳页**（真 bug，虽然当前 UI 上走不到）：对话历史是一个**整页**，
>    而设置是一个占满全屏的浮层。`openTalkHistory()` 原来是直接往 `#view` 里渲染，
>    如果此刻正开着浮层，历史就渲在浮层**底下**——屏幕一点没变，表现就是
>    「点了没反应」。现在加了 `dropSheets()`（只拆浮层、不重渲染），
>    在 `openTalkHistory / renderTalkRecord / resumeTalk / backFromHistory` 四处入口调用。
>    `check_talk_history.js` 加了一条断言钉住它（先开设置再进历史，浮层必须没了）。
>    真实的坑比这条路径大：**状态换了、屏幕没换**，这跟之前那个「状态在、显示没了」
>    是同一类。是截图脚本先撞出来的。
> 2. **界面上还残留三处「语音陪练」**：`index.html` 的 meta description、
>    发模型的系统提示词第一句（写的是「你在做一个「语音陪练」」——模型会以为在跑语音）、
>    以及设置页「密钥在哪拿」那段里的「语音陪练和另外两个 AI 模块用不了」。
>    都改成了与界面一致的叫法（AI 陪练 / 情景对话陪练）。
> 3. **测试里的三处同源失败**（都是这轮改造带来的，不是删语音造成的）：
>    · `check_reported_bugs.js` 数「切换条上有几个按钮」——历史入口现在挂在切换条上，
>      于是 3 个子页被数成 4 个。给子页按钮加了 `.subnav-tab`，断言数它；
>      同时**新增**一条：切换条上除子页之外只能有带 `.subnav-act` 的动作入口
>      （不然就是把一个点了不换页的东西混进了子页队列）。
>    · `check_scene_talk.js` 那条名字写「六个」、身子却查 14 个的标题断言——场景涨到 14
>      之后名字和身子就对不上了。重写成两条：默认摊开的 6 张卡各自印着自己的标题；
>      点「看全部」之后 14 个标题全部进 DOM（比原来更强），再断言列表收回默认状态。
>    · `check_doubao.js` 数模型胶囊用了 `.chips .chip-btn`，把设置页顶上那 6 个分区导航
>      一起数进来了（3 个模型数成 7 个）。收窄到 `#setBody .chips .chip-btn`，
>      另加一条：分区导航是 6 个、且没混进模型胶囊里。
> 4. **自查时被自己的样本骗了两次**（记下来，免得下次再犯）：
>    · 截图脚本里历史记录写进了 `eq-state` 这个键，而真实键是 `eq-state-v2`——
>      页面照样打开、只是空的，看着像"历史功能坏了"。**键写错不报错，只少东西。**
>    · 同一份样本里 `goods/bads` 塞的是 `['先问再答']` 这种数组，真实字段是**次数**，
>      于是「接住了」那一格印出一整句话、把三格标签挤得高低不齐，看着像排版 bug。
>      两处都改的是样本，不是产品——**断言/截图和事实冲突时，先假设样本错了**。
>
> 交付：`py -3 tools/run_checks.py` → 25 项 0 失败（含 audit 0 错），
> `check_settings_ui.js`（要 server.py 在跑）全部通过；APK 重新构建后**逐项核过**：
> 513575 字节、versionCode 33、6 个权限、`RECORD_AUDIO` 0 条、包内无
> `lib/`/`vosk`/`jna`/`.so` 条目、dex 里无 `LocalAsr`/`vosk`，
> 包内 `assets/index.html` 与现在的离线版**逐字节相同**（哈希一致）。
> 桌面留 2.30 + 2.31 + 固定名三个文件，固定名与 2.31 同一份。
>
> ***这一轮我还是验证不了***：真机上手感（切换条右侧那个历史入口会不会太挤、
> 设置页顶上那排分区胶囊滑起来顺不顺）——截图只能看出"没坏"，
> 顺不顺手要他用一次才知道。
>
>
> **v0.10.5（2.30）变更（已实现）：App 自带开源离线识别；对话结束后的历史记录**
>
> 用户的两句话：「点击开始说之后，它会提示这台手机没有可用的语音识别引擎，可能没装识别服务。
> **你可以装上呀**，比如说开源的识别服务，就像各种输入法之类，它就有语音转文字那种。」
> 「ai 对话可以给出一个结束功能，点击结束之后对整段对话进行一个综合分析以及提出建议，
> 之后保存到历史记录，有一个小图标可以点进去查看。**以及不用的可以删除。**」
>
> **1. 识别从"问系统要"改成"自己带一套"。** 原来只有两条路：系统的识别服务、
> 系统识别界面。他手机上两条都没有（很多国产 ROM 把 Google 那套去掉了），
> 于是按他的要求把引擎**装进 App 自己身上**：开源的 Vosk（Apache 2.0）
> + 中文小模型 `vosk-model-small-cn-0.22`（同为 Apache 2.0，允许随包分发）。
> 不联网、不依赖系统里有没有识别服务。代价是体积：安装包 14 MB → **48 MB**，
> 其中 42 MB 是那个模型（解开 65 MB，放在 App 私有目录，卸载时跟着一起消失
> ——这条正好卡在他的底线上：不往外留任何东西，也不碰系统设置）。
>
> **2. 四样东西必须同时成立，缺一样都只是"语音用不了"。** 模型进 `assets/`、
> 两个 ABI 的 `.so` 进 `lib/<abi>/`、Vosk 的类进 `dex`、manifest 的
> `extractNativeLibs=true`。**第四样是沉默的杀手**：前三样全对、所有静态检查全过，
> 只有它是 false 时 JNA 按名字 `dlopen` 会失败，真机上就是一句
> 「本地识别库加载失败」。所以 `build_apk.py` 现在逐项核对这四样（缺任何一样
> 直接中止构建），`audit_project.js` 也**对磁盘上那个包**再查一遍——
> 构建时的检查管不了"这个包是什么时候打的"。
>
> **3. 路由顺序：系统识别服务 > 自带离线识别 > 系统识别界面 > 打字。**
> 本地那套排在"识别界面"**之前**是有理由的：识别界面是模态的（每说一句都要
> 弹出去点确认），自带那套能边说边出字、也能中途停，所以免提在它上面是能用的。
> 有系统识别服务时仍然优先用系统的——一般是在线识别，中文准确率高得多，
> 自带那套是**兜底**，不是升级。
>
> **4. 第一次用要解 65 MB：说清楚，而且不许"松手之后自己开麦"。**
> 解包几十秒，这段时间界面原来只会停在「在听…」而什么都没录——现在先明说
> 「第一次要先准备模型（约 40 秒，只做一次）」，进对话页就开始后台解。
> 更阴的一个坑：用户按下「开始说」→ 还没解完就松手 → 解包完成时那段
> "准备好就开麦"的回调还在，**于是手已经松了，麦克风反而自己开了**，而且界面上
> 看不出任何异常。现在 `stopListen()` 会把这个待办一起取消掉，并有一条断言守着。
>
> **5. 失败一律降级，不崩。** native 库加载、模型解包、录音启动三条路在原生那边
> 都是 `catch (Throwable)` + 返回错误字符串，绝不抛到外面（`UnsatisfiedLinkError`
> 也在里面）；网页拿到错误就退回打字。诊断页现在**分四行如实报**：识别服务、
> 识别界面、自带离线识别、模型解开了没有，再加上库加载状态和**上一次错误的原文**。
>
> **5.5 接这块的时候抓出一个真 bug：麦克风权限从来没有人要过。**
> 录音权限只在原生 `listen()`（系统识别那条路）里申请，而这条路他手机上走不通；
> 本地识别**不经过** `listen()`，于是第一次点「开始说」时权限还没给过，
> 代码直接去建 `AudioRecord` → `SecurityException` → 被 catch 住，
> 用户看到一句「启动本地识别失败：java.lang.SecurityException」，**而系统的授权框
> 永远不弹**。更前面还有一道拦路的：网页在 `V.caps.mic === false` 时只说一句
> 「麦克风权限没给」就 return 了——它以为"没给就是没给"，但权限是 App 自己要来的。
> 两处一起修：原生补 `requestMic` 桥方法 + `listenLocal` 自己申请并在授权回调里
> **接着把这次收音跑起来**（用户点的是"开始说"，不是"申请权限"，让他点完允许还要
> 再点一次是多余的一步）；网页那边把"干挡住"改成去要权限。
> 断言里守住了三条：没权限时**绝不开麦**、授权回来接着听、拒绝时不留一个
> 永远在等重试的状态。这个 bug 只在"系统没有识别服务"的机器上出现——
> 也就是他这台。
>
> **6. 对话历史：结束 → 整段分析 → 存 → 点小图标看 → 删。** 分析本来就是有的
> （结束页那段复盘），这次补的是"存得住、找得到、删得掉"。三个决定值得记下来：
>   · **存整局，不只存分析。** 分析是模型的话，逐轮记录才是他自己的话；
>     隔一个月再看，「我那天到底怎么说的」比「模型说我怎样」有用得多。
>   · **分析失败也要存。** 没密钥、没网、模型抽风都会让分析生成不出来，
>     但那一局是真发生过的：记录先落盘（分析留空），历史详情里给一个
>     「现在生成分析」随时补。反过来，为了"分析没成功"丢掉整局才是真可惜。
>   · **按 sid 覆盖，不按时间堆。** 每局开局就定一个 id，重试生成分析是往
>     同一条记录上补，不会在历史里堆出第二条（这类"看着一条、其实两条"的毛病
>     在这个项目里出现过）。
> 删除是**两步确认**（点删除 → 那一行就地变成"确定删除／取消"），不用
> `window.confirm`：记录本地和备份各一份，删掉就真没了。只留最近 **40 局**，
> 超出的丢最旧的，而且这件事在界面上明说，不偷偷丢。
>
> **7. 新增 `tools/check_talk_history.js`（65 项），并登记进 `run_checks.py`。**
> 覆盖：路由四种组合的顺序、模型没解开时不静默开麦、松手后不许自动开麦、
> 麦克风权限（要权限 / 授权后接着听 / 拒绝 / 旧版桥没有这个方法）、
> 失败时说出原因、诊断四行、存的东西全不全（含"没把给模型的 messages 一起存进去"）、
> 重开一局不会并成一条、列表/详情/补生成分析/两步删除/清空、老存档没有
> `talks` 键也不崩。**我自己的断言错了两条**：验证"准备完成后自动开麦"时
> 先调用再清日志，读到的是"没开麦"；读 `#turnHint` 的 DOM 时那一页根本没渲染
> 对话框（元素不存在，读出来永远是空串）——两条都是断言错，不是代码错。
> 另外改了一处措辞：诊断里"没有原生桥"和"这版桥里没有这个方法"原来混成一句，
> 会让人往错方向查。
>
> **7.5 截图脚本自己骗了我一次，值得记。** 拍出来的四张图全是「每日计划」的闸门
> 浮层，而我以为在拍对话页——**断言查的是 `#view` 的 DOM（对的），截图拍的是屏幕
> （被浮层盖住的）**，两者都不报错，脚本一路绿灯、图全错。原因是项目里每个截图
> 脚本都会先 `document.querySelectorAll('.sheet,.gate').forEach(remove)`，
> 我这次漏了那一行。现在每拍一张之前都清一次，而且每一步都先等"屏幕上真的是
> 那一页"再拍（等条件而不是等毫秒）。
>
> **8. APK 2.30（versionCode 32）。** 权限仍是 7 个（没有因为识别而新增权限，
> `RECORD_AUDIO` 早就有了）。体积 14 MB → **48 MB**。
> 回归：`run_checks.py` **25 项全绿**（含新加的 check_talk_history），audit 0 错。
> 顺带记一个编译错：`new Thread(this::listenLocalImpl)` 写在 Bridge 内部类里，
> `this` 指的是 Bridge 而不是 MainActivity，javac 只报「找不到符号 listenLocalImpl()」，
> 从报错看不出是"类写错了一层"。改成方法放在 MainActivity 上、两边都直接调。
>
> ***我验证不了什么（必须说清楚）***：这台电脑上没有安卓手机，
> 所以「Vosk 在他手机上到底能不能加载、识别准不准、第一次解包要多少秒」
> **我没有实测过**。我验掉的是：包里的四样东西齐全（逐字节/逐条目查过）、
> 网页这一侧的路由与降级（48 项断言）、以及原生代码里所有失败路径都兜住了。
> 真正只能靠装上去看的：native 库在他那个 ROM 上会不会被拦、
> 65 MB 解包的实际耗时、以及离线小模型对口语的识别率（官方说它是"small"，
> 长句子和口音会明显更差——所以有系统识别服务时仍然优先用系统的）。
> 万一哪一步失败，界面上会直接写出是哪一步、原话是什么，不会再出现
> 「没有可用的识别引擎」这种说不清原因的一句话。
>
> **v0.10.4（2.29）变更（已实现）：卡住时给提示；她的话不再被一行报错盖住**
>
> 用户的两句话：「我对话的时候，有时候完全不知道怎么回，能不能给提示呢？
> 就在旁边，点击可以查看提示。我感觉我完全不会说话了。」
> 「他的对话还会加上一个前缀，叫做返回的不是 json。这个前缀删掉，直接输出对话就行。」
>
> **1. 对话里不再出现「返回的不是 JSON」。** 那句话原来是被当成**报错**插进
> 对话区的（`#herSlot` 里的 `.v-err`），于是她想说的那句人话就藏在红框后面。
> 现在对话这一路（只有对话）允许模型**直接说人话**：解析不出 JSON 就把原文当她的话，
> 缺的字段（语气/内心/打分）补成"明说了没有"，卡片上说明原因，你照样接着回。
> 体检/复盘/出题仍然必须给 JSON——它们要的是结构化结果，那是对的。
>
> **2. 接话提示（用户要的核心功能）。** 「提示」按钮就在输入框**左边**
> （放右边会被浮标盖住，见下面第 4 条）。点开给四样东西：方向、做法、
> 一个句式骨架、最容易犯的错。分两层：
>   · **第一层是本地规则，不调模型、不花钱、断网也能用。** 这不是省成本，
>     是功能成立的前提：他卡住的那一刻最需要帮助，那时如果要等模型三秒、
>     或者因为没密钥而不可用，这个功能就等于不存在。断言里专门守着这条
>     （「打开提示一次模型调用都没有」）。
>   · **第二层是「给我一句能照着说的」**，点了才调一次模型（实测 0.6–1.9 秒），
>     给一句不超过 30 字的口语示范，并且提醒"先自己说一句""填进去改一改再说"——
>     照着念练不到东西。方向挑选按她那句话的**形状**猜：情绪词 → 先接情绪；
>     句末问号/吗/呢 → 先正面答再递回去；一两个字 → 该你多给一点；
>     玩笑自嘲 → 顺着玩；长段 → 先接住重点。界面上写明这是猜的、不是标准答案。
>
> **3. 方向挑选的两个真坑（都是我自己踩的）。** 第一版把「要不要/是不是」
> 也算成疑问词，于是「我最近在**要不要**换个工作」这种陈述被判成"她在问你"，
> 方向直接指错；改成**强信号**（句末问号/吗/呢，多长都算问句）和
> **弱信号**（疑问词只在短句里算）。另外「情绪」要排在「问句」前面：
> 「你是不是觉得我很烦？」既是问句又带情绪，而先接情绪永远是对的。
>
> **4. 顺手修的一个布局问题。** 提示按钮原来放在输入框右边，而那里正好是
> 「♪声音」浮标的位置（bottom:122px，占住 122–160px 那一段）——
> 截图里按钮被盖住了一半。按钮挪到左边，并把页面底部预留从 122px 加到 170px
> （原来那个 122 是按「卡住了」浮标算的，比声音浮标低）。
>
> **5. 真要一句示范时踩到的两件事**（都是真接口上打出来的）：
>   · 这一步**不能带 `response_format`**：提示词要"只输出那一句话"，
>     而网关又拿着 `{"type":"json_object"}` 当 schema 要求它回 JSON，
>     两个要求互相矛盾——实测模型回了个 JSON 对象（于是显示成空白）。
>     所以给 llmCall 加了 `noFormat` 选项，第一次就不带。
>     顺带发现：去掉它之后同一条请求从 9–12 秒降到 0.6–1.9 秒，
>     因为模型不再为那个矛盾反复推理。
>   · 额度 600 不够：这个模型的推理很凶（实测几百到两千多字），
>     推理吃完就只剩正文空。示范那条提到 1600。
>   · 万一它还是回了 JSON（见过 `{"suggestion":"…"}`），
>     就把对象里第一个字符串捞出来当示范，而不是显示空白。
>
> **6. 新增 `tools/check_talk_hint.js`（30 项）**，并把它**登记进
> `run_checks.py`**——第一版漏登记了，等于那一块没查（这个脚本存在的意义就是
> 别漏）。断言里最要紧的三条：打开提示不调模型；不同形状的话给不同方向
> （防"写死一个方向"）；对话区里**一个字的「返回的不是 JSON」都不许出现**。
> 另外还改了 `check_voice_talk` 之外的两处测试自身的错（把"数我自己的算术"
> 写成固定条数、查文案而不是查语义）。
>
> **7. APK 2.29（versionCode 31）。** 权限仍是 7 个。
> 回归：`run_checks.py` 24 项全绿，audit 0 错。
> 真接口上验过：对话一轮、12 轮连打、四种模块形状、以及新提示的四句话
> （示范分别是「真的啊，改来改去最磨人了」「你这嗯得我心里没底啊」这类
> 可以照着说的一句）。
>
> **v0.10.3（2.28）变更（已实现）：查清「模型返回空内容」到底是什么原因**
>
> 用户的原话：「这个 AI 语音出了这么多轮错误，究竟是为什么？你要告诉我。
> 究竟是什么原因，然后你自己要试一下，是否能成功。」他还特别提醒：
> 「不要自己闷头改呀，得多查阅资料。」
>
> **结论先说：不是他的模型名写错了，也不是这个 App 的提示词写坏了。**
> 是模型服务在 **JSON 模式下的偶发空返回**——官方文档自己承认的。
> 旧版报错让他去「检查设置里的模型名」，方向是错的，所以他怎么改都不会好。
>
> **1. 先取证：我这边测的是不是他那边？** 他贴的那段返回里有 `logprobs`
> 而我们的请求根本没要过它，这就有疑问。于是把原始返回**原样**打出来比：
> id 是 UUID（他的是 `c672c520-…`，我的是 `017024c6-…`）、字段顺序完全一致
> （`id, object, created, model, choices[0].index, message{role,content,
> reasoning_content}, logprobs`）、同样带 `logprobs`。**三样全对得上，
> 所以确实是同一个服务**，我这里的复现是有效的。
>
> **2. 再把每一种调用形状都打一遍**（不是只打一次）：
> 4 种模块形状（对话一轮 / 复盘打分 / 表达体检 / 复盘出题+揭晓）× 2 个模型、
> 连打 12 轮真实对话、按输入长度分 5 档（2k–48k 字，最大 prompt 34401 tokens）、
> 畸形参数（max_tokens 1/2/5/10/30/0/-1/"3000"/null）、
> 以及 `response_format` 带与不带的 A/B 各 12 次——
> **累计 110 多次真实调用，全部返回了可用内容**，那个「一个空格 + finish=stop」
> 的签名一次都没复现出来。这本身就是结论：它是偶发的，不是确定性的。
>
> **3. 顺手量出来的几件真事**（都在这台机器上实测）：
>   · 这个模型**推理很凶**：4000 额度的请求里推理写了 3305–8620 字，
>     实测 completion 最多 2923 tokens。所以「推理吃光额度 → content 空 +
>     finish_reason=length」是**另一条**真实存在的路（app 早就会翻倍重试）。
>   · `response_format` 传下去之后，网关会把 `{"type":"json_object"}` 当**schema**
>     交给模型——在推理过程里能看到它在琢磨「输出必须符合 schema
>     {"type":"json_object"}」。提示词没写清字段时，模型就会把这个 schema
>     回显成答案（实测复现过 `{"type":"json_object"}`）。App 的 10 处提示词
>     都写了字段形状（我逐个审过），所以这条在正式流程里不触发。
>   · 网关严格实现了官方那条规则：提示词里没出现 "json" 这个词，
>     带 `response_format` 就 **400**（`Prompt must contain the word 'json'`）。
>   · **`deepseek-chat` 在这个端点上并不存在**：端点只报 `deepseek-flash` 和
>     `deepseek-v4-pro`。发 `deepseek-chat` 会被映射成 flash（1.0 秒返回、
>     推理 0 字）。所以「换成 deepseek-chat 会更快」这句话在这台服务上不成立
>     ——它们其实是同一个模型。
>
> **4. 查文档**（用户要求的）：DeepSeek 的 JSON Output 文档写着
> 「the API may occasionally return empty content」，并且说「我们正在优化这个问题」
> 「可以试着改提示词来缓解」。也就是说这条**官方承认**。
> 文档还要求：提示词里必须出现 "json"、最好给一个格式样例、max_tokens 要给足
> ——这三条 App 都满足。
>
> **5. 于是改的是"怎么处理"而不是"猜原因"**：
>   · **空内容自动重试一次**，而且那一次**换个形状**（去掉 `response_format`），
>     因为失败最可能就是 JSON 模式这条路的问题；两次都空才报错。
>     只重试一次：第二、三次会是一模一样的请求，只是多花他的钱。
>   · 回显 schema（`{"type":"json_object"}`）也算"没给内容"，同样重试——
>     否则它是一份**合法 JSON**，会一路被当成"模型答非所问"，报错指不到原因。
>   · **删掉那句「检查一下设置里的模型名」**，换成真正能用的信息：
>     这是官方文档承认的偶发情况 + 再点一次通常就好 + 一行可以拿去问我的诊断
>     （模型 · 端点 · finish_reason · prompt/输出 token 数 · 额度 · 推理字数 ·
>     第二次去掉了 response_format）+ 服务端原文。
>   · 401/403/400 这类错误**不重试**（重试只是白花钱），这条也加了断言守着。
>
> **6. 断言补在 `check_reported_bugs.js` 第三节**（这一节就是被这件事逼出来的）：
> 空一次→第二次成功必须**静默成功**；两次都空必须报出 finish_reason/端点/额度；
> 不许再出现「检查模型名」那句话；截断那条路不能被改坏；401 不重试。
> 每条都用假的原生桥造返回，不花真钱。
>
> **7. APK 2.28（versionCode 30）。** 权限仍是 7 个。
> 回归：`run_checks.py` 23 项全绿，audit 0 错（1 个提示是这条 PLAN 版本号，
> 写完这段就一致了）。真机侧仍然只能用真密钥验：这四项我在电脑上都跑通了
> （对话一轮、开一局、12 轮连打、48k 字长输入）。
>
> **v0.10.2（2.27）变更（已实现）：删复盘、重做雨、放自己的音乐、查识别引擎**
>
> 用户提了四件事，其中第三件把一条**从 2.22 起就存在、一直没被发现的 bug** 挖了出来。
>
> **1. 删掉「今日复盘」。** 代码、`state.reviews`、`renderToday` 里那张卡、
> 相关样式（`.review-card` / `.why-block` / `.review-past`）全部清掉，
> 并确认「换人称」那条**两边都有的边界**已经在微课 `p33.warn` 里（619 字，
> Giovanetti 2019 与 Kross & Ayduk 2009 都在）——删功能没有把知识删掉。
> `check_read_flow.js` 从「测它有」改成「测它删干净了」（函数、状态、样式、界面文字
> 四处都查），`check_today_ui.js` 那条安全边界断言改成打到 p33 那一课上：
> 它是"功能本身可能有害"的那一类，不能因为功能删了就让文字消失。
>
> **2. 雨重做（用户：「有点吵，而且难听」）。** 他说得对，原因也很具体：
> 旧的那条雨是**一段连续粉噪**低通到 1500 赫兹再叠一层棕噪，听感是一片「嘶——」，
> 而它的说明里写着「中高频有细密的颗粒」——**那句话在旧实现里是假的**。
> 现在改成颗粒合成：一层很闷的底噪 + 按泊松过程丢出的雨滴（每滴是带通滤过的
> 噪声短促爆发乘指数衰减包络），预先算成 16 秒缓冲再循环，不需要定时器。
> 另加一条「大雨」。断言从"参数对得上"升级成可量的听感代理：包络波动系数
> （0.52 vs 连续粉噪 0.25）、峭度（6.5 vs 2.9）、高频占比（0.11 vs 粉噪 0.18，
> 也就是不再是一整片嘶）、不削顶、循环段够长。顺带纠正自己两个错判据：
> 数"比中位数高 3 倍的峰"在雨大时会失效（中位数被顶上去，于是"大雨没有颗粒"），
> 以及**按最响那一瞬间归一化会把颗粒压平**——改成 99.5 分位归一 + 只作用于
> 0.5 以上的软拐点。
>
> **3. 「我自己的音乐」。** 用户说「自用、不商用，所以选有版权的也行」。
> 他的判断我照做，但**打包别人的商业唱片**这个决定我不替他做：那等于把来源不明的
> 音频塞进一个他要求「卸载必须干净」的 App 里。所以做的是更好的那条路——
> 放他自己手机里的音乐：原生 `MediaPlayer` + `ACTION_OPEN_DOCUMENT` + 持久化
> URI 授权，选一次就记住（重启还在），卸载即全部消失、我们不留任何拷贝。
> 设置里新增「我自己的音乐」一块，并附一段「为什么不内置几首歌」的说明
> （想内置也行：他给文件，或者用 CC0／公有领域录音，我随时能打进去）。
> 播放时她说话会自动压低（和合成音轨同一条规则）。
>
> **4. 「这台手机没有可用的语音识别引擎」——真因是我们自己的 bug。**
> `probeNative()` 写好了、**却没有任何一处调用它**。于是 `V.caps` 一直是全 false：
> 在真机上 `V.caps.asr=false` → 一律显示「没有可用的语音识别引擎」
> （**用户报的就是这个**，他手机很可能是好的）、`V.caps.tts=false` → 合成退到
> Android WebView 根本没有的 `speechSynthesis`、`V.caps.mic=false` → 还会多说
> 一句"没给权限"。已修：载入时探一次 + 原生在 TTS 就绪后推一次 `__onCaps`
> （TTS 是异步就绪的，只探一次会永远拿到 false）+ 要说话/要出声前
> `ensureCaps()` 再确认一次。另外补了两处真问题：manifest 的 `<queries>` 缺
> `android.speech.RecognitionService`（Android 11 起少了它，
> `isRecognitionAvailable()` 在**装了引擎的手机上也返回 false**，症状和"没装"
> 一模一样）；以及给"只有识别界面、没有识别服务"的 ROM 加一条退路
> （`listenViaActivity`）。设置里新增「查一下到底缺什么」：把识别服务／识别界面／
> 引擎来自哪个应用分开报出来，并给一条能立刻走的下一步。
>
> **5. 自检另外挖出三处「看着有、其实没有」**：`fogPassDaily()`（脑雾通道的
> "今天记为未达标"）**没有任何按钮指向它**，于是那个出口点了只能做恢复练习、
> 永远记不下"今天我认了"——已在 `fogHome()` 里接上（只在从今天页/闸门进来时出现，
> 从卡片中途点「缓一下」进来不会顺手判未达标）；`setCardsView` 与 `testVoice`
> 是真的没人用的旧壳，删掉。为此把 audit 的"死函数"检查从 WARN 提到 ERR，
> 并改成**数调用点而不是数提及次数**——`window.__onCaps = probeNative` 这种写法
> 会让"提及"骗过检查，而它其实一次都没被调用。检查本身验过：插一个空函数进去它会红。
>
> **6. 补上一条构建漏洞（和 index.html 是同一个坑）。** 原生源码其实有**两份**：
> 仓库里的 `android/` 和真正被编译的 `E:\android-build\app`。我在仓库里改完
> `MainActivity` 跑构建，脚本一路绿灯打印「构建完成」，而打出来的 APK 里那几行
> 根本不存在——是 `test_bridge.js` 报"网页调用的方法在 Bridge 里找不到"才发现的。
> 已改成构建时自动同步 `src`/`res`/`AndroidManifest.xml` 并逐字节核对，
> "两份拷贝会漂移"从根上没有了；`test_bridge.js` 的默认读取路径也改回仓库那份。
>
> **7. 新增 `tools/check_music_asr.js`（30 项）与 `tools/run_checks.py`。**
> 前者用「把原生桥整段换成假对象」的办法，让**只能在真机上跑的逻辑**在电脑上也能验：
> 网页有没有真的调用它、参数对不对、原生状态回来之后界面有没有如实反映。
> 剩下真正只能靠手机的只有两件（MediaPlayer 能不能出声、识别服务在不在），
> 这两件在交付说明里如实写出来，不用"应该没问题"糊过去。
> 后者把二十多个检查脚本一次跑完并给总账——检查多到会漏，漏一次等于没查。
>
> **8. APK 2.27（versionCode 29）。** 权限仍是 7 个（音乐功能没有多要权限）。
> 回归：`run_checks.py` 22 项全绿 + `check_settings_ui` 单独跑 + 离线 39 项，
> `audit_project.js` 0 错。
>
> **v0.6 → v0.7 变更（已实现）**
>
> 用户要求：每日计划（量少、可加练、有提醒、达标制），并且「打游戏之前先完成」，
> 跳过一次可以、第二次不能跳。还提到可以用 root。
>
> 1. **每日计划**：默认 4 张卡片 + 1 条微课就达标，量刻意小。加练无上限、不计入
>    达标线、无提醒（否则会变成第二个必须完成的指标，有断言守住）。
> 2. **Never Miss Twice**：第一次打开可跳过，第二次没有跳过按钮。依据是
>    Lally 等（2010）——错过一次对习惯养成没有实质影响，但累积的错过会显著降低
>    最终自动化水平；真正致命的是「反正已经破了」的连锁反应（Marlatt & Gordon）。
>    跳过一次不打断连续性。
> 3. **脑雾通道**：闸门里也留着「卡住了」。走了记为未达标但不连续打断两天——
>    与 App 既有的「降负荷而不是忍着做完」一致。
> 4. **每日提醒**：只在没达标时响（天天响的提醒会被无意识忽略）。
>    `setAndAllowWhileIdle` + 自排下一天 + `BOOT_COMPLETED` 重建，
>    不用「闹钟和提醒」特殊权限。
> 5. **护城河（打游戏前先完成）**：无障碍服务检测目标应用 → `?gate=1` 拉起闸门。
>    证据：Grüning 等（2023，PNAS，n=280）摩擦 + 明确放弃选项让打开次数降 57%，
>    而且预注册实验发现**单纯提示语无效**。所以闸门是「要动手才能过」。
>    **分工：网页算是否达标，原生只执行**——判定逻辑只有一份，不会漂移。
> 6. **不用 root**（用户提议过）。提醒/检测/闸门全部普通权限可做，且无障碍服务
>    配置成 `canRetrieveWindowContent=false`，权限范围**比 root 更小**；
>    root 还会带来系统升级失效、换机失效、攻击面变大。README 里有对照表。
> 7. APK 2.4（versionCode 6），权限 4 个：INTERNET / RECORD_AUDIO /
>    POST_NOTIFICATIONS / RECEIVE_BOOT_COMPLETED。测试新增 18 项（`test_daily.js`）。
>
> **v0.5 → v0.6 变更（已实现）**
>
> 用户的原始要求：「需要可以稍微吃点亏，但是不能一直吃亏，就不能让我成为情绪的垃圾桶，
> 但是如果一次两次其实是可以的，相当于是做一个让步、一种包容。」并要求覆盖
> 朋友抱怨 / 家人日常 / 公司职位 / 伴侣四个场景。
>
> 1. **新增两个误读类型**：`过度让步`（该说的时候咽了）和 `过度计较`（小事升成原则问题），
>    直接接入既有偏差画像——它们正好是用户说的那一对失败模式，不用另建机制。
> 2. **10 张「让 vs 守」卡片**，覆盖朋友（情绪垃圾桶、临时放鸽子）、家人（亲戚饭局、
>    每天转养生文章）、公司职位（领导抢功、下班前第三次加活、下属推锅、平级反复求助）、
>    伴侣（发火后没道歉、同一件事吵到第六次）。**让/守答案刻意 7:6 对半**——
>    只守会教出斤斤计较，只让会教出老好人。测试里有一组断言守住这个平衡。
> 3. **统一判据（三个问题）**：这是第一次还是模式、有没有实际损失、对方的诉求是
>    关系性的还是控制性的。三条都指向「偶发 + 无损失」才让。
> 4. **两课**：`p18` 让步什么时候是加分项（Rusbult 等 1991 的让步定义；
>    Impett 等 2010 的接近型 vs 回避型动机）；`p19` 「情绪垃圾桶」是怎么形成的
>    （Rose 2002 共同反刍，n=608；Rose 等 2007 双向关系）。
> 5. **两个现实任务**：`a_concede_once`（让一次，不带情绪）和 `a_hold_once`（不让一次，
>    把次数说出来）。
> 6. 内容规模：卡片 44→54，微课 25→27，现实任务 5→7。APK 2.3（versionCode 5）。
>
> **v0.4 → v0.5 变更（已实现）**
>
> 1. **新增「阶段评估」模块**（第 7 个 tab，离线可用）。6 关，每关只声明「你能做什么」，
>    不声称「你是什么人」。通过条件全部落在可验证的行为上：准确率、误读类型的变化方向、
>    现实任务的执行率。**第 2 关的纵向比对（最初 1/3 vs 最近 1/3 的误读占比）是整个 App 里
>    唯一真正的进步指标**——命中率受题目难度影响，偏向不受。
> 2. **现实任务 + 对账**：每关配一个现实里的小任务，三个选项（做了 / 没做 / 没遇到场合）。
>    第三个选项是信息不是台阶——它区分「触发条件太窄」和「动作不够自动化」，
>    指向计划的前半句还是后半句。执行率比答对率诚实，因为它发生在屏幕外面。
> 3. **身份陈述**：用户提出"让 App 告诉我是个好男人，我会潜移默化往那个方向长"。
>    这个直觉抓到了对的东西（身份层面 > 行为层面），但**被告知这个版本有反效果记录**：
>    写自己正面道德特质的人随后捐钱最少（Sachdeva 等 2009），回想善行的人随后作弊更多
>    （Jordan 等），机制叫道德许可（Merritt、Effron & Monin 2010）。
>    所以改成：App 只摆出你自己做出的事实，由你写成条件式（`在…的时候，我会…`）——
>    结论式会被拦下，因为「我是个好人」是可以放下的事实，「有人倒苦水时我会先接住」
>    是还在生效的标准。
> 4. **天花板声明**（`boundary`）：常驻入口，不需通关即可看。分栏写明测到了什么、
>    测不到什么（累的时候耐不耐烦、没人看见时认不认错、无聊没回报时回不回家），
>    并明确说这个 App 不会回答「你是不是好男人」。**这一节不要删——只有进度条没有天花板的版本更糟。**
> 5. **测试新增 28 项**（`tools/test_stages.js`），其中一条是不变式：
>    「答案多但偏向没变 → 不通过」，守住关卡不能被刷题量刷过去。
> 6. **修掉一个静默失败模式**：`boot()` 原来按白名单逐个列内容键，加 `stages.json` 后
>    忘了加，接口正常返回但 `CONTENT.stages` 一直是 null、页面显示「内容还没加载」而无任何报错。
>    改成自动吸收服务端返回的所有键。APK 版本 2.2（versionCode 4）。
>
> **v0.3 → v0.4 变更（已实现）**
>
> 这一轮的出发点是"多查资料再改进"，所以每条改动都对应一个能查到的研究结论，
> 而不是设计偏好。详细论证写在 README 的「这些机制是从哪来的」一节。
>
> 1. **逐选项诊断**（`why` + `err`，44×4=176 个选项全部覆盖）。frame-of-reference
>    训练的有效机制是比对判断差异、解释差异来源，不是公布答案（Sulsky & Day 1992；
>    Loignon 等 2017；机制研究 Baret & de Kock 2018）。
> 2. **偏差画像**：把"命中率"和"往哪边歪"分开统计，六类误读互相可区分
>    （West & Kenny 2011 的 truth and bias model）。样本 <6 次时明确说"说明不了什么"。
> 3. **if-then 行动预案**：用户自己写、正面表述、可复看。证据最扎实的一个机制——
>    d=.65（Gollwitzer & Sheeran 2006，94 个检验），2024 年更新的 642 个检验里
>    条件式 d=.43 优于日程式 d=.29，且复看过的人效果更大。禁止式表述会被拦下
>    （否定框架有逆火风险）。
> 4. **追问计数器**：语音陪练每局报告"你提了几个问题、几个是追问"。
>    提问多的人被更喜欢、追问尤其有效且收益累积（Huang 等 2017；Yeomans 等 2019），
>    而人们普遍预料不到这一点。
> 5. **内容扩充 36→44 张卡**：补了整类"友善≠好感"（原来一张都没有，而这正是最初
>    提出的问题；La France 等 2009 平均 r≈.32，Bendixen 2014 有 88% 这个数字）
>    和"事务化"（未被请求的建议会让对方反应更差，Bolger & Amarel 2007，N=257）。
> 6. **微课 20→25 条**：追问、if-then、友善误读、准确率≠偏差、未被请求的建议。
>    每条 `warn` 字段写清证据边界。
> 7. **测试**：`test_plan.js` 18 项、`test_ask.js` 12 项、`verify_offline.js` 28 项。
>    三个测试都各自抓到过真缺陷（见 README「踩过的坑」）。
> 8. **构建加固**：`build_apk.py` 自己同步网页内容并逐字节校验——之前会用旧 HTML
>    打包而日志一切正常。APK 版本 2.1（versionCode 3）。
>
> **已知没做的**：21 张卡因 `ok` 字段偏松而只有一个干扰项，区分度偏弱；偏差画像和
> 追问计数都是启发式的，不是心理测量。原因写在 README「已知限制」。
>
> **v0.2 → v0.3 变更（已实现）**
> 1. 更名 **读人 → 认知训练**，图标换成手写的卡通角色 SVG
> 2. 内容从"恋爱为主"扩为**五领域**：恋爱 17 / 职场 7 / 朋友 4 / 家人 4 / 泛社交 4（共 36 张卡），卡片页可按领域筛选
> 3. 新增**脑雾恢复**模块（常驻按钮 + 四种分级手段 + 记录），设计原则是"降负荷"而不是"缓解后继续"
> 4. 新增**操控识解**系列（8 条微课）：讲透 PUA 类手法的机制、给出长期代价、重点教识别被用在自己身上、并把"思路"逐条翻译成可持续做法
> 5. 微课分三系列：关系心理学 10 / 操控识解 8 / 证据纠错 2
> 6. 研究补充：脑雾的急性干预证据、微休息元分析、操控策略的长期代价
>
> **v0.1 → v0.2 变更**
> 1. 技术栈改为 **Python**（本机 Python 3.12.10 + pip 25 + openai SDK 3.3.1 已装，零额外安装）
> 2. 明确 **双功能**：本地学习模式（离线、零成本）+ AI 陪练模式（联网），共享同一套进度
> 3. 新增**语境校准引擎**——专门解决你说的"同一句话在不同场合能不能说"
> 4. 新增**表达五维体检**——给"油腻"下一个可计算的定义，做成趣味玩法
> 5. 新增**关系心理学课程库**，每条标注证据等级（含两个"流行但被证伪"的纠错卡）
> 6. 设计依据从"我觉得好"改成"研究支持 + 明确告知边界"

---

## 一、定位

**不是替你会聊天，而是让你看得懂、说得准。**

市面上的中文产品（心动恋聊、恋爱军师、AI嘴替、昧聊键盘）几乎都是"AI 嘴替"：截图进去、话术出来。用户吐槽集中在"机械""复制粘贴回微信太慢，情绪早没了""每个字都是 AI 替我说，还不如直接找个机器人"。

**我们做教练，不做嘴替**：同一句话，先让你判断，再揭晓解析，最后才给可选回应。你越用越不需要它。

---

## 二、调研结论（v0.2 新增 6 条，共 13 条）

### 关于"该教什么"

**1. ⚠️ 最重要的一条：不能做成"女性密码本"。**
心理学上有个概念叫 **expected mindreading（期待被读心）**——"对方应该不用我说就懂"。研究显示它是**功能不良的信念**，损害关系满意度（临床样本 r = −.28），触发"他不懂我 = 他不爱我"的负面归因，进而导致冷暴力和疏远。操控类策略（Dark Triad）与情感亲密度、满意度长期负相关，代价双方共付。

**2. ⚠️ 而且"读不懂"的一部分其实是"读太多"。**
**Error Management Theory（Haselton & Buss）**：男性在演化上系统性地**过度推断**女性的性兴趣。所以训练要有相当比重花在"这里没有潜台词"上——**过度解读比读不懂更伤人**。

**3. 你讨厌的"油腻"，恰好是被研究反复证明效果最差的那一类。**
Bale / Morrison / Caryl (2006) 按效果排序开场表达：**展示品格/性格 > 文化阅历 > 杂类 > 幽默 > 夸奖 > 性暗示调侃**。
Kleinke 的类型学 + Senko & Fyffe (2010)：**cute/flippant（罐头式俏皮话）被评为最不恰当、最无效**，使用者被认为"更滑稽但**更不可信、更不聪明**"，女性更不愿意与他交谈或考虑长期关系。长期伴侣她们看重 **可信度和智力，超过幽默**。
Weber / Goodboy / Cayanus (2010)：第三方引荐最有效最恰当，直接自我介绍也恰当；**直接恭维、试图幽默、罐头俏皮话三者被评同等不恰当且无效**。
关键区分（Kaufman 综述）：**罐头式俏皮话 ≠ 贴合语境的机智**。后者因为显得聪明、有创造力，"can be incredibly sexy"。
→ 结论：**"油腻"的本质是"罐头"——与你此刻这个场景无关的预制表达**。所以教材不提供任何可背诵的句子，只训练"就着当下情境说话"的能力。

**4. ⚠️ 纯背脚本的迁移性很差，这是有证据的。**
社交技能训练（SST）的综述（Curran 1977 等）反复发现：效果**集中在自评量表中**，行为层面和真实约会频率的改善有限，6 个月随访后与对照组无差异；角色扮演表现与真实情境行为**不相关**。
但有一条明确的出路：**认知自我陈述（cognitive self-statement）训练向未训练情境的迁移，显著优于纯行为脚本训练**。
→ 产品含义（这不是价值观，是证据）：**教原理和判断框架，不教可背诵的话术**。每道题都要能回答"为什么"，并且要换场景考迁移。

**5. 微学习 + 间隔重复 + 检索练习，是被反复验证的留存机制。**
大规模 App 研究：加间隔重复卡片的一组次日留存 61.4% vs 对照 46.2%，30 日 14.5% vs 3.6%。「做选择」比「看内容」留存高得多。

**6. 游戏化是有条件的，排行榜会反噬。**
2 万条语言 App 评价分析：支持自主感/胜任感/归属感的设计留存更好；**内部指标（连续天数、熟练度）促进坚持，外部队列/排行榜导致压力、浅层学习和放弃**。

### 关于"读不懂"本身

**7. 言外之意是有教材的学科——语用学。**
Grice 的"会话含义"、Searle 的"间接言语行为"就是"潜台词"的学术名字。国内实证研究证明，**显性教学 + 推理训练**能显著提升理解言外之意的能力。

**8. Gottman 的"连接请求"（bids）是最实用的透镜。**
追踪 130 对新婚夫妻 6 年：**还在一起的，86% 会回应对方的连接请求；离婚的只有 33%**。且"忽略"比"吵架"伤害更大。
→ "她这句话什么意思"很大一部分答案是：**它在问"你还在吗、我重要吗"**，与字面内容无关。

**9. 情绪粒度可训练，且这是"情商低"的核心短板。**
干预实验证明情绪知识训练能显著提升情绪区分度。"命名情绪"本身有神经机制支撑——fMRI 研究显示说出感受的名字会降低杏仁核反应、激活右腹外侧前额叶。
→ 多数人只有"开心/不开心"两个词，所以做一套**情绪词汇图鉴**。

**10. "分场合"有现成骨架：社会渗透理论 + Knapp 十阶段。**
Altman & Taylor：自我披露有**深度、广度、频率**三维，且有**互惠效应**——你披露的深度应与对方匹配；**广度先于深度**。
Knapp 双阶梯十阶段：走近（发起→实验→强化→整合→结合）／走远（分化→限制→停滞→回避→终止）。大多关系停在"实验"阶段。
→ 这直接支撑你要的"分场合训练"：**同一句话，在"实验"阶段是油腻，在"强化"阶段是恰当。** 判定标准是可结构的，不是玄学。

### 关于"什么让人反感"

**11. "油腻/爹味"的机制被拆得很清楚。**
- 年轻人反感的不是"被指导"本身，而是**不平等的对话关系**（"我不要你觉得，我要我觉得"）
- **对话自恋（conversational narcissism）**：焦点始终在自己身上。识别指标很硬——**句中"我"的使用频率越高，关系越危险**
- 需要的是**情感共鸣和价值肯定**，而不是被上一课
- **越界**：强行用自己的评判体系评价对方
→ 这四条全部可以量化，下面变成可计算功能。

### 关于证据的诚实

**12. 五种爱的语言：核心假设未通过验证。**
Impett / Park / Muise (2023-24) 逐条检验：①"每人有一个主要爱的语言"——**被否**，五个都被评为同样重要，强迫选择得出的"主要语言"与量表最高分不一致；②"有五种"——**被否**，五项之间相关高达 .54–.75，不像五个独立维度，因子分析得出 3、4、5 因子结构都不匹配；③"说对方的语言能改善关系"——**未能复现**，多项研究（Bland & McQueen 2018；Bunt & Hazelwood 2017；Polk & Egbert 2013；Surijah 2020）发现匹配与不匹配无差别；Chopik et al. (2023) 发现**所有**爱的表达都能预测满意度。499 人预注册研究（Flicker et al., 2025）里**不到一半的人能识别出主要语言**。
→ 结论：改为教 **"均衡饮食"模型**——不是找她那一种，而是五种都给，别偏科。

**13. Gottman 的"90% 预测离婚"是样本内拟合，不是真实预测力。**
Heyman & Slep (2001)：把 528 人分两半，用一半建模、另一半验证——训练集准确率 90%，**验证集阳性预测值从 65% 掉到 29%**，按真实离婚率校正后约 **21%**。DeKay et al. (2002)：最终回归模型只基于 **15 个案例**，还含一个强影响离群值，模拟显示解释方差"仅略高于随机数据"。Lutz & Weinmann-Lutz (2006) 用瑞士和美国大样本得到 PPV 也只有 20–40%。
→ 但**描述性发现是稳的**：**四骑士（批评、蔑视、防御、筑墙）尤其是蔑视，与关系困扰和离婚的关联在多个独立纵向样本中都被复现**。失效的是"从关联跳跃到预测工具"这一步。
→ 结论：教四骑士 + 各自的解药，但明确写"**它不能预测，别拿它去诊断女朋友**"。

---

## 三、双功能架构

| | **学习模式**（离线，零成本） | **陪练模式**（AI，联网） |
|---|---|---|
| 内容 | 每日卡片、心理学课程、语境校准判断题、情绪词汇图鉴、错题回放 | 情景剧场、真实复盘教练、表达五维体检、自由角色陪练 |
| 依赖 | 纯本地 JSON，不联网不烧钱 | DeepSeek（你已有的 key，服务端调用） |
| 场景 | 通勤、睡前 3 分钟、没网 | 想深练、有具体困境时 |
| 进度 | ← 共享同一套熟练度与连续打卡 → | |

**关键：学习模式必须能独立完整使用。** AI 是加成，不是前提。

---

## 四、核心内容模型

### 4.1 三层解码（保留 v0.1）

| 层 | 问什么 | 例 |
|---|---|---|
| 字面层 | 她说了什么 | "没事" |
| 语境层 | 关系阶段 / 场合 / 时间 / 上一句 / 她的状态 | 你刚爽约，她等了 40 分钟 |
| 意图层 | 她想要什么 | 不是"没事"，是想被看见"我有事" |

**六种言外之意类型**：①面子保护型（委婉拒绝/不满）②连接发起型（bid）③确认需求型 ④边界/可靠性试探型 ⑤信息缺口型 ⑥**纯字面型（必须有，作为正确答案）**

**四种回应动作**：承接情绪 / **核实歧义**（"你是想我现在听你说，还是想一起想办法？"性价比最高）/ 回应需要 / 划清边界

**三条能力线**：察觉 Notice → 解读 Interpret → 回应 Respond

### 4.2 语境校准引擎 ⭐（对应你的"分场合"要求）

**三维输入** → **三档判定**

```
轴 1 · 关系阶段（Knapp 简化 6 档）
  陌生初见 → 认识试探 → 升温暧昧 → 在一起磨合 → 降温期 → 结束/边界

轴 2 · 场合
  公开 / 私下 · 线上 / 线下 · 有无第三方在场 · 她此刻忙/累/情绪低/轻松 · 工作场 / 社交场

轴 3 · 关系深度（社会渗透）
  你这次披露的深度，是否超过了她已经给你的深度？（越级 = 油腻的主要来源之一）
```

**输出不是对错，是三档 + 理由：**

| 判定 | 含义 |
|---|---|
| ✅ 能说 | 这句话在这个阶段+场合是恰当的 |
| ⚠️ 改说法才能说 | 意图没问题，说法越界了 → 给"降一级"的版本 |
| ❌ 别说 | 这份关系深度还接不住这句话，或者场合不对 |

**示例（这正是你要练的）：**
```
候选表达：「你今天这条裙子很好看，是想见我吗？」
场景 A：第一次约出来，刚坐下 20 分钟        → ❌ 别说（越级 + 罐头 + 性暗示三连）
场景 B：认识两个月、已经单独出来 5 次、她刚主动发消息问你到哪了
                                          → ⚠️ 后半句删掉可以说
场景 C：在一起半年，两人在开玩笑互怼        → ✅ 能说
→ 同一句话，三档判定完全不同。训练的不是句子，是"读房间"（read the room）的能力。
```

### 4.3 表达五维体检 ⭐（把"油腻"变成可计算的东西）

AI 陪练模式里的一个玩法：你把**打算说的话**写进去（或者粘贴聊天记录里你刚发的那句），出五维雷达图 + 可解释的扣分理由。

| 维度 | 什么意思 | 依据 |
|---|---|---|
| **罐头度** | 是预制模板，还是就着此刻这个场景说的？ | cute/flippant 被评最差；贴景机智最强 |
| **越级度** | 亲密/披露的深度超过关系阶段了吗？ | 社会渗透的互惠效应、广度先于深度 |
| **自恋度** | 句里"我"vs"你/我们"的比例 | 对话自恋：我字频率越高，关系越危险 |
| **评判度** | 在对她下判断、给未经请求的建议、说教？ | 爹味 = 居高临下的指导欲 |
| **性暗示度** | 研究里效果最差的档 | 效果排序：品格 > 阅历 > 机智 > 夸奖 > 性暗示 |

**同时给"升级版"参考顺序**（来自研究排序）：
`展示品格/性格 > 共同经历/文化 > 贴景机智 > 具体关心 > 泛泛夸奖 > 性暗示`

这个功能一举四得：有趣、有教育性、可解释、能立刻用在真实对话里。

### 4.4 关系心理学课程库（带证据等级）

每条都标 **证据等级**，这是"学习性"的一部分——你学到的东西也得知道它有多硬。

**A 级（证据强，可放心用）**
- **依恋类型**：Bartholomew 四类型（安全 / 焦虑 preoccupied / 回避 dismissing / 恐惧 fearful）＋ **earned secure**（依恋是可以变的，不是判决书）
- **Gottman 四骑士 + 解药**：批评→温柔开场（I-message）；蔑视→表达欣赏；防御→承担一点责任；筑墙→自我安抚+暂停
- **连接请求（bids）**：86% vs 33%
- **情绪粒度 + affect labeling**（命名即安抚）
- **Error Management Theory**：男性系统性过度推断性兴趣
- **社会渗透 + Knapp 阶段**：洋葱模型、广度先于深度、互惠效应

**B 级（框架有用，但预测力有限）**
- **Sternberg 爱情三角**：亲密 / 激情 / 承诺 → 8 种爱。适合当**描述语言**，不适合当尺子。
- **冲突风格与修复（repair）**：关系能不能长久，更多取决于吵完怎么修，而不是吵不吵

**C 级（流行但证据弱——做成"纠错卡"教）** ⚠️
- **五种爱的语言**：核心三条假设均未通过验证 → 教成 **"均衡饮食"**：别找她那一种，五种都得给
- **"四骑士能预测离婚 90%"** → 教成：四骑士是**风险信号**，不是**预测工具**，别拿去诊断对方

> 说明：把这些"流行但站不住"的东西**明确标注并讲清哪里站不住**，比避而不谈更有价值——你以后会到处看到它们，得知道该怎么看。

### 4.5 五个学习模块

1. **每日卡片**（主力，离线，3–5 分钟，2–4 张，SM-2 间隔 1/3/7/21 天）
2. **语境校准判断题**（4.2，三档判定，练"读房间"）
3. **情绪雷达 + 情绪词汇图鉴**（30 秒限时，8 个近义情绪词里选最贴的；每掌握一个词解锁图鉴卡，对应情绪粒度训练）
4. **翻车回放**（每周，错题换场景重考——考迁移不考记答案）
5. **心理学微课**（4.4，每条 90 秒读完 + 一道应用题）

### 4.6 四个陪练模块

1. **情景剧场**：多轮分支，AI 扮演她，每轮结束给你"你刚发出了什么信号"，最后做转折点复盘（哪一句是分水岭）
2. **真实复盘教练**：粘贴聊天 → 先出 2–3 题让你判断 → 揭晓分析（含"也可能是字面意思"的另一版）→ 才给可选回应
3. **表达五维体检**：4.3
4. **自由角色陪练**：选人格 + 关系阶段，自由对话，结束后给报告

### 4.7 成长系统

做：连续打卡 / 三条能力熟练度（察觉·解读·回应）/ 情绪词汇图鉴收集（30→80 词）/ 等级头衔（直男实习生 → 共情学徒 → 语境侦探 → 读人师）/ 每日 2–4 题（**宁可少不要多**）
不做：❌ 排行榜和对战 ❌ 话术库检索 ❌ 情感打分（"她 78 分想复合"这类伪科学）

---

## 五、技术方案（Python）

| 项 | 选择 | 理由 |
|---|---|---|
| 运行时 | **`py -3` → Python 3.12.10**（`C:\Users\Lenovo\AppData\Local\Programs\Python\Python312\python.exe`） | 已验证可用；本机另有 3.11 / 3.9 / 3.2 |
| 依赖 | **零额外安装**：标准库 `http.server` + 已装的 `openai 3.3.1` | 不需要 pip install，不会被网络和环境卡住 |
| 后端 | 单文件 `server.py`（ThreadingHTTPServer），提供静态文件 + `/api/*`（含 SSE 流式输出） | 一个命令启动 |
| 前端 | 原生 HTML/CSS/JS 单页，移动优先，无构建 | 改一行立刻生效，方便你自己加题 |
| 数据 | `data/cards.json`、`data/curriculum.json`、`data/phrases.json`；进度 `data/progress.json` + localStorage | 纯 JSON，你能手改、我能批量生成 |
| 模型 | `deepseek-v4-pro`（沿用 `deepseek_client.py` 的 BASE_URL 与调用方式） | 已有 key |
| 加密钥 | key 只读 `.env`，服务端调用，**永不发给浏览器** | 前端代码里搜不到 key |
| 访问 | 启动后打印 `http://192.168.x.x:8787`，手机连同一 WiFi 打开，「添加到主屏幕」 | 零安装，像原生 App |

**注意**：`python`（不带 3）在你的 PATH 里指向 `E:\MGL tools\python.exe`（Python 2.7.11），所以启动命令一律用 `py -3`。

**隐私**：题库全部离线。只有陪练模式联网，粘贴的聊天记录会发到 DeepSeek API。→ 提供**自动模糊化开关**（把称呼、手机号、地名替换掉）和不联网的纯离线模式。

**为什么不做微信小程序**：个人主体需要 AppID 审核、服务器域名要备案、还要 HTTPS，起步成本远超收益。用顺了再搬只是换层壳。

---

## 六、里程碑

| 阶段 | 交付 | 你能看到什么 |
|---|---|---|
| **M1** | `server.py` + 前端骨架 + 每日卡片 + 连续打卡 + 熟练度 + 离线进度 | 一个能每天真的用起来的东西，手机可访问 |
| **M2** | 语境校准判断题 + 表达五维体检（**你特别要的"分场合"**） | 练"这句话现在能不能说" |
| **M3** | 真实复盘教练（粘贴聊天，先答后揭） | 直接解决你的真实痛点，差异化核心 |
| **M4** | 心理学微课库 + 情绪雷达/词汇图鉴 | 底层能力 + 理论支撑 |
| **M5** | 情景剧场 + 自由角色陪练（LLM 多轮） | 趣味性主力 |
| **M6** | 打磨：PWA 安装、夜间模式、导出/分享 | 便携性收口 |

**建议顺序**：M1 → M3 → M2。先有个能每天用的，再上真实复盘（真痛点），再上分场合训练。

**内容产出**：我先写种子库给你审——**60 张卡片 / 40 道语境校准题 / 12 条心理学微课 / 20 个情绪词**，六大言外之意类型和三个关系阶段都覆盖。你审完我再写进 JSON。

---

## 七、我要守的规矩（写进代码，不只是写进文档）

1. 所有解析都给**置信度**和**另一种可能**，不用"她一定是……"的口气。
2. 每题必须有**纯字面型**选项参与，防止训练出过度解读。
3. 反馈给**原理 + 方向**，不给逐字模板；每道题都要能回答"为什么"。
4. 同一原理换场景再考（对抗"背答案"，研究显示认知框架迁移性最好）。
5. 模型系统提示硬编码红线：不生成套路/操控/冷暴力/欲擒故纵；信息不足时必须说"这句话信息不足，最好的做法是**问出来**"。
6. 标注证据等级，C 级内容明确告知"流行但未通过验证"。

---

## 八、需要你拍板的（我已按推荐值填好）

| # | 问题 | 我的默认 | 备选 |
|---|---|---|---|
| 1 | 场景范围 | **恋爱/约会为主线**，留扩展位给职场、朋友、家人 | 只做恋爱 / 泛社交混排 |
| 2 | 每日强度 | 3–5 分钟，2–4 张卡 | 10–15 分钟系统课程式 |
| 3 | 陪练是否默认开 | 默认开，但学习模式**离线也能完整用** | 默认关，手动开 |
| 4 | 语气尺度 | **不油腻、不糖、不爹味**；允许适当玩笑但必须贴景 | 更保守/更活泼 |
| 5 | 起点阶段 | 从**陌生初见 → 认识试探**开始（最容易翻车的区间） | 从你的实际处境开始 |
| 6 | 聊天记录模糊化 | 默认开（自动替换称呼/号码/地名） | 默认关 |
| 7 | 先做哪三个模块 | M1 → M3 → M2 | 你指定 |

---

## 参考来源

**表达效果与"油腻"**
- [Kaufman：开场白的认知心理学（含 Kleinke 类型学、Lewandowski 认知耗竭研究）](https://www.psychologytoday.com/us/blog/beautiful-minds/201201/the-cognitive-psychology-of-pick-up-lines)
- [Bale, Morrison & Caryl (2006)：开场白作为男性展示的效果排序](https://www.sciencedirect.com/science/article/abs/pii/S0191886906000426)
- [Senko & Fyffe (2010)：flippant 开场白与长期关系意愿](https://link.springer.com/article/10.1007/s10508-009-9584-5)
- [Weber, Goodboy & Cayanus (2010)：恰当与有效的开场白实验研究](https://www.tandfonline.com/doi/abs/10.1080/08824091003738016)
- [对话自恋与"我"字频率](https://www.psychologytoday.com/us/basics/narcissism)

**该教什么：迁移性与训练设计**
- [Curran (1977)：社交技能训练治疗异性社交焦虑的综述（迁移与随访缺陷）](https://psycnet.apa.org/record/1977-21767-001)
- [Twentyman & McFall (1975)：6 个月随访后行为收益消失](https://psycnet.apa.org/record/1976-20412-001)
- [在线情商活动提升听力理解（实验研究）](https://journals.ekb.eg/article_359295_abcbd22b143b41bc26d5a00a1e03a05f.pdf)
- [HearMe：同伴互助 App 中的主动倾听训练](https://www.tandfonline.com/doi/full/10.1080/10410236.2024.2360178)

**言外之意与情绪能力**
- [Grice 会话含义与 Searle 间接言语行为（中文教学综述）](https://mooc1.chaoxing.com/mooc-ans/ztnodedetailcontroller/visitnodedetail?courseId=201660742&knowledgeId=125195340)
- [言外之意的显性教学实证研究](https://apps.wanfangdata.com.cn/thesis/article:Y1473335)
- [情绪知识干预提升情绪区分度，Frontiers in Psychology 2021](https://www.frontiersin.org/articles/10.3389/fpsyg.2021.703757/full)
- [心理咨询师情绪识别训练及其一年后效应，PMC 2023](https://pmc.ncbi.nlm.nih.gov/articles/PMC10402901/)
- [模糊表情 + 反馈训练提升情绪分类准确率，ScienceDirect 2016](https://www.sciencedirect.com/science/article/abs/pii/S0306452216001603)

**关系心理学**
- [Gottman：连接请求与"转身回应"](https://www.gottman.com/blog/turn-towards-instead-of-away/)
- [四骑士与各自的解药](https://www.gottman.com/blog/the-four-horsemen-recognizing-criticism-contempt-defensiveness-and-stonewalling/)
- [Heyman & Slep (2001)：离婚预测的交叉验证失败](https://onlinelibrary.wiley.com/doi/10.1111/j.1741-3737.2001.00070.x)
- [DeKay, Greeno & Houck (2002)：对 Gottman 模型的统计批评（N=15）](https://onlinelibrary.wiley.com/doi/10.1111/j.1545-5300.2002.41312.x)
- [Impett, Park & Muise：五种爱的语言的科学检验](https://journals.sagepub.com/doi/10.1177/09637214231218099)
- [Flicker et al. (2025)：499 人预注册研究，不到一半人有可识别的主要爱的语言](https://onlinelibrary.wiley.com/doi/10.1111/jmft.12757)
- [Bartholomew 四依恋类型与 earned secure](https://journals.sagepub.com/doi/10.1177/0265407591084002)
- [社会渗透理论（Altman & Taylor）与 Knapp 十阶段阶梯模型](https://openstax.org/books/organizational-behavior/pages/1-introduction)

**学习机制与游戏化**
- [间隔重复卡片对移动学习留存的提升（PACARD，6.3 万用户）](https://neu2025.neu.edu.vn/Resources/Docs/SubDomain/HomePage/ThongBao/2018/2018_2/PACARD%20A%20New%20Interface%20to%20Increase%20Mobile%20Learning%20App%20Engagement,%20Distributed%20Through%20App%20Stores.pdf)
- [语言学习 App 的自我决定理论分析（2 万条评价）](https://www.academia.edu/145270356/)
- [游戏化的条件性效果与"识别—产出鸿沟"](https://ejournal.iainpalopo.ac.id/index.php/ideas/article/download/8711/5339/33254)

---

## v0.4 新增参考

**判断准确性可以训练（frame-of-reference）**
- [Sulsky & Day (1992)：FOR 训练同时提升评分准确性和分类准确性](https://doi.org/10.1037/0021-9010.77.3.344)
- [Loignon et al. (2017)：受训组评分方差中归因于被评人的比例接近翻倍](https://doi.org/10.1017/iop.2017.5)
- [Powell & Goffin (2009)：FOR 类训练提升面试官对人格的判断准确性](https://doi.org/10.1111/j.2044-8325.2009.00089.x)
- [Baret & de Kock (2018)：机制指向 dispositional reasoning，不是动机或自我效能](https://doi.org/10.1111/ijsa.12213)
- [Freudenstein et al. (2020)：情境建构（situational construal）预测人际表现](https://doi.org/10.1111/ijsa.12290)

**准确率与偏差是两个维度**
- [West & Kenny (2011)：truth and bias model](https://doi.org/10.1037/a0022568)
- [Lyusin & Ovsyannikova (2016)：强制选择格式的局限，建议用多维度量尺](https://doi.org/10.11621/pir.2016.0301)

**把意图变成行动（if-then 计划）**
- [Gollwitzer & Sheeran (2006)：94 个独立检验，d=.65](https://doi.org/10.1016/S0065-2601(06)38002-1)
- [Sheeran, Listrom & Gollwitzer (2024)：642 个检验，条件式 d=.43 优于日程式 d=.29](https://doi.org/10.1016/bs.adms.2024.03.001)

**对话行为与回应性**
- [Huang et al. (2017)：提问——尤其是追问——提升被喜欢的程度](https://doi.org/10.1037/pspi0000097)（[2025 更正与审计材料](https://osf.io/rymv8/)）
- [Yeomans et al. (2019)：追问的收益是累积的](https://doi.org/10.1037/pspi0000182)
- [Bolger & Amarel (2007)：未被察觉的支持有效，被察觉的支持无效甚至加重反应（N=257）](https://doi.org/10.1037/0022-3514.92.3.458)

**友善被误读成好感**
- [La France et al. (2009)：元分析，平均 r≈.32](https://doi.org/10.1080/03637750903074701)
- [Bendixen (2014)：88% 的女性报告友善曾被误读成性兴趣](https://doi.org/10.1016/j.paid.2014.01.013)
- [Haselton & Buss (2000)：错误管理理论](https://doi.org/10.1037/0022-3514.78.1.81)
- [Perilloux, Easton & Buss (2012)：速配法直接测量误读量级](https://doi.org/10.1177/0956797611424801)


---

## v0.5 新增参考

**道德许可（为什么不能让 App 给人盖章）**
- [Sachdeva, Iliev & Medin (2009)：写自己正面道德特质的人随后捐钱最少](https://doi.org/10.1016/j.jesp.2009.02.009)
- [Merritt, Effron & Monin (2010)：道德自我许可综述「当好人使你得以作恶」](https://doi.org/10.1111/j.1751-9004.2010.00265.x)
- [Monin & Miller (2001)：道德凭证与非偏见的表达](https://doi.org/10.1006/jesp.2000.1438)
- [Jordan, Mullen & Murnighan (2011)：回想自己的善行降低后续亲社会意愿](https://doi.org/10.1016/j.jesp.2010.12.005)

**自我肯定（怎么做才有用）**
- [Sherman & Cohen (2002)：自我肯定降低防御，机制是稳住自我价值才吞得下威胁信息](https://doi.org/10.1111/1467-8721.00160)

**身份与行为**
- [Oyserman & Lewis (2017)：身份动机理论——身份必须挂在具体行为上才有动力作用](https://doi.org/10.1111/sipr.12031)
- [Oyserman, Fryberg & Yoder (2007)：身份动机与健康行为](https://doi.org/10.1037/0022-3514.93.2.188)

**迁移（为什么必须有现实任务）**
- [社交技能训练的迁移问题](https://doi.org/10.1037/0033-2909.84.3.459)


---

## v0.6 新增参考

**让步与付出（吃点亏什么时候有益）**
- [Rusbult, Verette, Whitney, Slovik & Lipkus (1991)：让步的定义与六个实证研究](https://doi.org/10.1037/0022-3514.60.1.53)
- [Impett, Gordon, Kogan, Oveis, Gable & Keltner (2010)：接近型 vs 回避型动机，同一付出两种结局](https://doi.org/10.1037/a0020014)
- [Le & Agnew (2003)：投资模型元分析（52 项研究，约 1.1 万人）](https://doi.org/10.1037/0022-3514.85.2.374)

**情绪垃圾桶（共同反刍）**
- [Rose (2002)：共同反刍同时预测亲密友谊与抑郁焦虑（n=608）](https://doi.org/10.1111/1467-8624.00509)
- [Rose, Carlson & Waller (2007)：双向关系的纵向证据](https://doi.org/10.1111/j.1467-8624.2007.01019.x)
- [Spendelow, Simonds & Avery (2017)：共同反刍与内化症状的元分析](https://doi.org/10.1007/s10578-016-0681-0)
- [Tompkins & Tang (2011)：成年早期样本未发现纵向关联（反面证据）](https://doi.org/10.1016/j.adolescence.2010.11.001)


---

## v0.7 新增参考

**摩擦门控（打游戏前先完成）**
- [Grüning, Riedel & Lorenz-Spreen (2023)：PNAS，n=280，六周，打开次数降 57%](https://doi.org/10.1073/pnas.2213114120)
  （预注册实验 n=500：放弃选项最强，摩擦其次，**单纯提示语无效**）
- [Haliburton 等 (2024)：CHI，131 万次交互的设计摩擦纵向研究](https://doi.org/10.1145/3613904.3642337)

**习惯养成与漏掉一天**
- [Lally 等 (2010)：96 人 12 周，错过一次无实质影响，累积错过才有害](https://doi.org/10.1002/ejsp.674)
- [Marlatt & Gordon 复发预防：what-the-hell effect / 破堤效应](https://doi.org/10.1007/978-1-4613-4697-3)


---

## v0.8 新增参考

**透明度错觉（憋需求的代价）**
- [Gilovich, Savitsky & Medvec (1998)：人高估自己内心状态被看穿的程度](https://doi.org/10.1037/0022-3514.75.2.332)
- [Savitsky & Gilovich (2003)：把这个偏差讲清楚本身就能降低焦虑、提升表现](https://doi.org/10.1016/S0022-1031(03)00026-X)

**幽默与关系**
- [Martin 等 (2003)：幽默风格问卷（亲和／自我增强／攻击／自我贬损）](https://doi.org/10.1016/S0191-8869(02)00306-9)
- [Hall (2017)：自贬型幽默与更低的关系满意度](https://doi.org/10.1080/10810730.2016.1261973)
- [Winterheld 等 (2013)：冲突里亲和型幽默减少愤怒，自贬型反而加重](https://doi.org/10.1016/j.jrp.2013.04.002)
- [Cann 等 (2011)：攻击型幽默与伴侣尴尬和低满意度](https://doi.org/10.1016/j.paid.2011.05.010)

**Android 实现（浮层闸门与桥）**
- [AccessibilityService：无法阻止应用启动，只能观察 + 叠加](https://developer.android.com/reference/android/accessibilityservice/AccessibilityService)
- [DevicePolicyManager.setPackagesSuspended：真正让应用打不开的另一条路](https://developer.android.com/reference/android/app/admin/DevicePolicyManager#setPackagesSuspended(android.content.ComponentName,%20java.lang.String%5B%5D,%20boolean))
