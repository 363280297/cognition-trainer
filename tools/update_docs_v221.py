# -*- coding: utf-8 -*-
"""README + PLAN 记录 2.21：把「表达体检」「真实复盘」修好 + 实测成本。

两件必须写进文档的事：

1. **那两个模块原来在手机上一直是死的。** 它们调 /api/ai/*，而手机里没有服务端，
   离线适配层一律回 503。实测点「体检」「出题」都只得到一句
   「这是离线单文件版，不含 AI 功能」。现在提示词和调用都搬到前端走原生桥。
   文档里要把这件事写明白，因为它同时解释了「为什么成本比预想的低」——
   原来那两个模块根本没在消耗。

2. **成本要给出实测数字**，而不是「大概很便宜」。量法是把 Node 侧的真发请求
   暴露给页面，让 App 自己拼的请求真发出去、读服务端返回的 usage。
   结论：一个场景约半分钱。

顺带把 README 里那句已经不准确的话改掉（离线版横幅原来写「AI 那两个需要
电脑上的 server.py」，现在不需要了）。
"""
import io
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
RP = os.path.join(ROOT, 'README.md')
PP = os.path.join(ROOT, 'PLAN.md')


def sub(s, old, new, n=1, tag=''):
    assert s.count(old) == n, f'{tag} 期望 {n} 次，实得 {s.count(old)}：{old[:70]!r}'
    return s.replace(old, new)


def main():
    r = io.open(RP, encoding='utf-8').read()

    r = sub(r, """### 语音识别（把你的话转成文字）""",
            """### 一次练习花多少钱（实测）

用真实请求量过（做法：把 Node 侧的真发请求函数暴露给页面，让 App 自己拼的
请求真发出去，读服务端返回的 `usage`）。单位是元，按 2026-09-10 官方 Flash 系列
价格（`deepseek-chat` 属于这个系列）：空闲时段输入命中 0.02 / 未命中 1 / 输出 4，
高峰时段翻倍。

| 步骤 | 次数 | 输入 | 输出 | 缓存命中 | 空闲 | 高峰 |
|---|---|---|---|---|---|---|
| 建场景 | 1 | 422 | 150 | 61% | 0.00077 | 0.00154 |
| 对话 | 8 | 4910 | 274 | 68% | 0.00274 | 0.00549 |
| 对话后复盘 | 1 | 614 | 360 | 21% | 0.00193 | 0.00386 |
| 表达体检 | 1 | 506 | 369 | 0% | 0.00198 | 0.00396 |
| 真实复盘（出题+揭晓） | 2 | 624 | 862 | 0% | 0.00407 | 0.00814 |
| **合计** | 13 | 7076 | 2015 | 52% | **0.0115 元** | 0.023 元 |

**一个场景（建场景 + 8 轮对话 + 复盘）= 10 次调用 ≈ 0.54 分钱（空闲）／1.09 分（高峰）。**
一次把体检和真实复盘也用上 ≈ 1.15 分钱。

按月推算（只用场景对话）：一天 2 局约 **0.33 元/月**，一天 5 局约 0.82 元/月，
一天 10 局约 1.63 元/月。也就是说**就算天天练，一个月也就几毛钱。**

三条从数字里看出来的事：

- **钱主要花在输出上。** 输出是 4 元/百万，比未命中的输入贵 4 倍、比命中的贵 200 倍。
  而输入有 52% 命中缓存（对话那几轮更是 68%），因为固定的系统提示词坐在每次请求最前面，
  正好吃到前缀缓存。这个结构本来就是对的，没什么可优化。
- **一局里的固定成本是两个「收尾」调用。** 建场景 + 复盘只占输出的一半以上，
  而 8 轮对话本身才 274 个输出 token。所以**少开新局、把一局聊透最省**。
- **表达体检和真实复盘的缓存命中是 0%**，因为它们每次的系统提示词都配着不同的用户内容，
  而且输出天然长（出题 396、揭晓 466 个 token）。这是它们贵一档的原因，不是 bug。

### 语音识别（把你的话转成文字）""", tag='cost')

    r = sub(r, """### 对话之后：评价 + 改进措施""",
            """### 表达体检 / 真实复盘现在走的是同一条路

这两个模块原来调的是 `/api/ai/checkup` 和 `/api/ai/replay/*` ——
也就是**只存在于电脑上 server.py 里的接口**。而 APK 里没有服务端，
离线适配层对这三个地址一律回 503，所以**在手机上它们一直是死的**：
点「体检」和「出题」都只会得到一句「这是离线单文件版，不含 AI 功能」。

现在提示词和调用都搬到前端了（`public/app.js` 里三份 `*_SYSTEM` 常量 +
`aiCheckup` / `aiReplayQuestions` / `aiReplayReveal`），统一走 `llmCall`：
**手机上是原生桥，电脑上是 `/api/proxy`**，两边行为一致、也只需要你那把密钥。
`server.py` 里那三个端点已经删掉——留着两处提示词必然会各自漂移。

### 对话之后：评价 + 改进措施""", tag='modules')

    r = sub(r, """versionCode 已经是 22，**可以直接覆盖安装旧版，数据不丢**。""",
            """versionCode 已经是 23，**可以直接覆盖安装旧版，数据不丢**。""", tag='vc')

    r = sub(r, """node tools/check_voice_pack.js   # 音色列表/试听/下载入口、识别模式真的传下去了""",
            """node tools/check_voice_pack.js   # 音色列表/试听/下载入口、识别模式真的传下去了
node tools/check_ai_modules.js   # 体检/复盘不再走 /api/ai/*（用会失败的 fetch 证明）
node tools/measure_cost.js       # 真实请求量一次练习花多少钱（要密钥，会花钱）
node tools/measure_tokens.js     # 真实请求量一局的 token 用量""", tag='tools')

    io.open(RP, 'w', encoding='utf-8').write(r)

    # ---------------- PLAN ----------------
    p = io.open(PP, encoding='utf-8').read()
    BLOCK = """> **v0.9.6（2.21）变更（已实现）：修好两个死掉的 AI 模块 + 实测成本**
>
> 用户问「会不会 token 消耗太多了，分析一下」，然后「要修复的地方你自己修复，
> 然后告诉我一个场景完成之后大概消耗多少钱」。
>
> **1. 查成本时发现：两个 AI 模块在手机上一直是死的。**
> 「表达体检」和「真实复盘」调的是 `/api/ai/checkup` 和 `/api/ai/replay/*`——
> **只存在于电脑上 server.py 里的接口**。APK 里没有服务端，离线适配层对这三个地址
> 一律回 503。实测点「体检」和「出题」都只拿到一句「这是离线单文件版，不含 AI 功能」。
>
> 也就是说 AI 页里三个模块平起平坐地摆着、README 也把它们列为 AI 模块，
> 而实际只有「场景对话」能用（它走原生桥，不经过 /api/*）。离线版顶上那条横幅
> 写着「AI 那两个需要电脑上的 server.py」，说明当初是有意为之——但它和现在的
> App 已经矛盾了：**手机端才是主产物**。
>
> 修法：三份提示词和三次调用都搬到前端（`public/app.js` 的 `aiCheckup` /
> `aiReplayQuestions` / `aiReplayReveal`），统一走 `llmCall`——手机走原生桥、
> 电脑走 `/api/proxy`，两边一致，且只需要用户那一把密钥。
> `server.py` 那三个端点删掉，避免两处提示词各自漂移。
> 顺带删掉随调用者一起失效的 `post()` 助手（又一个「死代码」实例）。
>
> *漏改一处、被测试抓住*：搬家时 `post()` 返回的是 `{ok, result}`，
> 而 `llmCall` 直接返回解析好的对象。体检那处我改对了，
> 复盘那五处 `r.result.xxx` 忘了改 → 一提交就报
> 「Cannot read properties of undefined (reading 'questions')」。
> 典型症状是「函数名换了、参数也对，只有返回值多套了一层」。
>
> **2. 顺手发现一个真 bug：`**` 会露给用户看，而且是选择性的。**
> 跑测试时随机翻到一张卡，诊断里印着 `**明确提问**`。查下来是一整类：
>
>     1098  esc(picked.why)       ← 你选错那条的解释：星号露出来
>     1108  rich(correctOpt.why)  ← 正确那条的解释：星号变粗体
>     1556  rich(r.quote) / 1557 esc(r.face_value) ← 同一张卡上下两行不一致
>
> **同一张卡里一个对一个错**，说明不是有意规则而是漏改；而出问题的恰好是
> 「你选错的那条为什么错」——用户最需要看的那段。
> `rich()` 就是 `esc()` 加一次安全的 `**`→`<b>`（先转义再替换，不可能引入 XSS），
> 所以统一改成 rich()：**一条规则（内容文本一律走 rich）比逐个字段判断可靠**，
> 而逐个判断正是当初漏掉的原因。共 20 处。
>
> **3. 一条写脆了的断言，顺手改成测设计不变式。**
> `check_tabs_unique` 里「刷卡片页是题目」原来数正文里 A/B/C/D 的个数，
> 但**卡片是随机抽的**，而带判局的卡片会故意先把四个选项藏起来
> （先判断「这是什么局」再选动作，这是 App 的核心设计）。
> 所以抽到判局卡时必然失败——这条断言时好时坏（实测 4 次里挂 2 次）。
> 改成直接问 DOM，并把设计不变式也断言上：**判局在时选项必须是藏着的**。
> 另外给「`**` 不许露出来」加了一条**确定性**的守卫：指定一张错项诊断里带 `**`
> 的卡，故意答错，断言渲染成粗体且无星号——靠随机抽卡碰上不可靠，
> 而这条 bug 原来就是靠随机抽卡才偶然发现的。
>
> **4. 实测成本（这是用户真正要的答案）。**
> 量法：把 Node 侧的真发请求函数用 `page.exposeFunction` 暴露给页面，
> 让 App 自己拼的请求真发出去，读服务端返回的 `usage`。
> 所以量的是 **App 真实发出的请求**，不是别处复刻的提示词。
>
> 一个场景（建场景 + 8 轮对话 + 复盘）= 10 次调用，5946 输入 + 784 输出，
> **约 0.54 分钱（空闲时段）／1.09 分（高峰）**；把体检和真实复盘也用上 ≈ 1.15 分。
> 一天 2 局 ≈ 0.33 元/月。
>
> 三条从数字里看出来的事：
>   - **钱主要在输出上**（4 元/百万，是未命中输入的 4 倍、命中输入的 200 倍）。
>     输入有 52% 命中缓存（对话那几轮 68%），因为固定系统提示词坐在每次请求最前面，
>     正好吃到前缀缓存——这个结构本来就对，没什么可优化的。
>   - **一局的固定成本是两个收尾调用**：建场景 + 复盘占输出的一半以上，
>     而 8 轮对话本身才 274 个输出 token。所以**少开新局、把一局聊透最省**。
>   - 体检和复盘的缓存命中是 0%，且输出天然长（出题 396 / 揭晓 466 token），
>     这是它们贵一档的原因，不是 bug。
>
> **5. APK 2.21（versionCode 23）。** 新增 `tools/check_ai_modules.js`、
> `tools/measure_cost.js`。权限仍然是 7 个。
>
> **v0.6 → v0.7 变更（已实现）**"""
    anchor = "> **v0.6 → v0.7 变更（已实现）**"
    assert p.count(anchor) == 1
    p = p.replace(anchor, BLOCK)
    io.open(PP, 'w', encoding='utf-8').write(p)
    print('README + PLAN 已更新到 2.21（含实测成本表）')


if __name__ == '__main__':
    main()
