# -*- coding: utf-8 -*-
"""README + PLAN 跟着 2.16 更新。

两件事：
  1. 闸门不再是「手填包名」，改成从已安装应用里挑——README 里那一段得跟着改，
     否则文档教的是已经不存在的方法。
  2. PLAN 补一条 v0.9.3，把这次那个「两个界面一样」的根因写清楚：
     `cardsView` 只被写、从来没被读——**设了一个没人读的状态**。
     这是这个项目里第二次栽在同一类问题上（第一次是内容白名单漏加键），
     所以值得单独记它的共同形状。
"""
import io
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
RP = os.path.join(ROOT, 'README.md')
PP = os.path.join(ROOT, 'PLAN.md')


def sub(s, old, new, n=1, tag=''):
    assert s.count(old) == n, f'{tag} 期望 {n} 次，实得 {s.count(old)}：{old[:70]!r}'
    return s.replace(old, new)


def main():
    # ---------------- README ----------------
    r = io.open(RP, encoding='utf-8').read()

    r = sub(r, """**开启步骤**（App 里点设置 → 每日计划，有直达按钮）：""",
            """**怎么选要拦的应用**：设置 → 每日计划 → 「从已安装应用里挑」，读一份
**有启动图标的应用**清单（也就是你能从桌面点开的那些），搜名字或搜包名，点一下选中、
再点一下取消，选中的打勾。挑到一半退出也不会丢——点一下就立刻存了。

比原来手填包名强在哪：手打包名打错一个字母，闸门就永远不触发，而界面上**没有任何异常**，
只会让人觉得这功能没用。现在包名从「要背的东西」变成「挑一下就行」。

双开的应用、没有启动图标的系统入口不在那个清单里，那种仍然可以手填包名
（同一页下面有个折叠起来的「手动填包名」）。

**这个功能没有多要任何权限。** Android 11+ 默认看不到别的应用，通常的做法是申请
`QUERY_ALL_PACKAGES`（一个广权限，商店要单独审核）——这里刻意没有用，
只在 manifest 里声明了要查的那一个 intent（`<queries>`），
所以读到的就是「有启动图标的应用」，权限列表仍然是 7 个。
（`tools/test_bridge.js` 里有一条断言盯着权限数量与不可残留那几项。）

**开启步骤**（App 里点设置 → 每日计划，有直达按钮）：""", tag='picker')

    r = sub(r, """versionCode 已经是 17，**可以直接覆盖安装旧版，数据不丢**。""",
            """versionCode 已经是 18，**可以直接覆盖安装旧版，数据不丢**。""", tag='vc')

    r = sub(r, """node tools/check_voice_talk.js   # 免提状态机（装 native 桩打回调）、随机、复盘""",
            """node tools/check_voice_talk.js   # 免提状态机（装 native 桩打回调）、随机、复盘
node tools/check_tabs_unique.js  # 十个页面正文两两不同、闸门挑应用、界面不许出现 ** 标记""",
            tag='tools')

    io.open(RP, 'w', encoding='utf-8').write(r)

    # ---------------- PLAN ----------------
    p = io.open(PP, encoding='utf-8').read()
    BLOCK = """> **v0.9.3（2.16）变更（已实现）：修「两个界面一样」+ 闸门改成挑应用**
>
> 用户报了两件：「练习和AI点进去发现两个界面变成一样了」；
> 「闸门的设置，那个包名太麻烦了，你直接读取应用，然后我告诉你选哪些就行」。
>
> **1. 「两个界面一样」的根因：设了一个没人读的状态。**
>
> 查下来是一行代码的事：`cardsView` 这个变量在 5 个地方被赋值，
> **没有任何一处读它**。`viewCards()` 无条件走 `renderCard()`，于是：
>
>     练习 → 卡片        → 刷卡片那一页
>     成长 → 偏差画像    → 也是刷卡片那一页     ← 应该是偏差画像
>     成长 → 我的预案    → 还是刷卡片那一页     ← 应该是我的预案
>
> 来由是上一轮界面归并（5 tab → 4 tab）：我把这两个视图从卡片页搬到成长，
> `VIEWS.growth` 里写了 `cardsView = 'bias'; viewCards();`，
> **以为 viewCards 会看这个变量——但它从来不看。**
>
> 这类 bug 的形状值得记住，因为它和这个项目上次栽的坑是同一个：
> **写了一个状态，但没有任何一处读它。** 上次是内容白名单（`CONTENT.stages`
> 一直是 null 而接口返回得好好的），这次是子页开关。
> 两次都是「不报错、不抛异常，功能静默退化」，两次都是靠人事后发现。
>
> 所以修完之后加的不是「检查一遍三个渲染函数」，而是一条**结构性断言**
> （`tools/check_tabs_unique.js`）：**十个页面（4 个 tab × 二级子页）的正文两两不同。**
> 任何一次「某个子页悄悄退化成另一个子页」都会立刻失败，不依赖人去逐个看。
> 比对时必须把二级切换条排除掉——「阶段|偏差画像|我的预案」这三个标签在
> bias 和 plans 下**文字完全相同**（只有高亮不同），带着它比会恰好漏掉这个 bug。
>
> 顺带：那条断言还扩到了 rich() 的三个消费方（卡片诊断、微课、复盘），
> 所以 rich() 本身哪天坏了也会被抓到。
>
> **2. 闸门改成从已安装应用里挑。** 原来只有一个输入框要手打
> `com.tencent.tmgp.sgame` 这种包名。手打的失败方式很难受也很隐蔽：
> 打错一个字母，闸门永远不触发，而界面上没有任何异常。
>
> 原生新增 `listApps()`：查 `MAIN`/`LAUNCHER` 的 activity，返回 `{label, pkg}`，
> 按应用名排序、按包名去重、排除自己。网页侧是一个面板：搜索（应用名和包名都能搜）、
> 点一下选中再点一下取消、选中的打绿勾、顶上说清已选几个。
>
> 几个刻意的取舍：
>   - **没有申请 `QUERY_ALL_PACKAGES`。** Android 11+ 默认看不到别的应用，
>     常规做法是要这个广权限，但这里只在 manifest 里声明要查的那一个 intent
>     （`<queries>`）就够了。这个 App 的底线是不做流氓软件，能不多要一个权限就不多要。
>     构建后核对过：**权限仍然是 7 个**，包内 manifest 里 `QUERY_ALL_PACKAGES` 不存在。
>   - **手填包名的入口保留**，收进折叠。双开的应用、没有启动图标的入口不在清单里，
>     全砍掉等于把那条路堵死。
>   - **点一下就立刻存**，不是等「完成」才提交。挑到一半接个电话退出去，
>     等提交的写法会把已经挑的全丢掉。
>   - **只重画被点的那一行**，不重画整个列表——列表可能有一两百项，
>     重画会把滚动位置和搜索词都清掉。
>   - 读不到应用 / 原生抛错，都要写出原因并指出手填那条路还在，不留一个空列表。
>
> **3. 顺带修掉两处「** 直接显示给用户看」。** 我在闸门那段写了一句
> `列表里只有**有启动图标的应用**`，界面上真的显示了两个星号。顺着查发现
> 不是只有这一处：`httpProblem()` 里那句「检查接口地址是不是**完整端点**」
> 也从 2.13 起就一直在设置页上露着，还有提醒那句「只在**没达标**的时候响」。
>
> 根因是这个 App 有**两条文本通道，而规则相反**：
> `rich()` 走 innerHTML（`**` 变粗体，内容文件里用了 100 多处）／
> `textContent` 原样显示（`**` 就是两个星号）。写文案时挑错通道不报错、只是难看，
> 所以一直没人发现。修法：进 textContent 的一律不带 markdown 标记。
> 断言：扫**真实渲染出来的文本节点**，出现 `**` 就失败——它不关心走的哪条通道。
> （这条断言我第一版写错了：扫了 `document.body.textContent`，而离线单文件版
> 把所有 JS 和内容 JSON 都内联在 `<script>` 里，源码注释里的 `**` 全被误报。
> 改成只看非 script/style 元素的直接文本节点才对。）
>
> **4. APK 2.16（versionCode 18）。** 新增 `tools/check_tabs_unique.js`。
>
> **v0.6 → v0.7 变更（已实现）**"""
    anchor = "> **v0.6 → v0.7 变更（已实现）**"
    assert p.count(anchor) == 1
    p = p.replace(anchor, BLOCK)
    io.open(PP, 'w', encoding='utf-8').write(p)
    print('README + PLAN 已更新到 2.16')


if __name__ == '__main__':
    main()
