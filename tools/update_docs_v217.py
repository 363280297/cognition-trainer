# -*- coding: utf-8 -*-
"""README + PLAN 跟着 2.17 更新。

这一版的结论比较特殊，值得在文档里写明白：**对话功能一直是好的，
「不像豆包」的原因是一个默认值**。所以 README 里要给出实测数字，
让以后改这块的人不用再花一次请求去量；PLAN 里要记下「改默认值必须同时迁移」
这个坑——只改代码默认值对老用户完全没有效果，这是最容易漏的一步。
"""
import io
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
RP = os.path.join(ROOT, 'README.md')
PP = os.path.join(ROOT, 'PLAN.md')


def sub(s, old, new, n=1, tag=''):
    assert s.count(old) == n, f'{tag} 期望 {n} 次，实得 {s.count(old)}：{old[:70]!r}'
    return s.replace(old, new)


def main():
    r = io.open(RP, encoding='utf-8').read()

    # 在「场景对话怎么玩」那一节后面插一段讲模型和延迟
    r = sub(r, """### 对话之后：评价 + 改进措施""",
            """### 用什么模型：这一项直接决定「像不像对话」

真实请求量出来的每轮耗时（`tools/probe_chat.py`，2.17 实测）：

| 模型 | 每轮耗时 | 推理 | 适合 |
|---|---|---|---|
| **`deepseek-chat`** | **约 1.6 秒** | 无 | **对话用这个（默认）** |
| `deepseek-flash` | 约 4.6 秒 | 1015 字 | 能用，但等得出来 |
| `deepseek-v4-pro` | **约 30 秒** | 5006 字 | 写内容可以，对话不行 |

**这两个数字之间就是「像不像豆包」的全部差别。** 对话功能本身（多轮、
每轮 JSON 字段齐全、语气和内心活动）一直是通的，实测内容也像真人
（「没多久，刚到。停车是挺难的，这附近。」），但一轮等三十秒那就不是对话了。

所以 2.17 把默认模型换成了 `deepseek-chat`，**并且迁移了已经装好的旧设置**——
这一点很关键：手机上 localStorage 里已经存着 `deepseek-v4-pro`，
只改代码里的默认值对老用户**完全没有效果**（默认值只在没存过时生效）。
设置页会把「替你换过、为什么换」写出来，想换回去点一下名字就行。

另外：**一轮实测超过 8 秒时会主动提示**。判断依据是实测延迟而不是模型名——
模型可用性会变，而「这一轮等了几秒」永远是真的。如果当前已经在用最快的那个，
提示就改说网络/地址，不会去劝你换成一个你已经用的模型。

（建场景和复盘这两个更复杂的提示词也在 `deepseek-chat` 上验过：
9 字段的场景对象 1.77 秒、带嵌套数组的复盘 3.33 秒，都稳定吐合格 JSON。）

### 情景是直接给你选好的

进 AI 页不需要先做选择——**顶部已经摆好一个**「今天就练这个」，一个按钮开始。
选法是本地的：练得最少的优先，同分时难度低的优先（不花钱、不发请求）。
「换一个情景」也是本地换。想自己出题或按弱项现造，在下面两条路里。

轮换不能省：少了它推荐会一直推同一个，你点两次就再也不看推荐了。
所以每练完一局会给那个情景记一笔（只记现成的六个，现造的场景每次 id 都不同，
记了就只是垃圾数据）。

### 对话之后：评价 + 改进措施""", tag='modelSection')

    r = sub(r, """versionCode 已经是 18，**可以直接覆盖安装旧版，数据不丢**。""",
            """versionCode 已经是 19，**可以直接覆盖安装旧版，数据不丢**。""", tag='vc')

    r = sub(r, """node tools/check_tabs_unique.js  # 十个页面正文两两不同、闸门挑应用、界面不许出现 ** 标记""",
            """node tools/check_tabs_unique.js  # 十个页面正文两两不同、闸门挑应用、界面不许出现 ** 标记
node tools/check_doubao.js       # 模型默认值、旧设置迁移、慢轮提示、情景直接选好
py -3 tools/probe_chat.py        # 真实请求量每轮耗时（要密钥，会花钱）""", tag='tools')

    io.open(RP, 'w', encoding='utf-8').write(r)

    # ---------------- PLAN ----------------
    p = io.open(PP, encoding='utf-8').read()
    BLOCK = """> **v0.9.4（2.17）变更（已实现）：让对话真的像对话 + 情景直接选好**
>
> 用户问：「你检测一下那个 ai 对话的功能，能不能像豆包一样那样交流。
> 然后情景是你直接选好的。」
>
> **结论先说：对话功能一直是通的，不像豆包的原因是一个默认值。**
>
> 用真实请求量了每轮耗时（`tools/probe_chat.py`）：
>
>     deepseek-v4-pro    30.65s/轮    推理 5006 字   ← App 原来的默认值
>     deepseek-flash      4.63s/轮    推理 1015 字
>     deepseek-chat       1.59s/轮    推理    0 字   ← 像豆包的是这个
>
> 多轮下来 JSON 字段齐全、内容也像真人（「没多久，刚到。停车是挺难的，这附近。」），
> 所以引擎没问题。**但一轮等三十秒那就不是对话，是写信。**
> 豆包的体感靠的就是「说完一秒内有回应」，这一项就把差别解释完了。
>
> 顺带纠正一个我之前的判断：上一轮用户报 404 时，我因为「本地脚本用
> `deepseek-v4-pro` 成功生成过 336 条变体」而断定这个模型名没问题。
> 名字确实是对的，但**它不适合对话这件事我没看出来**——生成是批量的、
> 可以等，对话不行。模型名有效 ≠ 适合这个用途。
>
> **1. 换默认模型，并且迁移已经装好的旧设置。**
> 这是本次最容易漏的一步：用户手机上 localStorage 里已经存着 `deepseek-v4-pro`，
> **只改代码里的默认值对他完全没有效果**——默认值只在「没存过」的时候生效，
> 而他早就存过了。「改了默认值但没迁移」= 什么都没改。
> 所以：改默认值 + 一次性迁移（只迁移正好等于老默认值的那一种，
> 用户自己手填过的其它名字不动）+ 设置页写明「替你换过、为什么换」。
>
> **2. 用实测延迟自检，而不是写死模型名。**
> 一轮超过 8 秒就主动提示。判断依据是**实测耗时**而不是模型名——
> 模型可用性会变（今天三个，下个月未必），而「这一轮等了几秒」永远是真的。
> 如果当前已经在用最快的那个，提示改说网络/地址，不劝人换成一个他已经在用的模型
> （第一版就是那么写的，被自己的测试抓出来了）。
>
> **3. 设置页的模型选择带实测速度。** 点名字就切换，下面写着「实测约 N 秒一轮」。
> 用户唯一需要知道的就是这个数字。
>
> **4. 情景直接选好。** 用户要「情景是你直接选好的」，所以进 AI 页顶部就已经
> 摆好一个「今天就练这个」，一个按钮开始。选法是**本地**的：练得最少的优先、
> 同分时难度低的优先——故意不用模型现造，那样每次打开 AI 页都要等一次请求、花一次钱，
> 而大多数人打开只是先看一眼。「换一个情景」也是本地换。
> 为此给每个现成情景记练过几次（只记现成的六个；现造的 id 每次不同，记了是垃圾数据），
> 少了这个轮换，推荐会一直是同一个。
>
> **5. 又一次「界面露着 ** 」**：截图里看到「接口地址要填**完整端点**」。
> 上一轮我加的那条断言没抓到它，因为断言只打开了「每日计划」面板、
> **没打开主设置页**——断言是对的，但覆盖面缺了一块，而缺的那块正好是出问题的地方。
> 这种「断言写对了但没盖到」比断言写错更危险，因为它给人已经测过的错觉。
> 现在扩到全部五个面板（主设置页/每日计划/挑应用/卡住了/闸门）。
>
> **6. 一个流程教训。** 上一个脚本因为一个锚点没对上就整体退出了，
> 而「练完一局记一笔」那段代码排在失败点**之后**，于是根本没写进去——
> 是测试里那条 `state.scenes` 断言把它抓出来的。
> 每处替换都断命中次数让脚本立刻停下（这是对的），但**脚本中途失败时，
> 必须确认它到底写进去了哪几处**，否则会留下一个半成品，而它看起来像成功。
>
> **7. APK 2.17（versionCode 19）。** 新增 `tools/check_doubao.js`、
> `tools/probe_chat.py`、`tools/probe_prompts.py`、`tools/probe_flash.py`。
>
> **v0.6 → v0.7 变更（已实现）**"""
    anchor = "> **v0.6 → v0.7 变更（已实现）**"
    assert p.count(anchor) == 1
    p = p.replace(anchor, BLOCK)
    io.open(PP, 'w', encoding='utf-8').write(p)
    print('README + PLAN 已更新到 2.17')


if __name__ == '__main__':
    main()
