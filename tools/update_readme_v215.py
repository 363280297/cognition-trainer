# -*- coding: utf-8 -*-
"""README 跟着 2.15 更新。

这一版把「语音陪练」变成了「场景对话」，又把它从「按住说话」变成了「免提」，
所以 README 里那一整节都得重写——它现在描述的是两个版本之前的交互。
"""
import io
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
P = os.path.join(ROOT, 'README.md')

VOICE_SECTION = """### 场景对话怎么玩

**三条路进同一个场景**：自己写一句提示词让它造、点「随机来一个」让它按你的弱项出题、
或者直接点开一个现成场景。那 6 个现成场景是打磨过的固定题目，随机那个每次都不一样。

**它默认是免提的——说完停一下，自己就发出去了。** 这是这一版最大的改动。
原来只有「按住说话」：按住才收音、松开才发。那是个对讲机，不是对话——
手不能离开屏幕，中途也不能停下来想一下。现在的循环是：

```
开始收音 → 你说话 → 系统判定你说完了 → 发出去 → 对方在想
        → 对方出声 → 说完 → 自动开始收音（回到第一步）
```

「系统判定你说完了」这一步**没有自己写 VAD**：Android 的 `SpeechRecognizer`
本来就会在静音之后自动结束并回调 `onResults`，这里只是把那个回调接起来。

按钮的文案**就是点下去的后果**，四种状态四种含义，因为这个麦克风必须随时能关掉：

| 状态 | 显示 | 点一下会怎样 |
|---|---|---|
| 该你说了 | 开始说 | 开麦 |
| 在听你说 | 说完了，发出 | 立刻把已经说的发出去（不用等它自己判静音） |
| 对方在想 | 对方在想… | （按钮禁用） |
| 对方在说 | 打断他/她 | 打断，并立刻开始听你说 |

有三件事是专门处理过的，因为它们的失败**在界面上看不出来**：

- **绝不在对方出声的时候开麦克风。** 否则对方的话会被自己录进去，变成自问自答。
  所以只有收到「说完」的回调之后才重新收音，并再等 350 毫秒（有些机型回调比实际
  尾音早到）。这条有断言直接量：调 `listen()` 的那一刻 `V.speaking` 必须是 false。
- **没听到声音不能无限重试。** 你可能只是把手机放下了。连续 3 次空结果就停，
  把控制权交回来，而不是让麦克风一直开着。
- **权限或引擎出错不静默重试**，说清楚并关掉免提——而且**写进设置**，因为下一次
  进来原因还是一样的，不记住就是每次白开一次麦克风。空结果只在本次暂停，不写设置。

不想用免提（比如在嘈杂的地方）就点右上角的开关切回「按住说话」，两种模式随时切，
**切换不会清掉已经聊过的记录**。

**它不是一个开放聊天。** 开放聊天里模型会配合你、顺着你说，你练的是「聊得下去」；
这里对方有立场、有没说出口的期待、也会因为你的失分而降温，你练的是
「在具体情况下做对那一个动作」。这个 App 的全部内容都指向后者，所以这里刻意
**没有**做成一个聊天框。

**随机不是图省事。** 自己选题会稳定地偏向自己已经擅长的那类——练得舒服，
但练不到短板上。所以随机池是由零件拼出来的（谁 / 在哪 / 要练什么 / 难在哪 /
最容易怎么错），再叠上你当前的偏差画像，让随机偏向你的弱项，而不是均匀随机。

### 对话之后：评价 + 改进措施

结束一局会给你两块东西。一块是**统计**（接住了几次、没接住几次、温度、
提了几个问题、哪一句最该回头看），另一块是**复盘**——一次单独的模型调用，
把整段对话交给它，让它给：

- 整体评价（像教练，不像鼓励师）
- 一个**反复出现的模式**（这是逐轮评分看不出来的东西）
- 做对了的、下一局要保持的
- 要改的动作 + **下一次可以这么说**的原话示例
- 下一局只盯这一件事

三条硬约束写进了提示词里，因为这类输出最容易烂成鸡汤：

1. **必须引用你真正说过的原话**（所以每一轮都把你的原话记进了记录里——
   原来只存了模型判定的「你这句发出了什么信号」，转述和引用原话在教练这件事上
   差别很大）。
2. **不许恭维。** 整体不好就直说；只有一处做得好就只说那一处。
3. 每条改进必须是**一个具体动作 + 一句原话示例**，不许写「多倾听」「要有同理心」
   这类正确但没法执行的话。

复盘会把**对方的开场白**也算进去（记作「第 0 轮」）。少了它，模型判不出你的
第一句有没有接上对方说的话——而那恰恰是最容易被看出来有没有听进去的一句。

**人称跟着场景走。** 场景对象带一个 `ta`（他/她），界面上的「他的温度」「他心里的
想法」都从它取。随机场景会造出男同事、男上级，写死「她」的话那一局的对不上。
6 个老场景都是女性、不带这个字段，默认「她」，行为不变。同理，扮演角色的提示词里
不再写死性别，性别由场景的 `her` 决定。

"""


def main():
    with io.open(P, encoding='utf-8') as f:
        s = f.read()

    start = s.index('### 场景对话怎么玩\n')
    end = s.index('---\n\n## Android App（真正的手机客户端）')
    s = s[:start] + VOICE_SECTION + s[end:]

    subs = [
        ("`android/认知训练.apk` —— 0.40 MB，装到手机上就独立运行，**不需要电脑**。",
         "`android/认知训练.apk` —— 0.41 MB，装到手机上就独立运行，**不需要电脑**。"),
        ("versionCode 已经是 16，**可以直接覆盖安装旧版，数据不丢**。",
         "versionCode 已经是 17，**可以直接覆盖安装旧版，数据不丢**。"),
        ("| **场景对话** | AI | **先给一句提示词说你想练什么，AI 把它造成一个具体场景**，然后按住说话——对方真的会用语音回你。每轮给你评分（妙/好/平/失误/漏着）、**他心里其实在想什么**、以及一个温度计，温度降到底就结束了。结束报告里统计**你提了几个问题、其中几个是追问**。另有 6 条示例提示词可一键填入再改 |",
         "| **场景对话** | AI | **默认免提——说完停一下自己就发出去了**，不用一直按着。三条路进场景：写提示词造、随机按弱项出题、点开现成的。每轮给你评分（妙/好/平/失误/漏着）、**对方心里其实在想什么**、以及一个温度计。**结束后给一份复盘**：整体评价、反复出现的模式、要改的动作 + 下一次可以这么说 |"),
        ("- 场景对话里的「对方」是模型演的，**他的反应不是现实中的必然**。它训练的是你的观察和回应习惯，不是预测某一个具体的人。",
         "- 场景对话里的「对方」是模型演的，**他/她的反应不是现实中的必然**。它训练的是你的观察和回应习惯，不是预测某一个具体的人。"),
        ("- 场景对话每轮都要调一次模型（**建场景也算一次**），会消耗你自己的 API 额度，一局 10 轮大约几毛钱。",
         "- 场景对话每轮都要调一次模型（**建场景和最后的复盘各算一次**），会消耗你自己的 API 额度，一局 10 轮大约几毛钱。"),
        ("**关于权限**：前两个是为场景对话，",
         "**关于权限**：`RECORD_AUDIO` 是免提收音用的，`INTERNET` 是对话用的，"),
        ("""node tools/check_scene_talk.js   # 建场景页：提示词在前、示例一键填入、坏输入不放行""",
         """node tools/check_scene_talk.js   # 建场景页：提示词在前、现成场景可点、坏输入不放行
node tools/check_voice_talk.js   # 免提状态机（装 native 桩打回调）、随机、复盘"""),
    ]
    miss = []
    for old, new in subs:
        if s.count(old) != 1:
            miss.append((s.count(old), old[:60]))
            continue
        s = s.replace(old, new)
    if miss:
        for n, t in miss:
            print(f'  !! 命中 {n} 次：{t}')
        raise SystemExit('有段落没对上')

    with io.open(P, 'w', encoding='utf-8') as f:
        f.write(s)
    print(f'README 已更新到 2.15（重写语音那一节 + {len(subs)} 处）')


if __name__ == '__main__':
    main()
