# -*- coding: utf-8 -*-
"""README + PLAN 记录 2.19/2.20：语音包（音色）+ 识别精度。

这两件事必须写清楚一个诚实的天花板：
  · 手机自带的合成引擎（不管哪个牌子）出来的声音**都不等于真人**。
    想接近真人只能走云端神经语音合成，那需要另一个服务和另一把密钥，
    而 DeepSeek 没有语音合成接口。所以这一版的定位是
    「把系统里已有的嗓子都用起来」而不是「给你一个和豆包一样的声音」——
    把后者写进文档就是撒谎。
  · 识别（语音转文字）本来就是有的，但被一行 `EXTRA_PREFER_OFFLINE = true`
    强制成离线了，准确率白白变差。这个改动值得单独记，因为它的取舍逻辑
    在别处或许成立（省流量、不怕被墙），在这个 App 里不成立（对话本来就要联网）。
"""
import io
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
RP = os.path.join(ROOT, 'README.md')
PP = os.path.join(ROOT, 'PLAN.md')


def sub(s, old, new, n=1, tag=''):
    assert s.count(old) == n, f'{tag} 期望 {n} 次，实得 {s.count(old)}：{old[:70]!r}'
    return s.replace(old, new)


def main():
    r = io.open(RP, encoding='utf-8').read()

    r = sub(r, """### 情景是直接给你选好的""",
            """### 对方的声音（音色）和「语音包」

设置页 → 对方的声音，列出**这台手机自己的合成引擎里所有中文音色**，每一条标着
「可离线 / 需联网」「音质高/中/很高」和一个能区分彼此的短编号（ccc / ddd 这种），
可以试听。旁边还有两个按钮：**去系统里下载音色**、**打开系统语音设置**。

**为什么是「音色」而不是自带语音包。** Android 上「语音包」就是指合成引擎的
音色数据包：Google 语音服务、讯飞、华为、小米各自带几个到十几个中文音色，
很多还是可下载的。所以这个 App 自己不做音色包——那等于往安装包里塞几十 MB，
质量还不如系统自带的那几个。做的是把已有的列出来、能挑、能试听、能引导去下载。

**一个诚实的边界：这些声音都不等于真人。** 手机自带的合成引擎（不管哪个牌子）
是轻量模型，出来的是「清楚的合成音」，不是「像真人」。想接近真人只能换成云端
神经语音合成（豆包、火山、Azure、MiniMax 那类），那需要**另一个服务的另一把密钥**，
而且每句话都要联网、有延迟、按字计费。**DeepSeek 没有语音合成接口**，
所以只填 DeepSeek 的密钥是拿不到真人音的。

如果你想上云端音色，最省事的路是找一个 OpenAI 兼容的 `/audio/speech` 服务，
把地址和密钥填进设置——目前这一版还没接这条路，需要的话说一声。

另外设置页会显示**当前实际生效的音色**。有了它才能看出「选了但没生效」——
音色没下载、或者被引擎忽略时，声音会静默退回默认，而界面上的勾还打着。

### 语音识别（把你的话转成文字）

一直是可以的：`SpeechRecognizer` 录你的声音 → 转成文字 → 当作用户这一轮发出去。
设置页有个「说一句话试试识别」，会把识别结果**原样**显示出来 + 当前用的模式，
方便你自己判断准不准。

**这一版修掉了一个真问题**：原来那行是 `EXTRA_PREFER_OFFLINE = true`，也就是
**强制走离线识别**。当时的理由是「能离线就别联网，也避免被墙的云引擎拖住」——
在这个 App 里这条理由不成立：**对话本身就必须联网**（要调大模型），
所以强制离线换不来任何离线能力，只是白白牺牲准确率（离线中文模型小得多，
长句子和口语错得更多）。现在改成可选，默认「优先在线」。
要如实说明的是：这只是**偏好**，引擎有权不听，所以界面上没写成保证。

### 情景是直接给你选好的""", tag='voiceSection')

    r = sub(r, """versionCode 已经是 20，**可以直接覆盖安装旧版，数据不丢**。""",
            """versionCode 已经是 22，**可以直接覆盖安装旧版，数据不丢**。""", tag='vc')

    r = sub(r, """node tools/check_doubao.js       # 模型默认值、旧设置迁移、慢轮提示、情景直接选好""",
            """node tools/check_doubao.js       # 模型默认值、旧设置迁移、慢轮提示、情景直接选好
node tools/check_voice_pack.js   # 音色列表/试听/下载入口、识别模式真的传下去了""", tag='tools')

    io.open(RP, 'w', encoding='utf-8').write(r)

    # ---------------- PLAN ----------------
    p = io.open(PP, encoding='utf-8').read()
    BLOCK = """> **v0.9.5（2.19/2.20）变更（已实现）：音色（语音包）+ 识别精度**
>
> 用户问：「他是不是应该会有语音包那种的？发出类似真人的声音。
> 然后这个对话也能读取我的音频，然后识别成文字。」
>
> **第二个问题的答案：一直可以。** `SpeechRecognizer` 录你的声音转成文字当用户那一轮。
> 但顺着查出一个真问题（见 2）。**第一个问题查出三个问题**，其中一个是
> 「这功能等于不存在」（见 1）。
>
> **1. `voices()` 早就写好了，但网页一次都没调用过。**
> 所以用户从来没能挑过音色，一直用系统默认那个嗓子。
> 这正好是上一版刚踩过的同一类 bug：**写了能力，但没有任何入口能碰到它**
> （上次是 `cardsView` 只写不读）。
>
> 而且更值得记的是：`tools/test_bridge.js` 里**早就有**这条检查
> （「Bridge 里暴露了但网页从不调用」），只是被写成**软提示**，理由是
> 「死方法不会导致功能坏掉，硬失败会让人习惯性忽略输出」。
> 这个理由现在被事实否掉了：死方法的确不会让功能坏掉，**它会让功能不存在**——
> 用户想要的语音包就是被它吃掉的。所以改成硬失败，并留一份**写明原因**的白名单
> （`httpGet` / `deepBlockStatus` / `openAppSettings`，每条都写了为什么保留）。
> 改完立刻抓出我自己新加却没人调用的两个方法（`currentVoice` / `openTtsSettings`）。
>
> **2. 识别被强制离线。** `EXTRA_PREFER_OFFLINE = true` 写死的，
> 注释理由是「能离线就别联网，也避免被墙的云引擎拖住」。
> 这条理由在这个 App 里不成立：**对话本身就必须联网**，所以强制离线换不来任何
> 离线能力，只是白白牺牲准确率（离线中文模型小得多，长句更易错）。
> 改成用户可选、默认「优先在线」，并如实说明这只是偏好、引擎有权不听。
>
> 还修了一处关联的路：等麦克风权限期间要**记住当初请求的是哪种模式**，
> 否则用户选了「优先离线」、点了允许，重试时却按在线跑。
>
> **3. 音色列表（语音包）。** Android 上「语音包」就是合成引擎的音色数据包。
> 所以不做自带音色（那等于往安装包里塞几十 MB，还不如系统自带的），
> 而是：列出本机所有中文音色 + 试听 + 去系统里下载音色 + 打开系统语音设置。
> 音色名（`zh-cn-x-ccc-local`）对用户没意义，翻成
> 「中文（中国大陆）· 可离线 · 音质高 · ccc」这类判断依据。
>
> **从截图里发现的一个真缺陷**：两行都显示「可离线 · 音质高」，只有底下的小字不同——
> 而这是个**用来「挑一个」的列表**，两项长得一样等于没得挑。所以把命名里那段
> speaker 编号（ccc / fff，引擎里确实是不同的嗓子）抽出来当短标签。
> 抽不出来就不加，不硬凑。
>
> 另外加了一个「当前实际生效的音色」显示：音色没下载或被引擎忽略时，
> 声音会静默退回默认而勾还打着——把真实状态显示出来，这种不一致才看得见。
>
> **4. 一个诚实的天花板，写进 README 而不是含糊过去。**
> 手机自带的合成引擎出来的**都不等于真人**。「像真人」只能走云端神经语音合成，
> 需要另一个服务的另一把密钥，而且 **DeepSeek 没有语音合成接口**——
> 所以只填 DeepSeek 密钥拿不到真人音。这一版给的是「把系统里已有的嗓子都用起来」，
> 不是「和豆包一样的声音」。想要云端音色，最省事的路是接一个 OpenAI 兼容的
> `/audio/speech`；这一版**没有**接。
>
> **5. APK 2.20（versionCode 22）。** 新增 `tools/check_voice_pack.js`（27 项）。
> 权限仍然是 7 个——这些能力都在系统里，没有新增任何权限。
>
> **v0.6 → v0.7 变更（已实现）**"""
    anchor = "> **v0.6 → v0.7 变更（已实现）**"
    assert p.count(anchor) == 1
    p = p.replace(anchor, BLOCK)
    io.open(PP, 'w', encoding='utf-8').write(p)
    print('README + PLAN 已更新（含「真人声音」的天花板说明）')


if __name__ == '__main__':
    main()
