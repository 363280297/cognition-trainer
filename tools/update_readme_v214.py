# -*- coding: utf-8 -*-
"""README 跟着 2.14 更新。

这一版动了三件会让人读 README 时被误导的事，所以必须一起改，不能只改版本号：
  1. 底部 tab 从 5 个变成 4 个（「课程」并进「练习」，卡片页的两个子视图并进「成长」）
  2. 「语音陪练」变成了「场景对话」——场景不再写死，而是由提示词生成
  3. 变体从每张 2 条变成 4 条，微课从 29 条变成 33 条，APK 从 0.25 MB 变 0.40 MB

README 里那种「说了一件事但已经不是那么回事」的句子，比没有文档更坏：
它是下一轮改动的地基，而地基是错的。
"""
import io
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
P = os.path.join(ROOT, 'README.md')

EDITS = [
    # ---- 六个模块表：语音陪练 → 场景对话 ----
    (
        "| **语音陪练** | AI | 主界面。**按住说话，她真的会用语音回你**。每轮给你评分（妙/好/平/失误/漏着）、她的内心独白、以及一个温度计——温度降到底就结束了。结束报告里会统计**你提了几个问题、其中几个是追问**（6 个场景，难度递进） |",
        "| **场景对话** | AI | **先给一句提示词说你想练什么，AI 把它造成一个具体场景**，然后按住说话——对方真的会用语音回你。每轮给你评分（妙/好/平/失误/漏着）、**他心里其实在想什么**、以及一个温度计，温度降到底就结束了。结束报告里统计**你提了几个问题、其中几个是追问**。另有 6 条示例提示词可一键填入再改 |",
    ),
    (
        "| **心理学** | 离线 | **29 条微课**，分三个系列：关系心理学 / 操控识解 / 证据纠错。每条标注证据强度，配一道应用题 |",
        "| **微课** | 离线 | **33 条微课**，分三个系列：关系心理学 / 操控识解 / 证据纠错。每条标注证据强度，配一道应用题 |",
    ),
    (
        "| **每日卡片** | 离线 | 一句真实的话 + 4 个选项，判断对方实际在表达什么。**84 张**（62 张输入侧 + 10 张「怎么接话」+ 12 张「读局」），每张带 4 条表面变体",
        "| **每日卡片** | 离线 | 一句真实的话 + 4 个选项，判断对方实际在表达什么。**84 张**（62 张输入侧 + 10 张「怎么接话」+ 12 张「读局」），每张带 4 条表面变体",
    ),

    # ---- tab 那一节：5 → 4 ----
    (
        "### 底部为什么是 5 个 tab（原来是 7 个）\n\n"
        "原来是 7 个平铺 tab，问题不是挤，是**分类维度混着**：AI 的三个功能（陪练 / 体检 / 复盘）散在三处，而它们其实是同一类事（都要联网、都要填密钥）；离线训练的两个（卡片 / 校准）也散着。改成按「你要做什么」分：\n\n"
        "| tab | 里面有什么 |\n"
        "|---|---|\n"
        "| **今天** | 打开就该看到的：达标进度、闸门状态、现实任务对账、当前关卡 |\n"
        "| **练习** | 卡片（含 我的预案 / 偏差画像 两个子视图）、语境校准 |\n"
        "| **AI** | 语音陪练、表达体检、真实复盘——**三个在线功能全部归这里** |\n"
        "| **成长** | 阶段评估、身份陈述、天花板声明 |\n"
        "| **课程** | 29 条微课 |\n\n",

        "### 底部为什么是 4 个 tab（原来是 7 个）\n\n"
        "原来是 7 个平铺 tab，问题不是挤，是**分类维度混着**：AI 的三个功能（对话 / 体检 / 复盘）散在三处，而它们其实是同一类事（都要联网、都要填密钥）；离线训练的两个（卡片 / 校准）也散着。改成按「你要做什么」分：\n\n"
        "| tab | 里面有什么 |\n"
        "|---|---|\n"
        "| **今天** | 打开就该看到的：达标进度、闸门状态、现实任务对账、当前关卡 |\n"
        "| **练习** | 卡片、语境校准、微课 |\n"
        "| **AI** | 场景对话、表达体检、真实复盘——**三个在线功能全部归这里** |\n"
        "| **成长** | 阶段评估、偏差画像、我的预案 |\n\n"
        "**为什么又砍掉一层。** 上一版把「我的预案 / 偏差画像」放在卡片页里当子视图，于是卡片 → 子视图 → 卡片内容成了三层，而那两个东西其实都不是「刷卡片」，它们是对**已经积累下来的记录**做回顾——和「阶段评估」是一类事，所以并进「成长」。微课同理，它不是练习，是阅读，但为了不再多一个 tab，和练习放在一起。\n"
        "另外「阶段评估」原来在成长里是唯一的入口，身份陈述和天花板声明挂在它内部；现在成长页直接给三块：阶段 / 画像 / 预案。\n\n",
    ),

    # ---- 卡片页三个视图 → 挪到成长 ----
    (
        "### 卡片页里的三个视图\n\n卡片页顶部有三个切换：**刷卡片 / 我的预案 / 偏差画像**。后两个不是装饰，各自对应一个研究结论。",
        "### 偏差画像与我的预案（在「成长」里）\n\n这两个原来在卡片页顶部当子视图，现在挪进「成长」。它们不是装饰，各自对应一个研究结论。",
    ),
    (
        "**表面变体（每次看到的不完全一样）。** 每张卡带 2 条表面变体：",
        "**表面变体（每次看到的不完全一样）。** 每张卡带 4 条表面变体：",
    ),

    # ---- 语音陪练怎么玩 → 场景对话怎么玩 ----
    (
        "### 语音陪练怎么玩\n\n她不是助手，是**演一个具体的人**。",
        "### 场景对话怎么玩\n\n**第一步是给提示词。** 你说一句「我想练什么」，AI 把它造成一个场景：对方是谁、此刻在发生什么、\n"
        "他嘴上没说但心里在意的东西、你这一局的目标、以及这一局最常见的错法。这一步不是包装——\n"
        "**它决定了这是训练还是闲聊**。开放聊天里模型会配合你、顺着你说，你练的是「聊得下去」；\n"
        "场景对话里模型有立场、有没说出口的期待、也会因为你的失分而降温，你练的是「在具体情况下做对那一个动作」。\n"
        "所以这里刻意**没有**做成「随便聊」的聊天框。\n\n"
        "生成出来的场景沿用 `data/scenarios.json` 那套字段（`her` / `her_state` / `opening` / `goal` / `trap`），\n"
        "所以下面那套对话机制一行都没有动。那 6 个老场景也不浪费，转成了 6 条示例提示词。\n"
        "开场白是这套机制的地基：模型漏了它就直接报错，**绝不放一个空场景进去**——否则界面会正常切到对话页，\n"
        "然后卡在「她说：undefined」，用户只会觉得「语音功能坏了」。\n\n"
        "**对方不是助手，是演一个具体的人**。",
    ),
    (
        "### 语音陪练第一次使用要配置\n\n点右上角 **⚙**",
        "### 场景对话第一次使用要配置\n\n点右上角 **⚙**",
    ),

    # ---- APK 体积与 versionCode ----
    (
        "`android/认知训练.apk` —— 0.25 MB，装到手机上就独立运行，**不需要电脑**。",
        "`android/认知训练.apk` —— 0.40 MB，装到手机上就独立运行，**不需要电脑**。",
    ),
    (
        "六个离线模块完整可用；语音陪练需要联网，并需要你自己的 DeepSeek API key（见下）。",
        "六个离线模块完整可用；场景对话需要联网，并需要你自己的 DeepSeek API key（见下）。",
    ),
    (
        "**关于权限**：前两个是为语音陪练，",
        "**关于权限**：前两个是为场景对话，",
    ),
    (
        "versionCode 已经是 15，**可以直接覆盖安装旧版，数据不丢**。",
        "versionCode 已经是 16，**可以直接覆盖安装旧版，数据不丢**。",
    ),
]


def main():
    with io.open(P, encoding='utf-8') as f:
        s = f.read()
    miss = []
    for old, new in EDITS:
        if s.count(old) != 1:
            miss.append((s.count(old), old.splitlines()[0][:70]))
            continue
        s = s.replace(old, new)
    if miss:
        for n, t in miss:
            print(f'  !! 命中 {n} 次：{t}')
        raise SystemExit('有段落没对上，先看一眼再改（不要用模糊替换）')
    with io.open(P, 'w', encoding='utf-8') as f:
        f.write(s)
    print(f'README 已更新（{len(EDITS)} 处）')


if __name__ == '__main__':
    main()
