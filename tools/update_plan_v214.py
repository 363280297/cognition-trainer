# -*- coding: utf-8 -*-
"""PLAN.md 补 v0.9.1（2.14）变更块，并给 2.13 块那两条「没做完」标注结果。

2.13 那一块最后写着「本次没做完的两件」：界面归并、AI 语音对话。
2.14 把两件都做了，所以那段文字现在是不准确的。留着一句不准确的「没做完」
比没有记录更坏——下次读的人会以为还要做，或者以为已经试过并放弃了。
所以不是删掉它，而是就地标注结果（保留判断过程）。
"""
import io
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
P = os.path.join(ROOT, 'PLAN.md')

NEW_BLOCK = """> **v0.9.1（2.14）变更（已实现）**
>
> 用户在这一版前的最后指令：「你自己决定界面规划。只要分成几个常用功能，用的最多的。
> 不要把它摊开。语音功能选后者，先给提示词，然后建造一个场景。」
> （「后者」指的是我在两版之间给的选项：自由聊天 vs 场景对话——他选了场景对话。）
>
> **1. 界面：5 tab → 4 tab，第三层彻底消掉。**
> 「我的预案 / 偏差画像」原来挂在卡片页下当子视图，于是卡片 → 子视图 → 卡片内容成了三层；
> 但这两个东西都不是「刷卡片」，它们是对**已经积累下来的记录**做回顾，和「阶段评估」同类，
> 所以并进「成长」（成长页直接给三块：阶段 / 画像 / 预案）。微课不是练习、是阅读，
> 但为了不再多一个 tab，和「练习」放在一起。现在四个是：今天 / 练习 / AI / 成长。
> 实现上 `growthSubview` + `SUBNAV.growth` 新增；`setCardsView` 改成只做重定向（兼容旧调用）；
> `cardsNav()` 返回空串。二级切换条仍然**只在 `go()` 里注入一次**——这个坑的说明还在下一节。
>
> **2. AI 语音对话：先给提示词，再建造一个场景。**
>
> *先澄清一个事实（我读代码后才发现，值得记下来）*：**对话引擎本来就在**。
> 原来的「语音陪练」不是念台词，它已经是「按住说话 → 模型演对方 → 每轮告诉你他心里在想什么」，
> 带轮次、温度条、追问计数。真正缺的只有用户点出来的那一步：
> **场景原来是写死的 6 个，不能从自己的提示词生成。** 所以这不是新功能，是补一个前端入口。
>
> 做法：
>   - 无会话时渲染**建场景页**：一句提示词（多行框）+ 6 条示例提示词，
>     点示例是**填进输入框再改**，不是直接开跑。
>   - `buildScenario(prompt)` 调一次模型，要求返回
>     `{title, stage, difficulty, her, her_state, opening, goal, trap}`
>     ——**和 `data/scenarios.json` 完全同形状**，所以下面那套对话机制一行都没动。
>   - `startSession` 改成「场景对象或 id」都收；那 6 个老场景按 id 照样能开，两条路都通。
>
> *刻意没做成开放聊天。* 理由不是界面，是训练目标：开放聊天里模型会配合你、顺着你说，
> 你练的是「聊得下去」；场景对话里模型有立场、有没说出口的期待、会因为你的失分降温，
> 你练的是「在具体情况下做对那一个动作」。这个 App 的全部内容都指向后者——
> 换成开放聊天等于换了一个产品。
>
> **一个必须挡住的静默失败**：建场景是一次模型调用，返回值什么都有可能（多了外层键、少了开场白）。
> 如果只是把返回值往 session 里一塞，界面会正常切到对话页，然后卡在「她说：undefined」——
> **用户在界面上看不出失败，只会觉得「语音功能坏了」**（这个项目已经踩过好几次同类坑）。
> 所以：缺 `opening` 直接报错、绝不放行；模型多套一层外层键时自动拆开
> （在 `Object.values` 里找带 `opening` 的那层），不白报错。
> `tools/check_scene_talk.js`（30 项）三层都盯着：界面（提示词在前、示例可点、缺密钥有出口）、
> 坏输入（空提示词 / 缺开场白 / 外层套一层都不许变成能进的会话）、以及进得去会话。
>
> **3. APK 2.14（versionCode 16）。** 新增浏览器检查 `check_scene_talk.js`。
> README 里顺手清掉两类会过期的东西：一是「测试合计 N 项」那种计数
> （改成逐条写它盯什么——数字每加一条断言就过期，而过期的数字看起来仍像事实），
> 二是三处「语音陪练」旧称。
>
> **4. 顺手修的仓库不一致（如实记下）。** `eq-app/android/` 下的源码副本还是最早那版脚手架
> （versionCode 2、只有 `MainActivity.java`），而真正构建用的是 `E:\\android-build\\app`
> （6 个源文件、versionCode 15）。已把目录和产物同步成实际构建的那一份。
> 桌面上的 `认知训练.apk`（未加版本号那个）原本是 **2.6**——最容易拿到的文件名反而是最旧的，
> 已用 2.14 覆盖，2.13 保留在同名的带版本号文件里可回退。
>
> **v0.6 → v0.7 变更（已实现）**"""


def main():
    with io.open(P, encoding='utf-8') as f:
        s = f.read()

    anchor = "> **v0.6 → v0.7 变更（已实现）**"
    assert s.count(anchor) == 1, s.count(anchor)
    s = s.replace(anchor, NEW_BLOCK)

    # 给 2.13 那两条「没做完」就地标注结果，不删判断过程
    old = ("> **本次没做完的两件（如实记下）**：\n"
           ">   - 界面的归并简化（用户说「很多窗口可以归为一类」）只做了「!」这一步，\n"
           ">     底部五个 tab 与二级子页的结构没动。\n"
           ">   - AI 的语音对话（像大模型那样说一句答一句）没做。现有的语音陪练是\n"
           ">     「系统给你一句台词 + 语音识别」，不是自由对话；要改成自由对话需要\n"
           ">     接一个新的对话回路（STT → 模型 → TTS，带打断和轮次控制），\n"
           ">     属于新功能，不是改配置。")
    new = ("> **本次没做完的两件（已在 v0.9.1 / 2.14 补上，判断过程保留）**：\n"
           ">   - 界面的归并简化（用户说「很多窗口可以归为一类」）只做了「!」这一步，\n"
           ">     底部五个 tab 与二级子页的结构没动。\n"
           ">     → **2.14 已做**：5 tab → 4 tab，卡片页下的第三层消掉。\n"
           ">   - AI 的语音对话（像大模型那样说一句答一句）没做。现有的语音陪练是\n"
           ">     「系统给你一句台词 + 语音识别」，不是自由对话；要改成自由对话需要\n"
           ">     接一个新的对话回路（STT → 模型 → TTS，带打断和轮次控制），\n"
           ">     属于新功能，不是改配置。\n"
           ">     → **2.14 已做，但结论和这里的判断不同**：读代码后发现对话回路**本来就有**\n"
           ">     （`nextTurn` → `llmCall` → 她带语气/评分/内心活动地回应），缺的只是\n"
           ">     「场景由提示词生成」这个入口。所以没有新写回路，只补了入口——\n"
           ">     这一条记错的判断留在这里，比改掉更有用。")
    assert s.count(old) == 1, s.count(old)
    s = s.replace(old, new)

    with io.open(P, 'w', encoding='utf-8') as f:
        f.write(s)
    print('PLAN.md 已补 v0.9.1（2.14）')


if __name__ == '__main__':
    main()
